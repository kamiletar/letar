# PLAN-INFRA — Инфраструктурные треки (вне auth-плана)

> Вынесено из `PLAN.md` §21 (2026-07-21) — секции §15–§20 не связаны с темой auth (для которой
> заведён `PLAN.md`), а сами себя помечали «добавлено по аналогии». Нумерация секций (`§15`…`§20`)
> сохранена как есть — на неё ссылаются `.claude/docs/*.md`, журнал сессий в `PLAN.md` и переписка
> агентов; перенос не меняет номера, только физическое расположение файла.
>
> §18.7 (тираж e2e-гейта) закрыт 2026-08-25 — весь тираж M (M1–M4) пройден, hard-gate подтверждён
> живьём на всех целевых приложениях. См. `PLAN-JOURNAL-1.md` §21 для истории решения о переносе.

---

## Назначение файла

`PLAN-INFRA.md` — журнал инфраструктурных треков монорепо letar: сервера, деплой, безопасность,
CI, координация агентов, документация — всё, что не относится напрямую к теме auth-плана
(`PLAN.md`). Секции пронумерованы `§NN` (нумерация исторически началась с §15, не перенумеровывается
при правках и переносах — см. выше). Статус секции виден в заголовке:

- без пометки или `🆕` — открыта, в работе или в очереди;
- `✅ ЗАКРЫТО (дата)` — завершена;
- `🟡 ЧАСТИЧНО` — частично сделано, есть открытый остаток;
- `🔥 ПРИОРИТЕТ` — берётся вперёд остальных открытых секций; такие секции перечислены ниже.

2026-08-20: файл превысил 10000 строк и был разрезан на 4 части по диапазонам §NN — процесс
разреза и правила навигации по нему описаны в
[plan-decomposition-pattern.md](/.claude/docs/plan-decomposition-pattern.md). Нумерация §NN при
разрезе не менялась.

## Карта частей

| Часть                               | Диапазон §NN | Тема (ориентировочно, границы не строгие)                 |
| ----------------------------------- | ------------ | --------------------------------------------------------- |
| [PLAN-INFRA-1.md](/PLAN-INFRA-1.md) | §15–§25      | сервер s3, deploy MCP, e2e-гейт, форматтер worktree, СДЭК |
| [PLAN-INFRA-2.md](/PLAN-INFRA-2.md) | §26–§48      | `libs/ui`, SEO, npm-публикация, деплой, redis, Traefik    |
| [PLAN-INFRA-3.md](/PLAN-INFRA-3.md) | §49–§61      | firewall, hard e2e-gate, cron-задачи, security-инциденты  |
| [PLAN-INFRA-4.md](/PLAN-INFRA-4.md) | §62–§147     | канарейка, GlitchTip, CI-гейт, Agent Mail                 |

Нужен конкретный `§NN` — ищи диапазон в таблице выше и открывай соответствующий файл. Это
**единственная** карта диапазонов: в самих частях её копий нет.

⚠️ **Добавил секцию в конец части — обнови диапазон в двух местах:** строка этой таблицы и
заголовок `# PLAN-INFRA-N — §X–§Y` в первой строке самого файла. Больше диапазон нигде не
записан. Сверка первой и последней секции файла:

```bash
grep -o '^## §[0-9]*' PLAN-INFRA-4.md | sed 's/^## §//' | sort -n | sed -n '1p;$p'
```

До 2026-08-28 та же таблица дублировалась в каждой части — шесть копий одного диапазона, и они
разъезжались: у части 4 одновременно жили четыре разных значения (`§62–§94`, `§62–§105`,
`§62–§113`, `§62–§118`). Дубли убраны, в частях осталась строка-указатель сюда.

## Приоритет сейчас

Секции, которые берутся вперёд остальных открытых. Список короткий намеренно: если в нём больше
двух-трёх пунктов, он перестаёт быть приоритетом и становится вторым оглавлением.

Пусто — большая ревизия 2026-09-02: закрыты [§60](/PLAN-INFRA-3.md) (инфра-сервисы вне git на
s2/s3/mail — kubo, relay, nginx-proxy-manager, maddy, все теперь git-трекаемые),
[§50](/PLAN-INFRA-3.md) (фолбэк `--frozen-lockfile → bun install` — root cause сам исчез),
[§90](/PLAN-INFRA-4.md) (s3 — второй offsite-получатель бэкапов s2, плюс попутно закрыт
смежный пробел: собственный traefik-бэкап s3 не реплицировался никуда). Дополнительно доведены
до предела возможного без решения владельца [§52](/PLAN-INFRA-3.md) (root cause 401 давно
починен, последний диагностический пункт — `ApiLog` на проде `driving-school` — проверен
напрямую в БД, хвоста нет) и [§56](/PLAN-INFRA-3.md) (зомби-проверка cron-задач чистая, механизм
ретира подтверждён на первом реальном случае §75). Следующий приоритет пока не выбран.
