# §18.8.1 PLAN-INFRA.md — манифест scripts/deploy-infra.sh для infra/traefik НА s2.
#
# ⛔ КРИТИЧНО: сюда НЕ добавлять acme-dns-accounts.json.enc. Тот .enc содержит урезанный ключ
# только для s3.letar.best (вырезан из полного файла s2 в M1a — см. infra/traefik/README.md,
# раздел «Учётные данные acme-dns»). На s2 по пути /home/deploy/lego/acme-dns-accounts.json
# уже лежит ПОЛНЫЙ файл с ключом зоны letar.best — расшифровка урезанной s3-копии поверх него
# уничтожит доступ к продлению боевых сертификатов без возможности восстановления без
# владельца (новый аккаунт acme-dns = новые fulldomain = переделывать CNAME у регистратора).
# docker-compose.s2.yml монтирует этот файл read-only напрямую, в обход этого манифеста.

dashboard-users.enc:/home/deploy/letar/infra/traefik/auth/dashboard-users:600
