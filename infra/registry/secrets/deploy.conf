# §157 PLAN-INFRA-6.md — манифест scripts/deploy-infra.sh для infra/registry.
# Формат: <файл>.enc:<целевой_путь>:<права_chmod>
#
# ⚠️ Целевой путь — внутри infra/traefik/auth/, не infra/registry/. Basic-auth для
# registry реализован middleware'ом Traefik (см. docker-compose.yml, label
# traefik.http.middlewares.registry-auth.basicauth.usersfile), а не встроенной
# аутентификацией самого registry:2 — файл должен лежать там, где Traefik уже монтирует
# ./auth в свой контейнер (infra/traefik/docker-compose.yml), без правки самого Traefik.

registry-users.enc:/home/deploy/letar/infra/traefik/auth/registry-users:600
