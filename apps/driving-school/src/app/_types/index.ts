/**
 * Общие типы приложения
 *
 * @example
 * ```ts
 * import type { VehicleBase, VehicleFileData } from '@/app/_types'
 * import { getVehiclePhotoUrl } from '@/app/_types'
 * ```
 */

export { getVehiclePhotoUrl } from './vehicle'
export type { VehicleBase, VehicleFileData } from './vehicle'
