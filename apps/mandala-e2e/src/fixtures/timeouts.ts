/**
 * Таймауты для операций, упирающихся в компиляцию Next dev
 *
 * Дефолтные таймауты живут в `playwright.config.ts`. Здесь — отдельный запас для трёх
 * операций, которые дороже обычного ожидания элемента:
 *
 * - загрузка изображения (`/api/upload` тянет sharp — холодный route handler компилируется
 *   единицы десятков секунд);
 * - Server Action с `redirect()` (компилируется и action, и целевая страница);
 * - переход по ссылке на ещё не скомпилированный маршрут.
 *
 * Против staging (`BASE_URL` задан) приложение уже собрано — там остаются прежние 15 секунд,
 * подобранные под параллельную нагрузку на общий контейнер.
 */
export const SLOW_ACTION_TIMEOUT = process.env['BASE_URL'] ? 15000 : 45000
