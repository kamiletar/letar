{
  "name": "kami-key-the-landing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/kami-key-the-landing/src",
  "projectType": "application",
  "tags": ["type:app", "scope:kami-key-the", "owner:letar"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next build",
        "cwd": "apps/kami-key-the-landing"
      },
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/apps/kami-key-the-landing/.next"],
      "metadata": {
        "description": "Build production bundle",
        "technologies": ["next"]
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next dev -p 3011",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": false,
      "metadata": {
        "description": "Start development server on port 3011",
        "technologies": ["next"]
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next start -p 3011",
        "cwd": "apps/kami-key-the-landing"
      },
      "dependsOn": ["build"],
      "cache": false,
      "metadata": {
        "description": "Start production server",
        "technologies": ["next"]
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "dependsOn": ["oxlint"],
      "options": {
        "command": "eslint .",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/.eslintrc.json"],
      "metadata": {
        "description": "Run ESLint",
        "technologies": ["eslint"]
      }
    },
    "oxlint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx oxlint src --config ../../.oxlintrc.json",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/.oxlintrc.json"],
      "metadata": {
        "description": "Fast lint with oxlint",
        "technologies": ["oxlint"]
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx dprint fmt \"**/*.{ts,tsx,js,jsx,json,md}\"",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": false,
      "metadata": {
        "description": "Format files with dprint",
        "technologies": ["dprint"]
      }
    },
    "format:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx dprint check \"**/*.{ts,tsx,js,jsx,json,md}\"",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/dprint.json"],
      "metadata": {
        "description": "Check file formatting with dprint",
        "technologies": ["dprint"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/tsconfig.base.json", "{projectRoot}/tsconfig.json"],
      "metadata": {
        "description": "Type-check TypeScript files",
        "technologies": ["typescript"]
      }
    },
    "typecheck:tsgo": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo --noEmit",
        "cwd": "apps/kami-key-the-landing"
      },
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/tsconfig.base.json", "{projectRoot}/tsconfig.json"],
      "metadata": {
        "description": "Type-check with tsgo (38x faster)",
        "technologies": ["typescript", "tsgo"]
      }
    }
  }
}
