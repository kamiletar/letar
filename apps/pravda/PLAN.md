# Pravda - План разработки

## Текущая версия: 1.9.0

## Текущий статус

✅ Фазы 1–7 завершены — перенесены в `PLAN_COMPLETED.md` (2026-08-09).

Активная работа — см. Backlog ниже.

---

## Backlog

### 🟡 Тема

- [ ] Аудит `_active: scale()` в `src/theme/recipes/*.ts` на `pressScale` (`@letar/ui`) — задача
      описана в [press-scale-audit-task.md](/.claude/docs/press-scale-audit-task.md)

### 🔴 Приоритетные баги — staging e2e (найдено BlackCove, §18.7 Тираж M1, 2026-07-22)

**Раунд 2026-08-12 (214/240 passed, 18 failed, 5 flaky) — частично закрыто:**

- [x] **Застрявший прогресс-бар TOC + случайный активный пункт** — `toc.tsx`/`mobile-toc.tsx`:
      `cancelAnimationFrame` в cleanup не сбрасывал `rafIdRef.current` в `null`, `handleScroll()`
      следующей инстанции эффекта видел «устаревший» id и навсегда пропускал кадр. Плюс два
      конкурирующих `IntersectionObserver` (Section-контейнер и вложенный Chapter) боролись за
      `aria-current`. Коммит `850f0f62`
- [x] **webkit: дублированная разметка `Article`** — `mdx/article.tsx` рендерил КАЖДУЮ статью
      дважды (отдельный `Flex` для мобильного и десктопного лейаута, оба в DOM одновременно, просто
      один `display:none`) — `bookmarks.spec.ts` матчил скрытый мобильный `<BookmarkButton>`,
      `cross-refs.spec.ts` ловил `strict mode violation` на задвоенном `<CrossRef>`. Коммит
      `bbc5aad2`
- [x] **TOC всё ещё пустой на всех 3 браузерах — закрыто 2026-08-25, баг был в тестах, не в
      приложении.** `<nav>` рендерится в SSR-разметке сразу (намеренно пустым — `useState<TocItem[]>([])`
      и на сервере, и при первом клиентском рендере, см. комментарий в `toc.tsx` про
      hydration-mismatch), а ссылки `<a>` внутри появляются позже, отдельным клиентским
      `useEffect` после гидратации. `toBeVisible(nav)` проходит мгновенно (сам `<nav>` уже есть),
      но `tocLinks.count()` — не auto-retry ассерт, снимает DOM-снимок сразу и гонится с
      гидратацией. Живой прогон в браузере (dev и staging) подтвердил: DOM содержит все ссылки
      корректно, count() читает 0 только из-за таймингового окна в самом тесте. Фикс — дождаться
      `tocLinks.first().toBeVisible()` перед чтением `count()`, во всех местах
      `documents.spec.ts`/`toc.spec.ts`, где встречался этот паттерн.
- [x] **RSC-навигация не меняет URL в Firefox/WebKit — диагностировано 2026-08-25, блокировано
      апстримом Next.js, не наш баг.** Причина — известный неисправленный баг Next.js 16.0–16.3
      в `output: 'export'` (статический экспорт, `apps/pravda/next.config.mjs:15` —
      `isProduction ? 'export' : undefined`, именно так собирается прод/staging): несовпадение
      путей RSC payload между сборкой и рантаймом браузера (`__next/page/__PAGE__.txt` на сборке
      vs `__next.page.__PAGE__.txt`, который запрашивает браузер) — клиентская навигация ловит 404
      на RSC-чанк и виснет вместо fallback на полную перезагрузку. Апстрим:
      [vercel/next.js#85374](https://github.com/vercel/next.js/issues/85374) (открыт, PR-фикс
      [#86948](https://github.com/vercel/next.js/pull/86948) тоже открыт на момент диагностики).
      Chromium, судя по всему, глотает 404 RSC-чанка иначе и восстанавливается — Firefox/WebKit
      застревают. Задокументированный обходной путь у автора баг-репорта — кастомный
      build-adapter, переписывающий пути RSC-чанков после `next build` — нетривиальный объём
      работы (пост-процессинг сборки под конкретную версию Next.js), решение о том, делать ли
      его, за владельцем. Проверить/снять пункт при апгрейде Next.js, когда апстрим зарелизит фикс.
      **Подтверждено живым прогоном на staging 2026-08-25** (коммит `82ebb75e`) — та же сигнатура
      `toHaveURL` не сработал, ещё в двух местах, ранее не отмеченных: `cross-refs.spec.ts:60`
      (firefox) и, вероятно, `bookmarks.spec.ts:132` (firefox, `toBeVisible()` на кнопке «Удалить
      из закладок» — не проверено глубже, может быть каскадом от того же перехода). Тот же кластер,
      не новый баг.
- [x] **Command Palette Escape (webkit)** — флейк, не воспроизведён в изолированных повторных
      прогонах, код не менялся. **Решение владельца (2026-09-01): не чинить** — pravda не
      критичное приложение, некритичные баги e2e можно принять как есть. Помечен
      `test.skip(browserName === 'webkit', ...)` в `search.spec.ts`.
- [x] **Кластер: assert'ы позиции скролла нестабильны под staging-нагрузкой** — найдено
      2026-08-25 при подтверждающем прогоне TOC-фикса. 8 тестов в `toc.spec.ts`/`documents.spec.ts`
      падали на проверках скролл-позиции/прогресса (`TOC скроллит к разделу`, `прокручивается к
      активному пункту`, `progress bar обновляется при скролле`, `прогресс чтения обновляется при
      прокрутке`) вперемешку по chromium/firefox/webkit, 3 из них ожили на retry (flaky), 8
      остались красными. Не диагностировано — гипотеза: фиксированные `page.waitForTimeout(300)` в
      тестах недостаточны под staging-нагрузкой (тот же класс проблемы, что нестабильность
      shop-навигации в mandala, см. `apps/mandala/PLAN_COMPLETED.md` § Раунд 6). **Решение
      владельца (2026-09-01): не диагностировать и не чинить** — оставлено как есть, без skip
      (не browser-специфичный флейк, привязка к конкретным тестам не подтверждена).
      **Частично диагностировано и закрыто 2026-09-22** — два реальных бага из этого кластера,
      не связанные с нагрузкой:
      1. `подсветка активного пункта при скролле` (и любой переход по `#hash`/клик по TOC) не
      срабатывала СТАБИЛЬНО во всех 3 браузерах, не флейково — `html { scroll-padding-top: 60px}`
      (globals.css) и `scroll-margin-top: 80px` секций/глав/статей складываются при
      `scrollIntoView` (поведение спеки CSS Scroll Snap), давая реальный отступ 140px вместо
      80, на которые рассчитан `ACTIVE_THRESHOLD` в `toc.tsx`. Фикс — `SCROLL_MARGIN_TOP`
      80px → 20px (`src/lib/constants.ts`), суммарный отступ стал 80.
      2. `клик по пункту TOC скроллит к разделу` (chromium) и `TOC автоскроллится к активному
         пункту` (флейк chromium/firefox) — `scrollIntoView({behavior:'smooth'})` зависает
      НАВСЕГДА без фокуса окна (Chromium не тикает rAF-анимацию, тот же класс, что и
      `raf-vs-timers-background-tab.md`), Playwright-браузеры в CI обычно без реального фокуса.
      Фикс — `behavior: 'instant'` в обоих местах `toc.tsx`. Подробности — `CHANGELOG.md`
      v1.9.8.
      Остаток кластера — **закрыт 2026-09-22**, третий баг того же файла: прогресс-бар/активный
      пункт TOC считались в throttle-обёртке на `requestAnimationFrame`, который полностью замирает
      без фокуса окна (тот же механизм, что и баги 1.9.8 выше) — на staging это давало стабильное
      падение в webkit и флейковое в firefox для `прогресс-бар обновляется при скролле`, `прогресс
      сбрасывается при навигации` (`toc.spec.ts`) и `прогресс чтения обновляется при прокрутке`
      (`documents.spec.ts`). Фикс — `requestAnimationFrame` → `setTimeout(50мс)`, `v1.9.9`. Локальный
      `nx e2e` на этот конкретный тест ненадёжен (гонка гидратации в dev-режиме, отдельная от этого
      бага) — подтверждение через `run_e2e` на s1 после деплоя, см. тред `pravda-e2e-first-run-failures`.

**Решение владельца (2026-09-01): pravda НЕ добавляется в `E2E_GATED_APPS`.** ⚠️ **Устарело
2026-09-22** — коммит `d1aa6686d` (`feat(infra-config): все прод-деплои — через e2e-гейт, весь
build — на s1`, PLAN-INFRA-6.md §157) уравнял `HARD_GATED_APPS` с `E2E_GATED_APPS` целиком:
теперь **любой** прод-деплой идёт через e2e-гейт, pravda — единственное приложение, ожидающее
первого подтверждённого прогона для регистрации (см. комментарий в
`libs/infra-config/src/index.ts:190`). RSC-навигация (апстрим-баг, ждём фикс Next.js) и Command
Palette Escape по-прежнему помечены `test.skip` с обоснованием — этого решения владелец не
отменял, снимать `test.skip` только при апгрейде Next.js с фиксом апстрима. Остаток
скролл-кластера (см. выше) тоже остаётся некритичным по прежнему решению владельца. Итог первого
прогона на staging (2026-09-22, `run_e2e`, коммит `d1aa6686d`) и фикса — тред agent-mail
`pravda-e2e-first-run-failures`.

- [ ] Подтвердить фикс класса 2 (`SCROLL_MARGIN_TOP`, `behavior:'instant'`) через `run_e2e` на
      staging после передеплоя (запрошен у `letar-dev`/`deploy-agent-dev`, тред
      `pravda-e2e-first-run-failures`), затем зарегистрировать pravda в `E2E_GATED_APPS` — ждём
      действий letar-dev/deploy-agent-dev, сам деплой не делаю
- [x] Печать документов (кнопка + CSS @media print)
- [x] Экспорт в PDF (через диалог печати браузера)
- [ ] Сравнение редакций
- [ ] История изменений статей
- [ ] Комментарии к статьям
- [x] Закладки пользователя (localStorage + страница /bookmarks)
- [x] SEO оптимизация (robots, sitemap, meta, OG, JSON-LD)
- [x] Перекрёстные ссылки (CrossRef компонент)

## Техдолг: подключить theme:check

Гейт сырых цветов/теней/transition в UI-коде (`nx g @letar/generators:theme-check-integrate
pravda`, генератор `libs/generators`, обёртка над `@letar/theme-check`) пока не подключён —
только тестово прогнан и откачен 2026-08-19 (нашёл реальные `scale()` в `src/theme/recipes/*`,
подтвердил работоспособность на четвёртом приложении). Уже подключено насовсем: domwellbes,
studio, aboi. Подключать по одному, не пакетно — allowlist легитимных исключений собирается
руками при первом прогоне. Разбор — `.claude/docs/theme-hardcode-gate-coverage.md`.

---

**Последнее обновление:** 2026-09-22
