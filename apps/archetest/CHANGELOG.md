# Changelog

Все изменения проекта документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
версионирование следует [Semantic Versioning](https://semver.org/lang/ru/).

## [0.26.3] - 2026-07-29

### Fixed

- **`MoodCheckIn` — кнопка «Пропустить»** обёрнута в `StickyActionBar` (`@letar/ui`).
  Была обычной inline-кнопкой в потоке документа — CSS-переменная `--letar-cookie-banner-
  height` её не поднимала, на короткой странице (без скролла) кнопка попадала под fixed
  cookie-баннер и перехватывалась его ссылкой (нашлось на повторном e2e-прогоне на 0.26.2:
  `safety-net.spec.ts`/`mood-check-in.spec.ts` зависали на клике «Пропустить»).

## [0.26.2] - 2026-07-29

### Fixed

- **`safety-net.spec.ts`** переписан на `/api/auth/dev-session` вместо прямого доступа к
  БД через `pg` — старый подход падал на staging-раннере (`DATABASE_URL`/`.env` там нет).
- **`CookieBanner`/`StickyActionBar` (`@letar/ui`)** — исправлен остаточный баг координации
  bottom-anchored компонентов (закрыто ранее сегодня, commit `1d8ae644`): высота баннера,
  публикуемая в CSS-переменную `--letar-cookie-banner-height`, замерялась один раз при
  монтировании и не переизмерялась без явного `window resize` — на статичном вьюпорте
  (фестивальный планшет) значение застревало неверным (замер `1655px` вместо реальных
  `142px`). `ResizeObserver` возвращён как основной механизм пересчёта.

## [0.26.1] - 2026-07-28

### Added

- **`docker-compose.staging.yml`** — staging-инстанс archetest заведён с нуля (не
  существовал), снимает блокер hard pre-deploy e2e-gate (PLAN-INFRA.md §18.7).
- **`/api/auth/dev-session`** — dev-only роут создания сессии без OIDC-редиректа
  (`createDevSessionRoute`, `@letar/auth/server`) для e2e-тестов на staging, тот же
  паттерн, что у svoichuzhie/driving-school/grandslamcup. Готовит закрытие
  `safety-net.spec.ts` (пока не переписан, см. PLAN.md).

## [0.26.0] - 2026-07-28

### Added

- **Этап 5.10 часть A — машинный аудит всего банка вопросов.**
  `scripts/audit-question-bank.ts`: семь проверок по 2126 вопросам (дубли двумя
  сигналами — триграммный Жаккар + IDF-взвешенный containment по редким смысловым
  словам; пустой скоринг; расхождения дамп↔справочник; покрытие шкал и дефицит до
  порога high; асимметрия вариантов; полнота EN; reverse-баланс). Отчёты
  `docs/question-bank-audit.md` + `.json` перегенерируются запуском скрипта.
  Чистые функции вынесены в `scripts/audit-lib.ts` (+13 unit). Главные находки:
  №101–1665 (74% банка) не переведены на EN; 342 пары дублей на решение части B;
  новых расхождений со справочником нет.

## [0.25.5] - 2026-07-28

### Fixed

- **CRITICAL: сайт был сломан для всех посетителей — `CookieBanner` рендерился вне `ChakraProvider`.**
  В `[locale]/layout.tsx` `<CookieConsent />` стоял сиблингом `<Providers>` (снаружи), а не
  внутри — `CookieBanner` (`libs/ui`) использует `Box`/`Button`/`Checkbox` из `@chakra-ui/react`,
  что бросало `ContextError: useContext returned undefined` на КАЖДОЙ странице сайта. Баг жил
  с деплоя v0.25.0 (152-ФЗ CookieBanner, тем же днём) — не пойман раньше, потому что верификация
  деплоев шла через HTTP `fetch` (статус-коды), а не через реальный рендер в браузере. Обнаружен
  пользователем в реальном Brave, воспроизведён и починен в течение сессии. Перенос `<CookieConsent />`
  внутрь `<Providers>` — единственное изменение.

## [0.25.4] - 2026-07-28

### Fixed

- **Настоящий источник бага 0.25.3 — плагин `@serwist/next`, не наш компонент.**
  Проверка деплоя 0.25.3 показала: `service-worker-registration.tsx` больше не единственная
  точка регистрации. `@serwist/next` по умолчанию (`register: true`) сам инжектит клиентский
  скрипт, который безусловно, на КАЖДОЙ странице, вызывает
  `navigator.serviceWorker.register('/sw.js', { scope: '/' })` — в обход консент-гейта
  `useOfflineConsent`. Именно этот авто-инжект, а не наш компонент, реально брал сайт
  под контроль SW. Теперь `withSerwistInit({ ..., register: false })` в `next.config.mjs` —
  единственная точка регистрации снова `service-worker-registration.tsx` (фикс 0.25.3
  остаётся в силе как defense-in-depth, но сам по себе не закрывал проблему).

## [0.25.3] - 2026-07-28

### Fixed

- **Service Worker перехватывал весь сайт, а не только `/express`.** `service-worker-registration.tsx`
  регистрировал `/sw.js` со `scope: '/'` — после одного согласия на офлайн-баннере
  (`archetest-offline-consent`) на `/express`, SW получал контроль (`clientsClaim: true`)
  над навигацией по всему домену, хотя `runtimeCaching` в `sw.ts` целенаправленно кэширует
  только `/express`/`/en/express`. Теперь регистрируются два отдельных SW-инстанса со scope
  `/express` и `/en/express` вместо одного на `/`. Требует передеплоя; у пользователей,
  уже давших согласие, старая широкая регистрация снимется только при отзыве согласия
  или обновлении SW-скрипта браузером.

## [0.25.2] - 2026-07-28

Разбор техдолга: закрыто девять пунктов из списка, три дефекта найдено по ходу.

### Fixed

- **Пороги достоверности шкал были инвертированы.** `confidenceFromCount` считала
  достоверность как долю банка, а вопросов на шкалу различается в 100+ раз (RES_PHYS — 10,
  SAD — 21 против AVD — 1192): «высокая точность» набиралась за 6 ответов по редкой шкале
  и за 715 — по населённой. Человек, прошедший 4 порции, видел «высокую точность» по редким
  шкалам и «Недостаточно данных» по самым населённым. Теперь **абсолютные пороги 5 / 15 / 30**,
  выведенные из полуширины интервала Уилсона при p = 0.5 (±33 / ±23 / ±17 п.п.).
  Прежняя величина сохранена отдельной функцией `bankCoverage` — это «покрытие», не «точность».
- **Ачивка «Полная карта» стала бы недостижимой** после смены порогов: она требовала
  достоверности `high` по всем шкалам ядра, а по SAD в банке 21 вопрос при новом пороге 30.
  Переведена на покрытие банка (`FULL_MAP_COVERAGE = 0.6`) — по смыслу она про полноту
  прохождения, а не про точность оценки; достижимость осталась прежней.
- **Числа «22 шкалы / 2096 вопросов» уже разошлись с реальностью** — банк вырос до 2126
  ещё на этапе 5.5. Публичные страницы обещали 2096.
- **merge-скрипт: защита от изменения баллов пропускала часть правок.** Первая версия
  сравнивала только максимум вопроса, поэтому правка балла варианта с 1 на 3 при соседнем
  варианте-«тройке» проходила молча — максимум не двигается, но `raw` каждого, кто выбрал
  этот вариант, меняется, включая уже записанные сессии.
- **merge-скрипт не обновлял `global_max_scores` для новой шкалы** — код, которого ещё нет
  в справочнике, не попадал в список обновляемых. Шкала HON молча не получила бы максимума,
  и нормализация по ней поехала бы с первого прохождения.

### Added

- `_data/bank-stats.ts` — единственный источник чисел (`CORE_SCALE_COUNT` из
  `ALL_SCALE_CODES.length`, `TOTAL_QUESTIONS`). Литерал, а не `Object.keys(maxScores).length`:
  справочник весит 462 КБ и value-импорт утащил бы его в клиентский бандл; от рассинхрона
  защищает unit-тест.
- `_data/scale-prototypes.ts` — таблица «группа шкал → прототип → DOI», из неё генерируются
  и `/for-professionals`, и слайд 02 презентации (раньше дублировалась в четырёх местах).
- `getScaleName(code, { audience, triadAlias }, isRu)` вместо четырёх локальных функций.
  Аудитории три, а не две: `construct` отдаёт клиническое название **только** для шкал
  белого списка `PUBLIC_CONSTRUCT_SCALES` (восемь вершин гексаграммы, где термины триад
  осмысленны), остальные молча падают на юзерское имя. Политика 5.6.1 стала механической.
- `robots.ts`, `sitemap.ts` и `metadataBase` — их не было вовсе. Sitemap с `hreflang`
  для обеих локалей; robots закрывает кабинет, настройки, API и dev-роуты.
- `canonical` + `languages` для главной, `/express`, `/privacy`; `noindex` для `/cabinet`
  и `/settings` через layout (сами страницы клиентские, `metadata` в них объявить нельзя).
- Режим `--update` в `merge-question-batch.ts` — вердикты «Править» по уже влитым вопросам
  применимы штатным путём. Правка баллов требует явного `--allow-scoring-change`.
- `_components/score-bar.tsx` — полоса балла была скопирована в трёх местах.
- `src/app/[locale]/dev/layout.tsx` — единый гейт dev-роутов вместо семи одинаковых проверок.
- [docs/app-patterns.md](./docs/app-patterns.md) — «производный индекс в `_lib`», двуязычие
  психологического контента парами полей `x`/`xEn`, именование шкал.
- Тесты: `bank-stats`, `scale-prototypes`, `scale-naming`, покрытие `bankCoverage`
  и `getScaleBankCoverage`. Итого **187 unit** (+16).

### Changed

- Типы скоринга больше не реэкспортируются из `'use server'`-модуля `quiz.action.ts` —
  единственный источник `_lib/scoring-core`.
- Dev-секрет `BETTER_AUTH_SECRET` перегенерирован (44 символа) — предупреждения Better Auth
  на старте больше не маскируют настоящие ошибки. Прод не затронут.

## [0.25.1] - 2026-07-28

### Added

- **Пакет ревью новой шкалы «Доброжелательность» (HON)** — 15 авторских ситуационных
  вопросов по четырём фасетам HEXACO Honesty-Humility (Ashton & Lee, 2009):
  Искренность 4, Справедливость 4, Нежадность 4 (якорный фасет), Скромность 3.
  Батч `prisma/question-batches/hh/HON.json`.
  - **Скоринг изолирован**: варианты начисляют баллы только `HON`. Это одновременно
    методологическое требование (общие пункты с тёмными шкалами сделали бы будущую
    корреляцию артефактом дизайна) и то, что оставляет `QUESTION_BANK_VERSION` без
    изменений — накопленная история сессий не выпадает из усреднения.
  - 8 reverse-вопросов при требуемых 5. Reverse здесь — балл за вариант, который
    выглядит невыгодно или неловко (признать, что не смотрел; заплатить, когда можно
    не платить; не пойти «по знакомству» ради близкого).
  - Сюжеты сверены с банком: ни один `scenario` не дублирует существующие 2126.
    Классика честности (сдача, кошелёк, счёт в ресторане, лотерея, наследство) уже
    занята, поэтому взяты бытовая экономика, соседство, потребление, семья — что
    попутно уводит вопросы от доменов MAC/NAR/ANT/SAD.
  - Поле `_facet` в батч-файле: в таблице ревьюера колонка «Шкала» показывает фасет,
    а не код (`HON` у всех пятнадцати). `merge-question-batch.ts` не менялся —
    он игнорирует неизвестные `_`-поля.
- **`docs/question-review/question-review-hh.xlsx`** — таблица для психолога-ревьюера
  с листом-инструкцией: зачем шкала, риск зеркальности тёмных шкал, требование
  изоляции скоринга, критерии в порядке важности.

### Changed

- `scripts/generate-question-review-xlsx.py` — батч `hh` (`--batch hh`), `INTRO_HH`,
  колонка «Шкала» берёт `_facet` вопроса, если он задан.
- `docs/question-review/INSTRUCTIONS.md` — раздел про пакет HH и таблица «три пакета,
  разные сроки» в шапке: у HH есть дедлайн (02.08) и его вопросов **ещё нет** в тесте,
  тогда как 5.1 и 5.5 уже работают и их ревью ничего не блокирует.
- `docs/honesty-humility-brief.md` — статус шагов 1–3 закрыт.

## [0.25.0] - 2026-07-28

### Added

- Cookie-баннер, `ConsentLog` и consent-aware аналитика (сессия 152-ФЗ, коммит `5cab2756`).
  Детали — в [PLAN_COMPLETED.md](./PLAN_COMPLETED.md).

## [0.24.0] - 2026-07-28

### Added

- **Индекс «Тёмное ядро» (Фаза 3)** — приближение D-фактора (Dark Factor of Personality)
  из четырёх уже существующих шкал ядра (MAC/NAR/ANT/SAD). Новых вопросов, миграций БД
  и изменения `QUESTION_BANK_VERSION` не потребовал — производный слой поверх `normalized`.
  - `_lib/dark-core.ts` — чистая функция `computeDarkCore`. Считает уровень ядра
    (unit-weighted composite), «вкусы» по Bader et al. (2023) — отклонение каждой шкалы
    от ядра, размах, интервалы Уилсона, агрегированную достоверность (минимум по
    измеренным) и ipsative-контекст (где тёмные шкалы стоят внутри профиля человека).
  - **Структурный вывод вместо ярлыка уровня**: `even` / `flavored` / `polarized` /
    `muted` / `insufficient`. Поля абсолютного уровня в типе нет намеренно — `normalized`
    это процент от максимума, а не перцентиль, и утверждение «высокое тёмное ядро»
    некорректно до накопления норм (5.6.2).
  - **Анализ чувствительности к нарциссизму**: второе значение ядра без NAR. Порог
    выводится из тождества `core − core₃ = deviation(NAR) / 3`, а не назначается.
    Основание — Hilbig et al. (2023): просадку приближения даёт именно нарциссическое
    «восхищение».
  - **Измеренность шкалы — по `relevantCounts >= 3`, а не по «балл > 0»**: `normalized = 0`
    бывает и при отсутствии данных, и при честном нуле, и различает их только число
    ответов. Банк асимметричен (SAD — 21 релевантный вопрос против 987 у ANT), поэтому
    при n = 1 один ответ двигал бы ядро на десятки пунктов.
  - `DarkCoreBlock` на странице клиента — только кабинет психолога, клиенту не показывается.
    Порядок блоков: структурный вердикт → вкусы → число → чувствительность → оговорка;
    обязательное поле `caveat` приходит из модуля вместе с числом.
  - Dev-превью `/dev/dark-core` — все пять структурных веток.
  - +24 unit-теста. Итого **171 unit**.
- **`generateMetadata` для `/for-professionals`** — у главной B2B-страницы не было
  собственных метаданных вовсе (она наследовала общий title из layout). Добавлены
  title/description/OpenGraph на обе локали и canonical.
- **Кликабельные источники в разделе «Методология»** — все 15 DOI сверены вручную
  (у SSIS в справочных материалах ходит неверный DOI). Плюс блок «Тёмное ядро: общий
  фактор и «вкусы»» с явными ограничениями: r ≈ .85 против полного D, предостережение
  авторов против трактовки тетрады как главного представления ядра, и критика
  Vize/Lynam о неотличимости D от антагонизма (шкалы Доброжелательности в тесте нет,
  проверить на своих данных невозможно в принципе).
- Якорь `#methodology` на секции «Методология» — для ссылок из презентации и раздатки.

### Changed

- `getClientDetailAction` отдаёт в кабинет `scoreRelevantCounts` и `scoreConfidence`
  (оба уже считались в `calculateScores`, но наружу не выходили). Побочный эффект:
  в `ProfileDetails` кабинета заработало ipsative-ранжирование — до этого оно молча
  не срабатывало без `relevantCounts`, и топ-3 сортировался просто по баллу, без
  интервалов и tie-групп.
- Раздел «Методология» больше не утверждает, что MAS — единственный конструкт без
  валидированного прототипа: добавлено описание трёх экспериментальных шкал
  (RES_PHYS/RES_AFF/SPEC_INT) и индекса «Броня и Радар». Долг тянулся с v0.23.0,
  где реализация была, а маркировка на публичной странице — нет.

### Fixed

- `ExperimentalScalesBlock` скрывал индекс «Броня и Радар» при истинном нуле по одной
  из осей: условие `phys > 0 && aff > 0` не отличало «шкала не отвечена» от честного
  нулевого балла. Теперь измеренность считается по числу ответов.

## [0.23.0] - 2026-07-09

### Added

- **Этап 5.5 — экспериментальные шкалы кабинета психолога** (P2, последний этап спринта
  в очереди). Три авторских/прототипных конструкта **вне ядра из 22 шкал**:
  - `RES_PHYS` «Физическая броня» (авторский конструкт), `RES_AFF` «Аффективный резонанс»
    (прототипы IRI Personal Distress / HSP Scale), `SPEC_INT` «Специальные интересы»
    (авторский конструкт). Отдельные тип `ExperimentalScaleCode`, массив `EXPERIMENTAL_SCALES`,
    списки `EXPERIMENTAL_SCALE_CODES` и `SCORED_SCALE_CODES` (= ядро 22 + 3) в `personality-types.ts`.
  - `ExperimentalScalesBlock` на странице клиента психолога: бета-шкалы с баллами,
    developmental-описанием и прототипом; конструктные названия показываются открыто
    (только психологу), везде β-бейджи.
  - Индекс **«Броня и Радар»** — `_lib/armor-radar.ts` (чистая функция, 2×2-квадрант:
    Громоотвод / Крепость / Оголённый нерв / Ровный фон) с акцентом на паттерне
    «броня↑ + радар↑» = риск соматизации и позднего выгорания.
  - Банк вопросов 2096 → 2126: батчи `prisma/question-batches/5.5/` (3 шкалы × 10 вопросов,
    ≥4 reverse на шкалу). Пакет ревью психологом — отдельный `question-review-5.5.xlsx`
    - раздел 5.5 в `INSTRUCTIONS.md` (файл 5.1 не тронут).
  - Dev-превью `/dev/experimental` (три сценария индекса «Броня и Радар»).
  - +17 unit-тестов (armor-radar, экспериментальные шкалы, изоляция ядра). Итого 147 unit.
- **Демо-чек-лист феста** (этап 5.7, последний пункт) — `docs/demo-checklist.md`:
  10 секций для буферной недели (офлайн-проверка SW, функциональный флоу, печать раздатки
  из dev-роутов, вычитка, контрольный звонок safety-net, 152-ФЗ/РКН, оборудование стенда,
  B2B-лиды, утренний чек, пост-фест). Сам прогон — в буферную неделю.

### Changed

- Скоринг (`scoring-core.ts`) и стратифицированная выборка (`stratified-shuffle.ts`) переведены
  на `SCORED_SCALE_CODES` (25 шкал); тип `QuizScores` расширен до `Record<ScaleCode, …>`
  (надмножество ядра — чтение баллов ядра обратно совместимо). Интерпретационные пути
  (ipsative-ранжирование, тизер экспресса, express-выборка, ачивки «тип личности») остались
  строго на ядре 22 шкалах.
- `merge-question-batch.ts` параметризован (`--batch <name>`): конфиг директории, размера
  батча, минимума reverse и **допустимых для скоринга кодов**. Для 5.5 варианты вправе скорить
  только экспериментальные шкалы.

### Fixed / Инварианты

- **Изоляция ядра:** варианты вопросов 5.5 скорят ТОЛЬКО экспериментальные шкалы →
  actual_max ядра не меняется, нормализация ядра сопоставима, `QUESTION_BANK_VERSION` остался 2.
  Инвариант «ни один вариант не смешивает коды ядра и экспериментальные» закреплён unit-тестом
  в `question-bank.test.ts`.

## [0.22.1] - 2026-07-09

### Added

- **Пакет для ревью вопросов психологом** (подготовка внешней P1-задачи):
  `docs/question-review/question-review-5.1.xlsx` — 135 вопросов этапа 5.1 одним листом
  (варианты с баллами, отметка reverse, дропдаун вердикта ОК/Править/Удалить, колонка
  правок) + `INSTRUCTIONS.md` (критерии ревью: конструктная валидность, дифференциация
  MAC↔ANT/SAD↔ANT/ASD↔ALX/DIR↔ANT, социальная желательность, этика; отдельная
  клиническая просьба — вопрос-маркер суицид-риска для safety-net). Генератор —
  `scripts/generate-question-review-xlsx.py` (перегенерация после правок батчей).

### Fixed

- **RU-вычитка фестивального пути** (RU-first к фесту): устаревшие цифры
  «1955 вопросов, 13 шкал» → «2096 вопросов, 22 шкалы» в SEO/OG-метаданных и на
  `/for-professionals` (там же: стратификация — по 22 шкалам, добавлена отсылка
  к остальным 9 шкалам ядра из раздела «Методология»); единый регистр «вы»
  («Узнайте все 22 шкалы»); гендерно-нейтральные формулировки — share-тексты
  («Мой экспресс-тест…» вместо «Прошёл…») и чекбокс согласия («Подтверждаю
  ознакомление и согласие…» вместо «Я ознакомился…»).
- Телефоны доверия safety-net проверены по открытым источникам (2026-07-09):
  линия ЦЭПП МЧС 8-800-333-44-34 и детская 8-800-2000-122 действуют; контрольный
  звонок — в демо-чек-лист буферной недели (зафиксировано в `crisis-resources.ts`).

## [0.22.0] - 2026-07-09

### Added

- **Презентация продукта для Инпсихофеста** (этап 5.7): print-friendly dev-роут
  `/dev/presentation` — 8 слайдов RU-first (что это → научный фундамент → гексаграмма →
  developmental-фрейм → честность данных → этика/право → демо → приглашение), живая
  `HexagramChart` с примерным профилем, разрывы страниц при печати. В production роут
  закрыт (как `/dev/qr`). Контент — на вычитку Kami перед фестом.
- **Маппинг шкал на дименсиональные домены** (этап 5.6.2, `/for-professionals`):
  к существующему абзацу про AMPD/PID-5, HiTOP и МКБ-11 добавлен ориентировочный маппинг
  шкал на пять доменов (Негативная аффективность, Отстранённость, Антагонизм/Диссоциальность,
  Расторможенность, Ананкастия/Психотизм + «пограничный паттерн») — конструктный уровень,
  с оговоркой про эмпирическую факторную проверку по выборке. RU + EN.

## [0.21.0] - 2026-07-09

### Added

- **Этап 5.6.5 — Ipsative-скоринг с доверительными интервалами** (последний P0-хвост
  спринта, решение 5.6 от 2026-06-18): `_lib/ipsative.ts` — чистый интерпретационный слой
  поверх normalized (по образцу interpretation-rules): ранжирование шкал внутри профиля,
  95%-интервалы точности (приближение Уилсона по числу отвеченных релевантных вопросов
  шкалы, n=0 → [0, 100]), tie-группы статистически неразличимых соседей (перекрытие
  интервалов). raw/normalized в БД не затронуты — миграция не нужна.
- **UI «Ведущие черты» по ipsative-рангу**: результаты квиза и «Мой профиль» на интро —
  диапазон точности рядом с баллом (`ciLow`/`ciHigh` в `DevelopmentalProfileCard`),
  неразличимые соседи честно помечаются «≈ выражены примерно одинаково» вместо ложной
  точности «61,2% > 60,8%», методологическая подпись «сравнение внутри профиля, а не
  с другими людьми». Клиентский фолбэк (гость/сетевая ошибка) — прежняя сортировка без
  интервалов. Ipsative-секция в dev-превью `/dev/developmental`.
- `QuizScores.relevantCounts` — число отвеченных релевантных вопросов по шкалам (вход
  интервалов), прокинуто через все три пути сабмита квиза и кумулятивный профиль.

### Changed

- **Ядро скоринга вынесено в `_lib/scoring-core.ts`** (рефакторинг 5.6, разблокировал
  unit-тесты 5.8): чистые функции `computeScoresCore`/`computeActualMax`/
  `countRelevantAnswered`/`getScaleLevel`/`getScaleConfidence` без next/db-зависимостей,
  справочник максимумов инжектируем (`ScoringData`, дефолт — `BANK_SCORING_DATA` из JSON
  психолога). `calculateScores` в `quiz.action.ts` — тонкая обёртка (БД → ядро); формулы
  и поведение не изменены, типы реэкспортируются — внешние импорты работают без правок.

### Tests

- +25 unit (итого **130 unit + 7 E2E**): `scoring-core.test.ts` (13 — raw/нормализация/
  уровни/достоверность на инжектированном справочнике, отсутствие мутации, smoke реального
  банка + сценарий «пересчёт гостевой сессии» — закрыт отложенный пункт 5.8),
  `ipsative.test.ts` (12 — интервал Уилсона, ранжирование, tie-группы, exclude состояний,
  детерминизм, мутации).

## [0.20.0] - 2026-07-08

### Added

- **Тизер оставшихся 12 шкал в результатах экспресса** (этап 5.7): под группами шкал
  гексаграммы — заблокированный серый список остальных шкал ядра (`ScaleTeaser`,
  `TEASER_SCALE_CODES` в `personality-types.ts`), CTA на полный тест. Названия — только
  юзерские/архетипы, без клинических ярлыков (политика 5.6.1); BAR/DPR (`STATE_CODES`)
  исключены из списка и упомянуты отдельной строкой как блок «Состояния».

## [0.19.0] - 2026-07-08

### Added

- **Локаль прохождения на `QuizSession`** (подготовка норм ru/en, 5.6.2/5.9.5): поле
  `locale` (миграция `20260708121718_add_quiz_session_locale`), прокидка из полного квиза
  (все три пути сабмита + `PendingQuiz` в sessionStorage) и из привязки экспресс-результата.
  Нормативные перцентили по решению 2026-07-03 считаются раздельно по локалям — язык
  прохождения нельзя восстановить задним числом, поэтому сбор начат досрочно, до появления
  самого механизма норм. Старые сессии — `null` (честно неатрибутированы).

### Documentation

- PLAN.md: расширен 5.9.5 «Нормативная гигиена» (правила референтной выборки: один
  человек = одна запись, исключение невалидных/спам-протоколов, нормы ru/en раздельно;
  блокер — отсутствие пайплайна перцентилей до N ≈ 200–300); зафиксирован **RU-first
  приоритет к фестивалю** (EN поддерживается, но не блокирует фестивальные задачи);
  добавлена задача **презентации продукта** (карта материала по этапам); в 5.7 — тизер
  остальных 14 шкал в экспресс-результатах (идея Kami); в Фазу 3 — СДВГ-скрининг
  (прототип ASRS-5; в текущих 22 шкалах конструкта СДВГ нет).

## [0.18.0] - 2026-07-08

### Added

- **Этап 5.7 — Offline-first PWA для `/express`**: Serwist (`src/app/sw.ts` — precache
  статики + NetworkFirst для документов `/express`/`/en/express` с install-прекэшем в общий
  кэш), `next.config.js` → `.mjs` с `withSerwist` (в dev отключён), build target
  `next build --webpack`. Service Worker регистрируется **только по согласию**
  (`OfflineConsentBanner` + `useOfflineConsent`, эталон mandala), баннер и регистрация —
  только на `/express`. Гостевой флоу работает офлайн целиком: выборка вопросов приезжает
  в закэшированном документе, подсчёт клиентский, результат в localStorage, QR из бандла.
- **Этап 5.7 — Kiosk-режим** (`/express?kiosk=1`): кнопка «Новый посетитель» с двухтапным
  подтверждением; сброс чистит гостевой результат, отложенные ответы и согласие (152-ФЗ:
  согласие персонально — следующий посетитель даёт своё) и полностью перезагружает страницу.
  +2 E2E (`kiosk.spec.ts`), итого 7 E2E.

### Fixed

- **tsconfig**: отсутствовал project reference на `libs/hooks` (вскрыт tsgo при первом
  импорте `@letar/hooks`); сгенерированные `public/sw.js`/`swe-worker-*.js` добавлены
  в `.gitignore` и eslint-ignores.

## [0.17.0] - 2026-07-08

### Added

- **Этап 5.9.3 — Кулдаун ретестов (решение Kami: «гибрид»)**: XP-гранула — уникальные
  UTC-сутки, не порция вопросов (`countUniqueUtcDays` в `ranks.ts`; 5 порций в день =
  100 XP, та же активность в 5 разных дней = 500 XP). Новые вопросы банка доступны без
  ограничений в тот же день — повтор отвеченных структурно невозможен (`excludeIds`),
  поэтому жёсткий суточный запрет на повторы не нужен как отдельный механизм; «окно
  точности» для ретестов после исчерпания банка отложено в Фазу 3 вместе с самими
  ретестами. `submitQuizAction` возвращает `xpCountedToday`; на экране результатов у
  повторной порции дня — подпись «XP за сегодня уже получены — каждый новый ответ
  уточняет профиль» (ru/en). +9 unit-тестов (`ranks.test.ts`)

### Fixed

- **`recalcLeaderboardEntry` считал XP по всем сессиям без фильтра `isValid`** — невалидный
  протокол (проваленные attention-check'и / монотонность) добавлял +100 XP при следующем
  валидном пересчёте, вопреки решению 5.1 «невалидные исключены из XP»

## [0.16.1] - 2026-07-07

### Fixed

- **Unit-тесты не запускались после обновления зависимостей монорепо** (vitest 4.1.10 перестал
  использовать вложенный vite 8.0.16 и перешёл на hoisted vite 8.1.3): новый oxc-трансформер
  vite (`vite:oxc`) резолвит tsconfig per-file и падает с `[TSCONFIG_ERROR] Tsconfig not found`,
  если файл не покрыт include ближайшего `tsconfig.json` или его project references. Тестовые
  файлы и `vitest.setup.tsx` были исключены из `tsconfig.json`. Фикс: новый
  `tsconfig.spec.json` (composite, покрывает `vitest.setup.tsx` + `src/**/*.{test,spec}.ts(x)`,
  с явным переопределением унаследованного `exclude`) + reference на него из `tsconfig.json`.
  ⚠️ Проблема касается всех приложений монорепо с vitest (kami воспроизведена) — тираж фикса
  на остальные приложения вне скоупа archetest.

## [0.16.0] - 2026-07-07

### Added

- **Этап 5.7 — B2B-конверсия (лиды психологов)**: модель `ProfessionalLead` (имя, email,
  согласие ПДн, locale, source) с гостевой политикой `@@allow('create', true)`; лид-форма
  на `/for-professionals#lead` (чекбокс согласия не предотмечен, ссылка на `/privacy`); CTA
  «Вы психолог?» на экране результатов экспресса → `/for-professionals?source=express-cta#lead`;
  dev-роут `/dev/qr` — печатные QR-коды на `/express` и `/for-professionals` для раздатки на фесте
- **createForm-инстанс `ArchetestForm`** (`src/archetest-form/`) — обязателен по правилам форм
  монорепо, ранее в archetest отсутствовал
- **`/privacy`**: новая категория данных «заявка специалиста: имя, email» (ru/en)

### Fixed

- **`submitProfessionalLeadAction` писал через raw `prisma`, а не `getEnhancedPrisma`** —
  enhanced-клиент делает read-back созданной записи, который падал на политике
  `@@allow('read', ADMIN)` для анонимного гостя (запись при этом уже была в БД, но клиент
  получал ошибку и не видел экран успеха). Найдено E2E-прогоном реального сабмита в браузере.

## [0.15.1] - 2026-07-07

### Added

- **E2E: триггер safety-net (этап 5.8)** — `archetest-e2e/src/safety-net.spec.ts` (1 тест):
  полный квиз (50 вопросов), на каждом вопросе выбирается вариант, максимизирующий DPR+BAR+BOR
  (справочник — `questions-dump.json`), до экрана результатов с кризисным блоком телефонов
  доверия. `submitQuizAction` требует авторизацию, а archetest поддерживает только OIDC-логин
  через ключницу — тест создаёт сессию напрямую в БД (тестовый юзер + строка `Session`,
  cookie подписывается тем же HMAC-алгоритмом, что и better-call/Better Auth) вместо гонки
  реального OAuth-редиректа. Добавлен `data-testid="quiz-scenario"` на заголовок вопроса
  (`quiz-question-card.tsx`) для сопоставления отображаемого вопроса с банком. Прогон chromium:
  5/5 passed (2 express + 2 mood + 1 safety-net)

## [0.15.0] - 2026-07-06

### Added

- **Раздел «Методология» на `/for-professionals` (этап 5.6.2)** — таблица маппинга 9 шкал
  archetest на валидированные конструкты (LTS, SD3/Dirty Dozen, ASP/SSIS, PID-5, AQ/AQ-10,
  Self-Monitoring/HEXACO, TAS-20, MDQ/HCL-32, PHQ-9), явное разделение валидированного ядра
  (21 из 22 шкал) и экспериментального авторского конструкта (MAS, помечен «бета»),
  абзац про независимость светлой/тёмной триад (Kaufman et al., 2019, r ≈ −.48) и абзац
  про дименсиональные модели (AMPD/PID-5, HiTOP, пять доменов МКБ-11)

## [0.14.1] - 2026-07-06

### Added

- **E2E: mood check-in (этап 5.8)** — `archetest-e2e/src/mood-check-in.spec.ts` (2 теста):
  интро полного квиза → согласие 152-ФЗ → mood check-in (сетка 3×3) → выбор ячейки/«Пропустить»
  → первый вопрос квиза. Гостевой доступ, без авторизации. Прогон chromium: 4/4 passed
  (вместе с существующими 2 express-тестами)

### Fixed

- Стейл-заметка в PLAN.md «экран согласия 152-ФЗ ещё не готов» для E2E safety-net — 5.6.3
  завершён давно, экран согласия покрыт mood-check-in.spec.ts

## [0.14.0] - 2026-07-06

### Added

- **Ачивки за ритм вместо объёма (этап 5.9.4)** — новая категория `rhythm`
  - `THREE_MOODS` («Три настроения») — ≥3 различных пары mood check-in (валентность×энергия)
    среди завершённых сессий
  - `RETURN_30` («Возвращение») — разрыв ≥30 дней между двумя соседними сессиями
  - `SPACING_SERIES` («Спейсинг-серия») — 4 сессии подряд с интервалом ≥7 дней между каждой парой
  - `FULL_MAP` («Полная карта») — все 22 шкалы достигли кумулятивной достоверности `high`
    (по всем отвеченным вопросам пользователя за всё время, не только текущая сессия)
  - Новый экспорт `getCumulativeConfidence` из `quiz.action.ts` — переиспользует существующую
    логику `getScaleConfidence`/`totalRelevantByScale` для расчёта вне текущей сессии

## [0.13.0] - 2026-07-06

### Added

- **Mood check-in перед сессией (этап 5.9.2)** — циркумплекс Рассела (валентность × энергия)
  вместо голого прохождения квиза «в вакууме»
  - Новый компонент `MoodCheckIn` — сетка эмодзи 3×3, ~10 сек на выбор, кнопка «Пропустить»
  - `QuizSession.moodValence`/`moodEnergy` (1-3, nullable) — миграция `20260706174730_add_mood_check_in`
  - Показывается перед каждой новой порцией из 50 вопросов (первый старт и «Продолжить»)
  - `SubmitQuizSchema` принимает оба поля опционально, сохраняются вместе с сессией
  - Не влияет на скоринг черт личности — задел для (а) кабинета психолога («профиль в грусти
    vs профиль в ресурсе», Фаза 3), (б) карты стабильности профиля (Фаза 3)
  - Экспресс-режим (`/express`) не покрыт — фестивальная скорость приоритетнее

## [0.12.0] - 2026-07-06

### Removed

- **Соревновательный слой геймификации (этап 5.9.1)** — решение Kami 2026-07-03: лидерборд
  загрязнял будущие психометрические нормы, SPEED_DEMON поощрял careless responding, XP
  за объём ответов стимулировал прокликивание
  - Роут `/leaderboard` + компоненты `LeaderboardTable`/`LeaderboardUserRow` удалены
  - Ссылки на лидерборд убраны из `header.tsx`, `mobile-drawer.tsx`, `quiz-results.tsx`
  - Ачивка `SPEED_DEMON` удалена из `achievements.ts`; `sessionDurationMs` больше не считается
    в `achievements.action.ts`
  - `calculateXp` — убран множитель за объём ответов (`totalAnswers * 2`); формула теперь
    `sessions × 100 + achievements`
  - Неиспользуемые i18n-ключи `nav.leaderboard` и `quiz.leaderboard.*` убраны из ru/en

### Changed

- Кэш `QuizLeaderboardEntry` в БД сохранён (не читается в UI) — пригодится для Фазы 3

## [0.11.0] - 2026-07-05

### Added

- **Липкая основная CTA на интро (системный фикс «кнопка под фолдом»)** — основная кнопка
  (`Начать экспресс` / `Начать тест`) больше не уходит под скролл, всегда видна:
  - Использует новые shared-примитивы `StickyActionBar` + `useScrollGate` из `@letar/ui@0.7.0`
  - `express-container.tsx` (интро экспресса) и `quiz-intro.tsx` (интро полного квиза):
    кнопки вынесены в `StickyActionBar` (последний ребёнок контейнера, `position: sticky`,
    `safe-area-inset-bottom`), контент — в прокручиваемом `VStack` с маркером-`sentinel` в конце
  - `useScrollGate({ enabled: !disclaimerAccepted })` — CTA `disabled`, пока не дано согласие
    (чекбокс внизу контента уже вынуждает доскроллить; для returning-visitor с согласием гейт
    выключен, кнопка сразу активна и видна)

### Changed

- Проверено в превью (десктоп + 375px): CTA видна без скролла (`visibleNoScroll: true`,
  на мобильном `btnBottom` 800 из 812 с safe-area), горизонтального скролла нет,
  disabled до согласия, после согласия+скролла — enabled; консоль чистая

## [0.10.0] - 2026-07-05

### Added

- **Mobile-first UI для фестивального демо (этап 5.4)** — полировка UX стенда на Инпсихофесте:
  - `_hooks/use-high-contrast.ts` + `_components/high-contrast-toggle.tsx` — **высококонтрастный
    режим** для планшета на выставке (читаемость под ярким светом). Кнопка `LuContrast` в шапке
    (десктоп + мобильный), состояние в localStorage (переживает перезагрузку между посетителями),
    атрибут `data-contrast="high"` на `<html>`. Оверрайды в `theme/index.ts` усиливают
    приглушённый текст (`fg.muted`/`fg.subtle` → `fg`) и границы (→ `border.emphasized`)
  - `_components/icebreaker-card.tsx` — **intro-карточка с двумя icebreaker-вопросами для
    психологов** на `/express` («Вы психически здоровы?» + факт о когорте Данидина ~17%
    с citable-источником Schaefer et al., 2017 — вместо мифической цифры «5%»)
  - `_components/share-result-button.tsx` — **Web Share API** на экранах результатов (полный
    квиз + экспресс): нативный лист шеринга на мобильных, копирование ссылки в буфер + тост
    как фолбэк. Экспресс делится ссылкой на полный тест
  - i18n: `common.share`, `express.icebreaker`, `express.shareText`, `quiz.shareText` (ru + en)

### Changed

- `quiz-question-card.tsx`: **тач-цели ≥ 56px** (`minH="56px"` на вариантах ответа и кнопке
  пропуска); **оптимистичное выделение** — локальный `optimisticIndex` подсвечивает выбранный
  вариант мгновенно (0ms lag), не дожидаясь round-trip через состояние родителя, сбрасывается
  при смене вопроса
- Проверено в превью: тач-цели 56–57.6px, горизонтального скролла нет (десктоп + 375px),
  высокий контраст меняет CSS-переменные (`!important` бьёт `@layer tokens`), оптимистичная
  подсветка < 150ms, Web Share-фолбэк без ошибок, icebreaker виден на мобильном

## [0.9.0] - 2026-07-05

### Added

- **Developmental-фрейм результатов (этап 5.6.1)** — сдвиг от diagnosis-фрейма к карте
  ресурсов и зон роста:
  - `_data/growth-practices.ts` — блок «Практики» для всех 22 шкал: 2–3 конкретных
    доказательных действия на шкалу с тегом модальности (КПТ / DBT-навык / Схема-терапия /
    Практика). Формулировки авторские, без заимствования пунктов опросников (лицензионная
    чистота 5.6.2). BOR → DBT (TIPP, противоположное действие, дневник эмоций), NAR/HUM/MAS/SAD →
    схема-терапия, AVD/DEP/OBC/PAR → КПТ (экспозиция, реструктуризация, поведенческие эксперименты)
  - `_components/developmental-profile-card.tsx` — карточка шкалы в три блока
    **Суперсила → Ловушка → Практики** (суперсила из `positive-profiles`, ловушка из `whenHigh`,
    практики из нового файла). Опциональные ранг/балл/достоверность для использования в топ-3
  - `positive-profiles.ts` расширен с 13 → **22 шкал** (добавлены суперсилы 9 новых шкал 5.1)
  - `_components/states-block.tsx` + `STATE_CODES` — **блок «Состояния»** (BAR, DPR) отдельно
    от черт: эпизодические аффективные состояния в радаре рядом с чертами — категориальная
    ошибка. Дестигматизирующий нарратив «черты стабильны, состояния приходят и уходят»
  - Dev-превью `/dev/developmental`; `growth-practices.test.ts` (7 unit: покрытие 22 шкал
    практиками и суперсилами, валидность методов, метки на обоих языках)

### Changed

- `profile-details.tsx`: топ-3 ведущих **черт** (состояния BAR/DPR исключены) рендерятся
  developmental-карточками вместо плоского «процент + описание + суперсила»
- **Убрана утечка клинической лексики в UI юзера** (этап 5.6.1): `getWarnings` в `quiz-results.tsx`
  показывает названия расстройств, «клиническую оценку» и дифференциальную диагностику
  ТОЛЬКО психологу/админу (`showClinical`); юзер видит мягкую developmental-формулировку
  «переменчивость настроения», сопровождение специалиста предлагается через практики блока «Состояния»

## [0.8.1] - 2026-07-05

### Added

- **Право на самостоятельное удаление данных (этап 5.6.3, 152-ФЗ ст. 14–17)**:
  - Политика `@@allow('delete', userId == auth().id)` на `QuizSession` — удаление сессии
    каскадит на ответы и пропуски (`onDelete: Cascade`); чужие данные не затрагиваются.
    Политика ZenStack — рантайм-проверка, миграция БД не требуется
  - `deleteMyQuizDataAction` — `deleteMany` под enhanced-клиентом (фильтр по владельцу)
  - Секция «Мои данные» в настройках: кнопка удаления + диалог подтверждения (ru/en),
    тост с числом удалённых сессий

## [0.8.0] - 2026-07-05

### Added

- **Информированное согласие и политика конфиденциальности (этап 5.6.3, 152-ФЗ)**:
  - `_data/disclaimer.ts` — единый источник текста дисклеймера (вынесен из `quiz-intro`)
  - `_components/disclaimer-consent.tsx` — shared-компонент согласия: полный дисклеймер +
    непредотмеченный чекбокс со ссылкой на `/privacy`. Используется в полном квизе и экспрессе
  - **Экспресс теперь тоже гейтит старт согласием** (раньше согласие было только в полном квизе) —
    гость, хранение в localStorage, на сервер ничего не уходит
  - `/privacy` — страница политики конфиденциальности (ru/en): оператор по реестровому номеру
    РКН 77-26-555440, категории данных, правовое основание (согласие), гостевой режим,
    срок хранения, защита, права субъекта (ст. 14–17), контакт privacy@letar.best,
    дисклеймер «не диагностика, не медизделие»
  - E2E `express.spec.ts` обновлён: принимает согласие перед стартом (клик по контролу
    чекбокса — в лейбле есть ссылка на /privacy); 2 passed

### Changed

- `quiz-intro.tsx` отрефакторен на shared `DisclaimerConsent` + `_data/disclaimer.ts`
  (текст дисклеймера больше не дублируется; чекбокс теперь ссылается на `/privacy`)

## [0.7.1] - 2026-07-05

### Added

- **Тесты ядра спринта (этап 5.8)**:
  - `client-scoring.test.ts` — 8 unit-тестов `computeClientScores` (нормализация raw/actual_max,
    суммирование max по вопросам, независимость шкал, игнор неизвестных id и вне-диапазонных опций)
  - `archetest-e2e/src/express.spec.ts` — 2 E2E (Playwright): полный гостевой флоу
    intro → 24 вопроса → гексаграмма с QR и CTA; сохранение результата в localStorage и
    восстановление после перезагрузки. Прогон chromium — 2 passed
  - `data-testid` (`quiz-option`, `quiz-skip`) на кнопках `QuizQuestionCard` — стабильные
    селекторы для E2E (пригодятся и E2E полного квиза, Фаза 4)
  - `archetest-e2e/playwright.config.ts` адаптирован под archetest: порт 3012, имя nx-проекта,
    `BASE_URL` управляет и `webServer.url` (прогон против уже поднятого сервера при коллизии порта)

### Notes

- Обновлён статус этапа 5.8 в PLAN: кросс-правила уже покрыты (`interpretation-rules.test.ts`);
  ipsative-скоринг и E2E-согласие заблокированы нереализованными фичами (5.6 / 5.6.3).

## [0.7.0] - 2026-07-05

### Added

- **Safety-net (этап 5.6.4)** — этический минимум + юридический щит для аудитории феста:
  - `_data/crisis-resources.ts` — единый источник телефонов доверия (ru/en в одном файле для
    ревью перед фестом): взрослая линия психологической помощи МЧС 8-800-333-44-34 первой,
    Всероссийский телефон доверия 8-800-2000-122 с явной пометкой «для детей, подростков и
    родителей», дисклеймер «звоните 112». Пороги и копирайтинг («это не диагноз»).
  - `_lib/safety-net.ts` — чистая логика: `needsSafetyNet` (шкалы состояния DPR/BAR/BOR ≥ 60%
    ИЛИ маркер риска), `needsDarkReassurance` (тёмные шкалы ≥ 60%). Дремлющий механизм
    маркера суицид-риска: реестр `SUICIDE_RISK_MARKERS` (пуст) + `hasSafetyMarker` — активация
    после разметки вопроса психологом (зависит от 5.6.2, аналог PHQ-9 item 9)
  - `_components/safety-net-block.tsx` — shared: `SafetyNetBlock` (кризисный блок с tel-ссылками,
    заботливый тон) + `DarkReassuranceNote` (мягкая формулировка «черта = ресурс, не приговор»)
  - Интеграция в результаты квиза (`quiz-results.tsx`) и экспресса (`express-results.tsx`)
  - Dev-превью обоих блоков: `/dev/safety-net` (ru/en)
  - 12 unit-тестов `safety-net.test.ts` (пороги, граница включительна, маркер, пустой реестр)

### Fixed

- Прежний кризисный блок в `quiz-results.tsx` показывал ТОЛЬКО детский телефон 8-800-2000-122
  (для взрослой аудитории феста — не та линия), а в английской локали телефон не выводился
  вовсе. Заменён на `SafetyNetBlock` с корректными линиями в обеих локалях.

## [0.6.0] - 2026-07-05

### Added

- **Express Scan — фестивальный экспресс-тест (этап 5.3)** — гостевой режим `/express`:
  - Роут `/express` — 24-вопросный тест без регистрации (8 шкал гексаграммы × 3 вопроса)
  - `express-select.ts` — детерминированное покрытие: выборка по доминантной шкале
    гарантирует ровно 3 вопроса на каждую из 8 шкал (HUM, KAN, FAI, MAC, NAR, ANT, SAD, MAS);
    инъекция `dominantOf`/`shuffle` для тестируемости; 6 unit-тестов (этап 5.8)
  - `getExpressQuestionsAction` — серверная выборка без attention-check'ов (24 вопроса
    слишком мало, чтобы жертвовать покрытием шкал)
  - `ExpressContainer` — гостевой флоу intro → quiz → results; результат считается на
    клиенте и хранится **только в localStorage** (на сервер ничего не отправляется)
  - `ExpressResults` — гексаграмма 8 шкал + краткие описания по группам (Светлая/Тёмная
    триады, деструктивные паттерны), β-бейдж MAS, QR-код на полный тест (`qrcode.react`),
    кнопка «Привязать результат к аккаунту»
  - Привязка к аккаунту передаёт **ответы, а не баллы** — сервер пересчитывает сам через
    существующий `submitQuizAction`; гость откладывает ответы в `PENDING_QUIZ_KEY` и входит,
    XP/ачивки только за серверно-подтверждённые сессии
  - Ссылка «Экспресс» в шапке и мобильном меню; i18n ru+en (namespace `express`)
- Общие хелперы (shared-first, убрано дублирование из `quiz-container`): `seeded-shuffle.ts`
  (mulberry32 + shuffleWithSeed), `client-scoring.ts` (`computeClientScores`),
  `storage-keys.ts` (единый источник ключей хранилища)

## [0.5.0] - 2026-07-05

### Added

- **Гексаграмма «Архитектура личности» (этап 5.2)** — SVG-визуализация наложения триад:
  - `hexagram-geometry.ts` — чистая геометрия: вершины треугольников Света (HUM/KAN/FAI,
    пик вверх) и Тени (PSY/NAR/MAC, пик вниз), S-вектор (центр тяжести), пересечение
    полигонов (Sutherland–Hodgman) → «зона интеграции» (Jaccard, в UI подписана как
    визуальная метафора), интенсивность ауры SAD/MAS; 18 unit-тестов
  - `HexagramChart` — компонент: два треугольника с заливкой пересечения, внешние
    кольца-ауры SAD/MAS (толщина и насыщенность по баллу, β-бейдж MAS), S-вектор
    с меткой «Конструктивный Архитектор» (MAC+KAN ≥ 60), прогрессивная rAF-анимация
    построения (с уважением prefers-reduced-motion), подписи с переносом длинных
    названий, dark mode через семантические токены, мобильная адаптация
  - Интеграция: блок в результатах квиза (с нарративом «Гексаграмма/Шаткона» и
    микрокопией о независимости триад) и блок «Архитектура личности» в кабинете
    психолога; показывается только при наличии баллов шкал триад (банк v2)
  - Dev-превью `/dev/hexagram` с 6 демо-профилями (в production — 404)

### Fixed

- Неиспользуемый `tCommon` в `mobile-drawer.tsx` ломал typecheck

## [0.4.0] - 2026-07-04

### Added

- **9 новых шкал (этап 5.1)** — ядро 22: MAC, HUM, KAN, FAI, SAD, MAS (бета), ASD, DIR, ALX;
  кластеры dark/light/destructive/spectrum, архетипы и описания ru+en, PSY — display-alias ANT,
  константы триад и гексаграммы (`HEXAGRAM_SCALE_CODES`, 8 шкал без DIR)
- **135 ситуационных вопросов** (15 × 9 шкал, банк 1955 → 2090): батчи для ревью психологом
  в `prisma/question-batches/5.1/`, ≥1/3 reverse-keyed, без само-ярлыков для тёмных шкал;
  `merge-question-batch.ts` с валидацией и автопересчётом справочника max-баллов
- **Шкалы валидности**: 6 attention-check вопросов (банк 2096, инжект 2/сессию),
  `computeValidityFlags` (провал ≥2 чеков / монотонность ≥90%), поля `isValid` +
  `validityFlags` в QuizSession; невалидные протоколы без XP/ачивок/лидерборда и вне усреднения
- **`questionBankVersion` в QuizSession** (миграция): v2 = 2090/22; усреднение только
  в пределах версии, история и кабинет отдают версию для UI
- **Кросс-шкальные правила** (`interpretation-rules.ts`, слой поверх raw): ASD×DIR → −80%
  отображения ANT/SAD + «Радикально честный»; «Маскирующий»; «Стратег» (MAC+SZD);
  NAR-фасеты «Уязвимая грандиозность»/«Грандиозный профиль»
- **Тесты (5.8):** 40 unit (шкалы, целостность банка, валидность, кросс-правила)

### Fixed

- **`questions-dump.json` был битым JSON** (смешанное экранирование, литеральные `\n`) —
  seed падал; дамп пересобран структурным парсером, 11 расхождений со справочником
  зафиксированы в `docs/question-bank-discrepancies.md` для ревью психологом
- **`seed-questions.ts`** переписан на ZenStack v3 ORM (старый импорт `PrismaClient` не работал)

---

Продолжение в ./CHANGELOG_2026_04_04.md
