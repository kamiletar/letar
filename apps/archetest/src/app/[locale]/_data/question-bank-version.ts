/**
 * Версия банка вопросов. Инкрементируется при любом изменении состава банка,
 * ломающем сопоставимость нормализации между сессиями.
 *
 * История:
 * - v1 — 1955 вопросов, 13 шкал (исходный банк)
 * - v2 — 2090 вопросов, 22 шкалы (этап 5.1, 2026-07-04): +135 вопросов новых шкал,
 *   кросс-скоринг новых вопросов затрагивает и старые шкалы → actual_max изменился
 *
 * Сессии с разными версиями НЕ сравниваются в динамике и не усредняются вместе.
 */
export const QUESTION_BANK_VERSION = 2
