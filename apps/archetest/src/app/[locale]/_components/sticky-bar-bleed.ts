/**
 * Пропсы `StickyActionBar` «от края до края» на телефоне: панель выходит за горизонтальные
 * отступы `Container` (px=4 на base), на десктопе — по ширине контейнера.
 *
 * ⚠️ Одного `mx={-4}` мало: у `StickyActionBar` `w="100%"`, и отрицательный отступ только
 * сдвигал панель влево на 16px — справа оставался просвет в 32px, сквозь который под
 * панелью просвечивал текст страницы (аудит 2026-09-24). Ширина компенсируется явно.
 */
export const STICKY_BAR_BLEED = {
  mx: { base: -4, md: 0 },
  w: { base: 'calc(100% + 2rem)', md: '100%' },
} as const
