'use client'

/**
 * ArchetestForm — createForm-инстанс архетеста (этап 5.7).
 * Пока без extraSelects/extraComboboxes/extraFields — появятся при необходимости.
 */

import { createForm } from '@letar/forms'

export const ArchetestForm = createForm({})
