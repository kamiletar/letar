# Выполненные задачи: Archetest — часть 9 (перенос из PLAN.md: Фаза 5)

> Точка входа и карта частей — [PLAN_COMPLETED.md](./PLAN_COMPLETED.md). Здесь: закрытые пункты Фазы 5 (Инпсихофест
> Спринт) — этапы 5.6–5.10 и приоритеты спринта. Журнал «Последнее обновление» — в [части 10](./PLAN_COMPLETED_10.md).

## Перенос из PLAN.md: Фаза 5: заголовок спринта, приоритеты, этап 5.6

> Решения, которые продолжают действовать (RU-first, developmental-фрейм, политика норм), остались в PLAN.md.

#### Фаза 5: Инпсихофест Спринт — ⏹ закрыт без феста (2026-09-24)

> **Фест отменён для проекта** (Kami на него не пошёл). Сделанное в спринте остаётся в продукте;
> даты и приоритеты ниже — исторические, не действующие. Решения внутри (RU-first,
> developmental-фрейм, политика норм) продолжают действовать.

> Цель: B2B-демо на фестивале психологов. Мобильный UI, 24-вопросный экспресс-тест,
> визуализация «Гексаграмма» (Шаткона — Тёмная + Светлая триады).

**Приоритеты спринта** (порядок работ, не сокращение скоупа — весь скоуп в спринте):

| Приоритет | Этапы                                                                                                                | Ориентир              |
| --------- | -------------------------------------------------------------------------------------------------------------------- | --------------------- |
| **P0**    | 5.1 ✅, 5.2 ✅, 5.3 ✅, 5.6.3 ✅, 5.6.4 ✅, 5.6.5 ✅, 5.8 ✅                                                         | недели 1–4 (до 31.07) |
| **P1**    | 5.6.1 ✅, 5.4 ✅, 5.7, 5.9, ревью вопросов психологом (пакет ревьюера готов: `docs/question-review/`, ждёт ревьюера) | недели 4–5 (до 07.08) |
| **P2**    | 5.5 ✅, полный 5.6.2 (α-метрики — по выборке)                                                                        | последними в очереди  |

> ~~Неделя 15.08 минус 7 дней — буфер: прогон демо, чек-лист феста, без новых фич.~~ Отменено.

**Этап 5.6 — Польза, согласие и научный фундамент** ⭐ (критично для доверия сообщества)

## Перенос из PLAN.md: этап 5.6.2 — выполненные пункты

- [x] Раздел «Методология» на `/for-professionals`: на каких инструментах основан тест
      ✅ 2026-07-06 (v0.15.0) — таблица соответствия 9 групп шкал валидированным конструктам
      (см. PLAN_COMPLETED.md)
- [x] Честно разделить **валидированное ядро** и **авторские/бета-конструкты** ✅ 2026-07-06
      (v0.15.0), долг закрыт 2026-07-28 (v0.24.0) — из текущих 22 шкал только MAS не имеет
      валидированного прототипа (авторский конструкт, помечен «бета» в UI и в разделе
      «Методология»). RES_PHYS, RES_AFF, SPEC_INT и индекс «Броня и Радар» реализованы
      в v0.23.0, но маркировка на публичной странице отставала — в v0.24.0 в «Методологию»
      добавлен абзац про три экспериментальные шкалы вне ядра (только кабинет психолога,
      «бета», на скоринг ядра не влияют)

- [x] Абзац про дименсиональные модели на `/for-professionals` (решение 2026-07-03): AMPD/PID-5,
      HiTOP, пять доменов МКБ-11 + маппинг 13 шкал на них ✅ 2026-07-09 (v0.22.0) — базовый абзац
      существовал с v0.15.0 (пункт не был отмечен — вскрыто ревизией), добавлен ориентировочный
      маппинг шкал на пять доменов (конструктный уровень, с оговоркой «эмпирическая факторная
      проверка — по выборке»); приостановка МКБ-11 в РФ (02.2024) упомянута

## Перенос из PLAN.md: этап 5.7 — фестивальная инфраструктура и B2B-конверсия

**Этап 5.7 — Фестивальная инфраструктура и B2B-конверсия — P1** 🆕

> Wi-Fi на выставках стабильно плох, а бизнес-цель феста — лиды психологов.
> Демо, падающее от сети, хуже отсутствия демо; демо без CTA — потерянные лиды.

- [x] Offline-first для `/express` ✅ 2026-07-08 (v0.18.0) — Serwist по эталону grandslamcup:
      `src/app/sw.ts` (precache статики + NetworkFirst для документов `/express` и `/en/express`
      с install-прекэшем в общий кэш `express-pages`), `next.config.js` → `.mjs` с `withSerwist`
      (dev отключён), build target `next build --webpack` (Turbopack не поддерживает Serwist).
      Регистрация SW строго по согласию (`useOfflineConsent('archetest-offline-consent')` +
      `OfflineConsentBanner`, эталон mandala) — только на `/express`. Вопросы в отдельный бандл
      НЕ выносились: страница серверная, выборка приезжает в закэшированном документе (онлайн —
      свежая, офлайн — последняя успешная копия), второй источник истины банка не создан.
      Гостевой флоу офлайн целиком: подсчёт клиентский, localStorage, QR из бандла
- [x] Лид-форма для психологов на `/for-professionals` (@letar/forms): имя + email + чекбокс ПДн
      ✅ 2026-07-07 (v0.16.0) — `ProfessionalLeadForm` на `/for-professionals#lead`,
      createForm-инстанс `ArchetestForm` (ранее отсутствовал в app), модель `ProfessionalLead`
      с гостевой политикой `@@allow('create', true)`, чекбокс не предотмечен
- [x] CTA после экспресс-результата: «Вы психолог?» → `/for-professionals#lead` ✅ 2026-07-07
      (v0.16.0) — карточка на экране результатов экспресса, `?source=express-cta` в query
- [x] QR-код на `/for-professionals` (раздатка / бейдж / стенд) ✅ 2026-07-07 (v0.16.0) —
      dev-роут `/dev/qr` (print-friendly, два QR: `/express` и `/for-professionals`)
- [x] Kiosk-режим на демо-планшете ✅ 2026-07-08 (v0.18.0) — `KioskResetButton` на
      `/express?kiosk=1`: фиксированная кнопка «Новый посетитель», двухтапное подтверждение
      (окно 4 сек — случайный тап посетителя не убивает его ответы), сброс чистит гостевой
      результат, отложенные ответы **и согласие** (152-ФЗ: согласие персонально) + полная
      перезагрузка страницы (чистый React-стейт, свежая выборка вопросов; офлайн — из SW-кэша).
      2 E2E (`kiosk.spec.ts`)
- [x] **Тизер остальных шкал в экспресс-результатах** ✅ 2026-07-08 (v0.20.0) — под группами
      шкал гексаграммы: `ScaleTeaser` показывает 12 оставшихся шкал ядра (заблокированный
      серый стиль, иконка замка) + CTA на полный тест. `TEASER_SCALE_CODES` в
      `personality-types.ts` = `ALL_SCALE_CODES` минус `HEXAGRAM_SCALE_CODES` минус
      `STATE_CODES`. Названия только юзерские/архетипы (без клиники, политика 5.6.1);
      BAR/DPR исключены из списка, упомянуты отдельной строкой как блок «Состояния»
- [x] ⏹ **Отменено 2026-09-24** (фест не состоялся для проекта) — Чек-лист демо + полный прогон
      за неделю до феста. Документ заархивирован. Офлайн-режим `/express` живьём так и не проверен —
      проверить при первом реальном выездном показе.
      ⚠️ **Печатать раздатку и презентацию можно ТОЛЬКО с локальной машины** (уточнено
      2026-07-28): `/dev/qr` и `/dev/presentation` вызывают `notFound()` при
      `NODE_ENV === 'production'`, а `NODE_ENV` в собранном `next build` всегда `production` —
      и на проде, и на staging (см. [env-files](/.claude/rules/env-files.md)). То есть по
      боевой ссылке этих страниц нет. Печать — через `nx dev archetest` локально, заранее,
      а не в день феста. Это же касается нового `/dev/dark-core`.
      **Документ-чек-лист готов** ✅ 2026-07-09 — [docs/demo-checklist.md](./docs/demo-checklist.md)
      (10 секций: офлайн-проверка SW, функциональный флоу, печать раздатки из dev, вычитка,
      контрольный звонок safety-net, 152-ФЗ/РКН, оборудование, лиды, утренний чек, пост-фест).
      ⏳ **Сам прогон — в буферную неделю** (живая офлайн-проверка: включить офлайн на планшете →
      `/express` открывается и проходится; dev отключает SW — до сих пор проверен только билд)
- [x] **Презентация продукта** (заявлено Kami 2026-07-08) — RU-first ✅ 2026-07-09 (v0.22.0) —
      нарратив собран в print-friendly dev-роут `/dev/presentation`: 8 слайдов по карте материала
      ниже, живая `HexagramChart` с примерным профилем, разрывы страниц при печати (образец —
      `/dev/qr`, в production роут закрыт). ⚠️ Контент — на вычитку Kami перед фестом
      (формулировки, бизнес-условия слайда «Приглашение»). Карта материала (источник слайдов):
  - Научный фундамент: маппинг 22 шкал на валидированные инструменты, честное
    разделение ядро/бета (5.6.2, раздел «Методология» на `/for-professionals`)
  - Гексаграмма/Шаткона: Светлая+Тёмная триады — независимые измерения, S-вектор,
    «индекс интеграции» с оговоркой (5.2)
  - Developmental-фрейм: Суперсила → Ловушка → Практики (КПТ/DBT/схема), без
    клинических ярлыков у юзера, «Состояния» отдельно от «Черт» (5.6.1)
  - Психометрическая честность: шкалы валидности, анти-спам геймификация
    (XP за дни, не за объём — 5.9), state/trait через mood check-in (5.9.2),
    дорожная карта норм: перцентили при N ≈ 200–300, RU-выборка первой (5.9.5)
  - Этика и право: 152-ФЗ/РКН, согласие, самоудаление данных, safety-net,
    «не диагностика» (5.6.3/5.6.4)
  - Демо: экспресс за 3–5 минут офлайн, kiosk-режим, QR-конверсия в полный тест
    и лид-форму (5.3/5.7)

## Перенос из PLAN.md: этап 5.9 — геймификация 2.0 (5.9.1–5.9.4)

> Пункт 5.9.5 «Нормативная гигиена» остался в PLAN.md (ждёт выборки).

**Этап 5.9 — Геймификация 2.0: персональная, анти-спам — P1** 🆕

> **Решение Kami 2026-07-03:** соревновательный слой выпиливается. Геймификация остаётся
> только персональной; цель — мотивировать _периодические_ прохождения в разном настроении
> (state vs trait: усреднение сессий в разных состояниях выделяет устойчивое trait-ядро
> из state-шума), при этом препятствовать слишком частым прохождениям.
> Старая механика активно вредила: SPEED_DEMON награждал за careless responding,
> XP за число ответов стимулировал прокликивание, лидерборд загрязнял будущие нормы.

- [x] **5.9.1 Выпилить соревновательный слой** ✅ 2026-07-06 — роут `/leaderboard` +
      `LeaderboardTable`/`LeaderboardUserRow` удалены, ссылки из `header.tsx`/`mobile-drawer.tsx`/
      `quiz-results.tsx` убраны (кэш `QuizLeaderboardEntry` остаётся в БД до Фазы 3, `recalcLeaderboardEntry`
      продолжает писать); ачивка `SPEED_DEMON` удалена (`achievements.ts` + `achievements.action.ts`,
      `sessionDurationMs` больше не считается); `calculateXp` — убран множитель за объём ответов
      (`totalAnswers * 2`), теперь `sessions × 100 + achievements`. TOP_PERFORMER_3 в коде не
      существовал (стейл-упоминание из v0.2.0-доков, только текст в PLAN_COMPLETED.md). Неиспользуемые
      i18n-ключи `nav.leaderboard`/`quiz.leaderboard.*` убраны из ru/en
- [x] **5.9.2 Mood check-in перед сессией** ✅ 2026-07-06 — 1 экран (~10 сек), сетка эмодзи 3×3
      по циркумплексу Рассела (валентность × энергия): `MoodCheckIn` (новый компонент),
      `moodValence`/`moodEnergy` (1-3, nullable) на `QuizSession` (миграция `20260706174730`).
      Показывается перед каждой новой порцией вопросов (первый старт и «Продолжить»), кнопка
      «Пропустить» без friction. `SubmitQuizSchema` принимает оба поля опционально; сохраняются
      в `submitQuizAction`. Не участвует в скоринге черт — только для (а) психолога — «профиль
      в грусти vs профиль в ресурсе» (кабинет — Фаза 3), (б) будущей карты стабильности профиля.
      Полный квиз only — экспресс (`/express`) не покрыт, фестивальная скорость приоритетнее
- [x] **5.9.3 Кулдаун ретестов** ✅ 2026-07-08 — **решение Kami 2026-07-08: «гибрид»**
      (скоуп уточнён: «сессия» ≠ порция из 50 вопросов):
  - **Новые вопросы банка — свободно в любой день**: выборка и так никогда не повторяет
    отвеченные/пропущенные (`excludeIds` в `getRandomQuestionsAction`), «Продолжить»
    исчезает при исчерпании банка → повтор вопросов структурно невозможен, жёсткий
    кулдаун на повторы не нужен как отдельный механизм
  - **XP-гранула — сутки, не порция**: `calculateXp` теперь принимает число уникальных
    UTC-дней с валидными сессиями (`countUniqueUtcDays` в `ranks.ts`, 9 unit-тестов);
    5 порций в день = 100 XP, та же активность в 5 дней = 500 XP — XP-ферма закрыта,
    спейсинг вознаграждается (согласовано с ачивками ритма 5.9.4)
  - **UI-коммуникация**: `submitQuizAction` возвращает `xpCountedToday`; повторная порция
    дня показывает подпись под CTA «XP за сегодня уже получены — каждый новый ответ
    уточняет профиль» (ru/en) — продолжение не наказывается, а честно объясняется
  - **Попутный фикс**: `recalcLeaderboardEntry` не фильтровал `isValid` — невалидный
    протокол добавлял +100 XP при следующем валидном пересчёте (вопреки 5.1)
  - ⏳ Отложено в Фазу 3 вместе с механизмом ретестов: «окно точности через N дней» и
    72ч-маркировка касаются повторного прохождения после исчерпания банка — самого
    ретеста в системе пока не существует (2096 вопросов мало кто исчерпает до феста)
- [x] **5.9.4 Ачивки за ритм вместо объёма** ✅ 2026-07-06 — новая категория `rhythm` в
      `achievements.ts` (+ i18n `categories.rhythm` ru/en): «Три настроения» `THREE_MOODS`
      (≥3 различных пары mood valence/energy среди сессий), «Возвращение» `RETURN_30`
      (разрыв ≥30 дней между соседними сессиями), «Спейсинг-серия» `SPACING_SERIES`
      (4 сессии подряд, каждый разрыв ≥7 дней), «Полная карта» `FULL_MAP` (все 22 шкалы
      с кумулятивной достоверностью `high` по ВСЕМ отвеченным вопросам пользователя —
      новый `getCumulativeConfidence` экспортирован из `quiz.action.ts`, вызывается из
      `achievements.action.ts` с уникальными `sortOrder` по всем сессиям). Логика — чистые
      функции `countDistinctMoods`/`hasGapOfAtLeast`/`hasSpacingSeries` в `achievements.action.ts`

## Перенос из PLAN.md: этап 5.10 — аудит банка вопросов: часть A и предусловие

> Часть B (пакет ревьюеру) осталась в PLAN.md.

**Этап 5.10 — Аудит всего банка вопросов** (часть A ✅ 2026-07-28 v0.26.0 · часть B — пакет ревьюеру в пуле 2026-09-24)

> Банк — фундамент всего теста, и он ни разу не проверялся целиком. Ядро v1
> (1955 вопросов от психолога, 19.03.2026) вошло в тест до появления нынешних
> критериев качества; ревью 5.1/5.5 покрывает только 165 вопросов из 2126.
> Всё, что стоит поверх банка — скоринг, ipsative-ранги, тёмное ядро, нормы —
> наследует его дефекты и не может быть точнее него.

**Оценка объёма — честно.** Ручная вычитка 2126 вопросов психологом это ~35–50 часов;
за один заход это не влезает. Поэтому аудит разделяется на две
части: **машинную** (полная, делается сразу, ловит структурные дефекты) и
**содержательную** (выборочная, идёт через ревьюера, без дедлайна).
Сначала машинная — она же покажет, куда направлять дорогое человеческое внимание.

**Часть A — машинный аудит (скрипт `scripts/audit-question-bank.ts`, отчёт в `docs/`):**
✅ **Выполнена 2026-07-28 (v0.26.0)** — отчёты `docs/question-bank-audit.md` (человеку)
и `.json` (полные списки, сырьё части B), перегенерируются запуском скрипта. Итоги по
пунктам ТЗ ниже: дубли — 0 точных, 19 почти-дословных + 323 сюжетных пары на решение
(понадобилось два сигнала — триграммный Жаккар слеп к сюжетным дублям разной длины,
контрольные пары «кошелёк»/«сдача» ловит только IDF-взвешенный containment по редким
смысловым словам); пустой скоринг — 0 (ожидание про №2096 не подтвердилось: это
attention-check, он без баллов намеренно); расхождения — ровно 11 известных, новых нет,
`global_max_scores` сходится с суммой per-question; перекос — от AVD 1192 до SAD 21
в ядре (дефицит до порога high=30: SAD −9, ASD −8, MAS −7, ALX −6); асимметрия — без
дистракторов 0, слабый сигнал 0, вариант-вездеход 1 (№1497), баллы все в 1..3;
**полнота EN — главная находка: №101–1665 (1565 вопросов, 74% банка) не переведены,
`scenarioEn`/`textEn` = копия русского**; reverse-баланс — размечены только 165
батч-вопросов (33–53%), ядро v1 меток не имеет. Плюс 13 unit на чистые функции
(`scripts/audit-lib.ts`).

- **Дубли и почти-дубли сценариев.** При работе над шкалой HON нашлось по 2–3 варианта
  одного сюжета («нашли кошелёк» — 3 вопроса, «кассир дал больше сдачи» — 2). Уникальность
  `scenario` проверяется точным совпадением, близкие переформулировки проходят.
  Считать нормализованное расстояние, выдать список пар на решение.
- **Вопросы, которые ничего не измеряют.** В справочнике максимумов уже есть записи
  с пустым скорингом (например номер 2096: `{}`) — такой вопрос отнимает место в порции
  из 50 и не даёт ни одного балла ни одной шкале.
- **Расхождения справочника max-баллов** — 11 известных (`KNOWN_DISCREPANCIES`,
  номера 691, 1946–1955): справочник заявляет шкалы, которых нет в опциях вопроса.
  Это отдельный пункт техдолга ниже, здесь он закрывается вместе со всем остальным,
  потому что чинится тем же проходом по банку.
- **Перекос покрытия шкал** — AVD 1192 вопроса против SAD 21, разница в 57 раз
  (пункт техдолга «выровнять банк по редким шкалам»): дать точные числа по всем
  25 скорящимся шкалам и посчитать, сколько вопросов не хватает каждой редкой шкале
  до приемлемого минимума.
- **Асимметрия вариантов**: вопросы, где все четыре варианта дают баллы одной шкале
  (нет дистракторов), или где максимум шкалы < 2, или где один вариант скорит 5+ шкал.
- **Полнота EN**: пустые/совпадающие с RU `scenarioEn` и `textEn`.
- **Reverse-баланс по шкалам**: доля reverse-вопросов у каждой шкалы (у ядра v1 метка
  `_reverse` не проставлялась вовсе — она появилась только в батчах 5.1).

**Предусловие — техдолг merge-скрипта** — ✅ режим `--update` сделан (PLAN_COMPLETED, «Режим `--update`»).
Открытым осталось другое: правка текста существующего вопроса не доезжает до БД (seed — append-only) —
режим `--sync-texts` ✅ 2026-09-24 (v0.28.5): `nx db:seed archetest -- --sync-texts [--dry-run]` —
обновляет только формулировки, на изменённых баллах откатывается целиком (exit 1). Исходный текст: любой вердикт «Править» по уже влитому
вопросу упирается в то, что `merge-question-batch.ts` умеет только дописывать
(см. техдолг ниже). Аудит без режима `--update` даст список проблем, который нечем
применить, — поэтому режим правки делается до части B, а не после.
