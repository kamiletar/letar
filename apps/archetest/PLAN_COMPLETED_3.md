# Выполненные задачи: Archetest — часть 3 (2026-07-04 … 2026-07-05, v0.4.0 → v0.11.0)

> Точка входа и карта частей — [PLAN_COMPLETED.md](./PLAN_COMPLETED.md). Здесь: этапы 5.1 (ядро 22 шкал), 5.2 (гексаграмма), 5.3 (Express Scan), 5.6.4, 5.8, 5.6.3, 5.6.1, 5.4, липкая CTA.

## Сессия 2026-07-04 — Этап 5.1: ядро 22 шкал (v0.4.0) ⚙️

> Первый кодовый инкремент спринта. Коммиты `21bf610` → `aca17b2`.

### Реализовано

- **Починка банка вопросов:** questions-dump.json был битым JSON (смешанная глубина
  экранирования + литеральные `\n`) — seed не работал с момента экспорта. Пересобран
  структурным парсером; 11 расхождений со справочником max-баллов (блок BAR/BOR
  1946–1955 + вопрос 691) → `docs/question-bank-discrepancies.md` на ревью психологу.
- **9 новых шкал** в `personality-types.ts` (ядро 22), архетипы ru+en:
  Гроссмейстер (MAC), Целитель (HUM), Законодатель (KAN), Маяк (FAI), Гладиатор (SAD),
  Мученик (MAS, бета), Инженер (ASD), Зеркало (DIR), Переводчик (ALX).
- **135 ситуационных вопросов** (банк 2090): батчи `prisma/question-batches/5.1/`,
  5/15 reverse на шкалу (помечены `_reverse`), кросс-скоринг обогащает и старые шкалы;
  `merge-question-batch.ts` — валидация + автопересчёт справочника. Валидатор в деле:
  поймал слабый вес KAN, дубль сценария «кошелёк» и нулевой вес в MAS.
- **Валидность протокола:** 6 attention-check вопросов (банк 2096, 2 инжектятся в сессию),
  `isValid`/`validityFlags` в QuizSession, провал ≥2 чеков или монотонность ≥90% →
  без XP/ачивок/лидерборда, вне усреднения. Первый кирпич Геймификации 2.0 (5.9).
- **questionBankVersion** (миграция `20260704045424`): v1=1955/13, v2=2090/22;
  усреднение только внутри версии.
- **Кросс-шкальные правила** `_lib/interpretation-rules.ts`: слой поверх raw без мутации.
- **Тесты:** 40 unit; seed переписан на ZenStack v3 ORM и прогнан (локальная БД 2096).

### Осталось в 5.1

- Пары согласованности (VRIN) и флаги времени ответа — отложены на после феста
- Ревью 135 вопросов и 11 расхождений психологом (P1)

---

## Сессия 2026-07-05 — Этап 5.2: Гексаграмма «Архитектура личности» (v0.5.0) ✡️

> Этап 5.1 закрыт полностью (NAR-фасеты были реализованы в 632af5a, отмечены задним числом).

### Реализовано

- **`_lib/hexagram-geometry.ts`** (TDD, 18 unit-тестов): чистая геометрия гексаграммы —
  оси через 60° (HUM 90°, MAC 30°, FAI 330°, ANT 270°, KAN 210°, NAR 150°), минимальный
  радиус 8% против схлопывания, S-вектор (центроид 6 вершин), пересечение выпуклых
  полигонов (Sutherland–Hodgman) + Jaccard как «индекс интеграции» (метафора; полная
  гексаграмма даёт ровно 0.5 — использовано как аналитический тест-инвариант),
  аура SAD/MAS 0..1, флаг «Конструктивный Архитектор» (MAC и KAN ≥ HIGH_THRESHOLD).
- **`HexagramChart`**: SVG-рендер с заливкой зоны интеграции, кольца-ауры (толщина
  и прозрачность по баллу, MAS пунктиром + β-бейдж), rAF-анимация построения
  (ease-out, prefers-reduced-motion → мгновенно; при обновлении баллов звезда
  перетекает — готово к live-режиму экспресса 5.3), перенос длинных подписей,
  тёмные шкалы под ярлыками триады (ANT → «Психопатия»), обе темы, мобильный рендер.
- **Интеграция:** блок в `quiz-results.tsx` (с нарративом Шатконы и микрокопией
  «независимые измерения») и Card-блок «Архитектура личности» в кабинете психолога;
  условие показа — ненулевые баллы шкал гексаграммы (банк v2).
- **Dev-превью `/dev/hexagram`:** 6 демо-профилей для визуального ревью (prod → 404).
- Итог: 58 unit-тестов в приложении, lint/typecheck чистые, проверено в preview
  (обе темы, desktop + 375px).

### Решения по ходу

- «Таб» кабинета реализован как Card-блок — страница клиента блочная, табов нет;
  вводить Tabs ради одного блока = лишний рефакторинг перед фестом.
- Микрокопия «независимые измерения» продублируется в «Методологии» вместе с 5.6.2,
  когда появится страница `/for-professionals` с разделом методологии.

---

## Сессия 2026-07-05 — Этап 5.3: Express Scan (v0.6.0) ⚡

> Фестивальный экспресс-режим — гостевой 24-вопросный тест на роуте `/express`.

### Реализовано

- **`_lib/express-select.ts`** (TDD, 6 unit-тестов): чистая выборка «3 вопроса на каждую
  из 8 шкал гексаграммы». Детерминированное покрытие через группировку по доминантной
  шкале (`expressDominantScale`) — не вероятностная стратификация полного теста. Инъекция
  `dominantOf`/`shuffle`/`perScale` для тестируемости. В банке минимум 6 доминантных
  вопросов на самую малую шкалу (SAD) → квота 3 гарантирована.
- **`_actions/express.action.ts`** — `getExpressQuestionsAction`: серверная выборка без
  attention-check'ов (на 24 вопроса жертвовать покрытием шкал нельзя), финальное
  перемешивание.
- **`ExpressContainer`** — гостевой флоу intro → quiz → results. Баллы считаются на
  клиенте (`computeClientScores`) и сохраняются **только в localStorage**
  (`EXPRESS_RESULT_KEY`); при возврате результат восстанавливается. На сервер ничего
  не уходит до явной привязки.
- **`ExpressResults`** — `HexagramChart` (8 шкал, нарратив Шатконы), краткие описания
  по группам (Светлая триада / Тёмная триада / деструктивные паттерны), β-бейдж MAS,
  QR-код (`qrcode.react`) на полный тест + CTA, кнопка привязки к аккаунту.
- **Привязка к аккаунту** переиспользует `submitQuizAction` (на сервер идут **ответы,
  а не баллы** — пересчёт авторитетный). Гость откладывает ответы в общий
  `PENDING_QUIZ_KEY` и входит через ключницу; `QuizContainer` на главной автосабмитит
  их после логина. XP/ачивки — только за серверно-подтверждённые сессии.
- **Shared-first рефакторинг:** общие хелперы `seeded-shuffle.ts`, `client-scoring.ts`,
  `storage-keys.ts` — дублирование убрано из `quiz-container.tsx`.
- Ссылка «Экспресс» в шапке и мобильном меню; i18n ru+en (namespace `express`).

### Проверка

- typecheck (tsgo) + lint чистые, 64 unit-теста зелёные.
- Preview: интро, старт квиза («Вопрос 1 из 24», 4 варианта + пропуск), экран
  результатов (гексаграмма, группы шкал с % и описаниями, QR, кнопки), без ошибок консоли.
- Замечание: rAF-анимация гексаграммы «замирает» в headless-preview (вкладка не в фокусе) —
  артефакт среды, идентично воспроизводится на уже отгруженной `/dev/hexagram`; в реальном
  браузере анимация идёт. Значения баллов проходят корректно (проверено через ScaleGroup).

### Отложено (следующие P0)

- Экран информированного согласия 152-ФЗ (5.6.3) — ещё не сделан.
- Offline-first PWA-прекэш `/express` (5.7) — тогда же зафиксировать набор вопросов
  экспресса детерминированно (сейчас выборка перемешивается на каждый заход).

---

## Сессия 2026-07-05 — Этап 5.6.4: Safety-net (v0.7.0) 🛟

> Этический минимум + юридический щит: телефоны доверия при выраженных шкалах
> состояния и мягкая подача «тёмных» шкал. Shared-first — блоки нужны и в квизе, и в экспрессе.

### Реализовано

- **`_data/crisis-resources.ts`** — единый источник (ru/en в одном файле — чтобы номера
  ревьюились в одном месте перед фестом): `CRISIS_HELPLINES` (взрослая линия МЧС
  8-800-333-44-34 первой, детский 8-800-2000-122 с пометкой аудитории), пороги
  (`SAFETY_NET_THRESHOLD = 60`, шкалы-триггеры DPR/BAR/BOR, тёмные шкалы), копирайтинг
  safety-net и dark-reassurance (тон заботливый, прямо «это не диагноз»).
- **`_lib/safety-net.ts`** (TDD, 12 unit-тестов): `needsSafetyNet(scores, { marker })`,
  `needsDarkReassurance(scores)`. **Дремлющий механизм маркера суицид-риска**:
  `SUICIDE_RISK_MARKERS` (пуст) + `hasSafetyMarker(answers)`. Настоящий маркер требует
  вопроса, размеченного психологом (аналог PHQ-9 item 9, зависит от 5.6.2) — фабриковать
  клинический порог нельзя, поэтому реестр пуст, активация = одна строка.
- **`_components/safety-net-block.tsx`** — shared: `SafetyNetBlock` (Alert с tel-ссылками,
  оба номера, дисклеймер «звоните 112») + `DarkReassuranceNote` («черта = ресурс, не приговор»).
- **Интеграция:** `quiz-results.tsx` (заменил самодельный блок, убрал `needsCrisisBlock`),
  `express-results.tsx` (dark-reassurance — у экспресса нет шкал состояния).
- **Dev-превью:** `/dev/safety-net` (оба блока, текущая локаль).

### Исправлено (баг прежнего блока)

- Старый кризисный блок показывал ТОЛЬКО детский номер 8-800-2000-122 (не та линия для
  взрослой аудитории феста), а в английской локали телефон вообще не выводился.

### Проверка

- `nx test archetest` (safety-net), `typecheck:tsgo`, `lint` — чистые (warnings только
  предсуществующие в prisma-скриптах).
- Preview `/dev/safety-net` в ru и en: оба блока рендерятся, взрослая линия первой,
  детская с пометкой, tel-ссылки, дисклеймер; консоль без ошибок; скриншоты обеих локалей.

### Отложено

- Маркер суицид-риска — до разметки вопроса психологом (5.6.2).

---

## Сессия 2026-07-05 — Этап 5.8: Тесты ядра спринта (v0.7.1) 🧪

> Частичное закрытие 5.8: то, что не заблокировано нереализованными фичами.

### Реализовано

- **`client-scoring.test.ts`** (8 unit): `computeClientScores` — нормализация raw/actual_max,
  суммирование max по вопросам, независимость шкал, игнор неизвестных id и вне-диапазонных опций.
- **`archetest-e2e/src/express.spec.ts`** (2 E2E Playwright): полный гостевой флоу
  intro → 24 вопроса → гексаграмма (QR + CTA) и восстановление результата из localStorage.
  Прогон chromium — 2 passed (против preview-сервера из-за коллизии порта 3012 с другим приложением).
- **`data-testid`** (`quiz-option`, `quiz-skip`) на `QuizQuestionCard` — стабильные E2E-селекторы.
- **`playwright.config.ts`** адаптирован: порт 3012, имя nx-проекта `archetest`, `BASE_URL`
  управляет и `webServer.url` (можно прогнать против уже поднятого сервера).

### Статус остальных пунктов 5.8

- ✅ Кросс-правила — уже покрыты `interpretation-rules.test.ts` (9, этап 5.1).
- 🔶 Merge гостевой сессии — покрыт через E2E-привязку + гарантию `SubmitQuizSchema.strip()`
  (баллы с клиента не принимаются). Отдельный юнит на `calculateScores` затруднён:
  'use server'-модуль тянет next-зависимости → вынести чистое ядро скоринга в `_lib` при 5.6.
- ⏳ ipsative-скоринг — заблокирован (сам скоринг не реализован, это 5.6).
- ⏳ E2E согласие + safety-net — заблокирован (экрана согласия 5.6.3 ещё нет).

### Проверка

- `nx test archetest`: 84 unit зелёные (8 файлов). `nx typecheck:tsgo`, `nx lint`
  (archetest + archetest-e2e) — exit 0. E2E chromium: 2 passed.

---

## Сессия 2026-07-05 — Этап 5.6.3: Согласие и приватность (v0.8.0–v0.8.1) 🔒

> Информированное согласие 152-ФЗ, политика конфиденциальности и право на удаление данных.

### Реализовано

- **`_data/disclaimer.ts`** — текст дисклеймера вынесен из `quiz-intro` (единый источник).
- **`_components/disclaimer-consent.tsx`** — shared: полный дисклеймер + непредотмеченный
  чекбокс со ссылкой на `/privacy` (`target="_blank"`). Общий для квиза и экспресса.
- **Экспресс теперь гейтит старт согласием** (раньше согласие было только в полном квизе):
  гость, хранение в localStorage (общий ключ `quiz_disclaimer_accepted`), на сервер ничего.
- **`quiz-intro.tsx`** отрефакторен на shared-компонент (дубль текста убран).
- **`/privacy`** (ru/en) — политика: оператор по реестру РКН (номер — `.claude/private/COMPLIANCE.md`) (ФИО/ИНН в публичном
  репо не дублируются, как требует docs/rkn-info-letter.md), категории данных, основание
  (согласие), гостевой режим, срок хранения, защита (сервера РФ), права ст. 14–17,
  контакт privacy@letar.best, дисклеймер «не диагностика/не медизделие».

### Найдено и исправлено

- E2E ловил баг: `getByText('Я ознакомился…')` кликал по вложенной ссылке `/privacy`
  (`target=_blank`) → навигация вместо переключения. Исправлено: клик по контролу чекбокса.
  Это стандартное поведение Chakra Checkbox (клик по тексту-ссылке не тоглит) — не баг компонента.

### Проверка

- typecheck/lint (0 ошибок), 84 unit зелёные, E2E chromium 2 passed (с шагом согласия).
- Preview ru: express-интро с согласием (кнопка старта disabled → enabled по клику),
  страница `/privacy` со всеми 7 разделами и футер-дисклеймером; скриншот.

### Часть 2 (v0.8.1) — Право на удаление данных ✅

- **Политика** `@@allow('delete', userId==auth().id)` на `QuizSession` (`schema.zmodel`) —
  удаление каскадит на ответы/пропуски (`onDelete: Cascade`). Политика ZenStack — рантайм,
  **миграция БД не нужна** (только `zenstack:generate`).
- **`_actions/data-deletion.action.ts`** — `deleteMyQuizDataAction`: `deleteMany` под
  enhanced-клиентом (ZenStack фильтрует по владельцу — чужое не затронуть).
- **Настройки** — секция «Мои данные»: кнопка + `alertdialog` подтверждения, тост с числом
  удалённых сессий; i18n ru+en.
- Проверка: typecheck/lint (0 ошибок). Браузерная проверка секции требует авторизации
  (страница настроек под сессией) — не выполнялась; корректность — статикой + политикой ZenStack.

---

## Сессия 2026-07-05 — Этап 5.6.1: Developmental-фрейм результатов (v0.9.0) 🌱

> Сдвиг философии: от diagnosis-фрейма («вот твои патологии в процентах») к карте ресурсов
> и зон роста. Для каждой ведущей черты — три блока: как она работает в плюс, где вредит,
> что с этим делать. Клиническая лексика — только психологу.

### Реализовано

- **`_data/growth-practices.ts`** — блок «Практики» для всех 22 шкал: 2–3 конкретных
  доказательных действия на шкалу с тегом модальности (`cbt`/`dbt`/`schema`/`general` →
  бейдж «КПТ»/«DBT-навык»/«Схема-терапия»/«Практика»). Привязка: BOR → DBT (TIPP,
  противоположное действие, дневник эмоций), NAR/HUM/MAS/SAD/SZD → схема-терапия,
  AVD/DEP/OBC/PAR/ANT/HIS → КПТ (экспозиция, реструктуризация, поведенческие эксперименты),
  BAR/ASD/DIR/ALX/MAC/FAI → общеразвивающие. Формулировки авторские — принцип лицензионной
  чистоты (без заимствования пунктов TAS-20/SD3, см. 5.6.2).
- **`_components/developmental-profile-card.tsx`** — карточка шкалы в 3 блока
  **Суперсила → Ловушка → Практики**. Источники: суперсила = `positive-profiles`,
  ловушка = `whenHigh` из `personality-types`, практики = `growth-practices`. Опциональные
  ранг/балл/достоверность (переиспользуется для топ-3 черт и для состояний). Клиника —
  только под `showClinical`; β-бейдж для MAS.
- **`positive-profiles.ts`** расширен 13 → **22 шкалы** (добавлены суперсилы 9 новых шкал 5.1:
  MAC, HUM, KAN, FAI, SAD, MAS, ASD, DIR, ALX; ru+en).
- **`_components/states-block.tsx`** + **`STATE_CODES`** — блок «Состояния» (BAR, DPR)
  отдельно от черт. Дестигматизирующий нарратив «черты стабильны, состояния приходят и уходят;
  высокий балл — повод присмотреться, не диагноз». Состояния получают тот же developmental-фрейм
  (практики BAR/DPR включают опору на сопровождение специалиста).
- **`profile-details.tsx`**: топ-3 ведущих **черт** (BAR/DPR исключены через `STATE_CODES`)
  рендерятся developmental-карточками вместо плоского «процент + описание + суперсила».
  Модификатор оставлен только для PAG.
- Dev-превью **`/dev/developmental`** (все 22 шкалы + блок состояний с примерными баллами).

### Найдено и исправлено

- **Утечка клинической лексики в UI юзера**: `getWarnings` в `quiz-results.tsx` показывал
  ВСЕМ «биполярное расстройство», «пограничному расстройству», «клиническую оценку» и
  дифференциальную диагностику. Теперь клинические формулировки и дифф-диагностика —
  только под `showClinical` (психолог/админ); юзер видит мягкое «заметна переменчивость
  настроения…». Прямо реализует требование 5.6.1 «убрать клинические ярлыки из UI юзера».

### Проверка

- typecheck:tsgo (0 ошибок, поймал неиспользуемый `HStack` — убран), lint (0 ошибок),
  тесты зелёные: `growth-practices.test.ts` (+7 unit — покрытие 22 шкал практиками и
  суперсилами, валидность методов, метки ru+en).
- Preview ru `/dev/developmental`: блок «Состояния» с BAR/DPR, три блока с иконками
  (суперсила/ловушка/практики), бейджи методов (КПТ, Практика); консоль без ошибок; скриншот.

### Осталось в 5.6 (после 5.6.1)

- ipsative-скоринг (замена `raw / GLOBAL_MAX`) — ядро 5.6, разблокирует unit 5.8.
- 5.6.2 «Методология» на `/for-professionals`, маппинг на валидированные инструменты.

---

## Сессия 2026-07-05 — Этап 5.4: Mobile-first UI для демо (v0.10.0) 📱

> Полировка UX стенда на Инпсихофесте: то, что посетитель видит и трогает у планшета.

### Реализовано

- **Тач-цели ≥ 56px** (`quiz-question-card.tsx`): `minH="56px"` на кнопках вариантов ответа
  и кнопке пропуска (последняя переведена `size="md"` → `size="lg"`). Без горизонтального скролла.
- **Оптимистичное выделение (0ms lag)** (`quiz-question-card.tsx`): локальный `optimisticIndex`
  подсвечивает выбранный вариант мгновенно, с приоритетом над `selectedOption` родителя;
  сбрасывается эффектом при смене `questionNumber`. Прогресс-бар и так считает от `answers.size`.
- **Высококонтрастный режим** (`_hooks/use-high-contrast.ts` + `high-contrast-toggle.tsx`):
  кнопка `LuContrast` в шапке (десктоп + мобильный Box с гамбургером), состояние в localStorage
  (`archetest-high-contrast`), атрибут `data-contrast="high"` на `<html>`. Оверрайды в
  `theme/index.ts` (`globalCss`): `fg.muted`/`fg.subtle` → `fg`, границы → `border.emphasized`.
- **Icebreaker-карточка для психологов** (`icebreaker-card.tsx`): на `/express` перед стартом —
  «Вы психически здоровы?» + факт о когорте Данидина (~17%, Schaefer et al., 2017) с citable-
  источником подписью. Бейдж «Для психологов», brand-subtle фон.
- **Web Share API** (`share-result-button.tsx`, shared): на результатах полного квиза и экспресса.
  `navigator.share` на мобильных → нативный лист; фолбэк — копирование ссылки в буфер + тост.
  Экспресс делится ссылкой на полный тест (`fullTestUrl`).
- i18n: `common.share.{cta,copied,error}`, `express.icebreaker.*`, `express.shareText`,
  `quiz.shareText` (ru + en).

### Найдено и исправлено (в процессе)

- **`@layer`-ловушка контраста:** первая версия оверрайдов без `!important` не применялась —
  globalCss попадает в `@layer base`, а токены Chakra в `@layer tokens`, который в порядке
  `reset,base,tokens,recipes` идёт позже и выигрывает по каскадным слоям (специфичность роли
  не играет). Решение: `!important` на custom-property значениях (important бьёт нормальные
  объявления в любом слое). Диагностировано через inspect стилей в превью.

### Проверка (preview, ru)

- typecheck:tsgo — 0 ошибок; lint — 0 ошибок (предупреждения только в чужих prisma-скриптах).
- Тач-цели: варианты 56–57.6px, пропуск 56px; `horizontalScroll=0` на десктопе и 375px.
- Контраст: `data-contrast="high"` меняет `--chakra-colors-fg-muted` #A1A1AA→#FAFAFA,
  `--chakra-colors-border` #3F3F46→#52525B; переживает reload через localStorage.
- Оптимистичное выделение: клик по варианту → solid-blue фон < 150ms (до авто-перехода 400ms).
- Web Share: `navigator.share` отсутствует в headless → фолбэк без исключений, консоль чистая.
- Icebreaker виден на мобильном (375px), скриншоты десктоп-результатов + мобильного интро.

### Осталось P1 в спринте

- 5.7 (фест-инфраструктура: offline-PWA, лид-форма, kiosk-режим), 5.9 (геймификация 2.0).

---

## Сессия 2026-07-05 — Липкая CTA: системный фикс «кнопка под фолдом» (v0.11.0) 📌

> Обратная связь Kami: основная CTA уходит под фолд на длинных интро — воспроизводится
> во всех приложениях. Нужен не костыль, а системное решение в монорепо.

### Реализовано

- **Shared-примитивы в `@letar/ui` (v0.6.0 → 0.7.0):**
  - `StickyActionBar` (`libs/ui/src/lib/sticky-action-bar.tsx`) — липкая панель действия внизу
    (`position: sticky; bottom: 0`, `safe-area-inset-bottom`, тень+граница сверху).
  - `useScrollGate` (`libs/ui/src/lib/use-scroll-gate.ts`) — гейт «прочитай до конца»
    (IntersectionObserver + `sentinelRef`); `enabled: false` отключает гейт.
  - Экспорт из barrel, README-секции, попутно curly-фикс `build-version.tsx` (lint-блокер).
- **Применено в archetest:** `express-container.tsx` (интро экспресса) и `quiz-intro.tsx`
  (интро полного квиза) — кнопки вынесены в `StickyActionBar`, `useScrollGate` по согласию;
  контейнер `pt={16} pb={0}`, контентный VStack `pb={8}`, sentinel в конце.

### Проверка (preview)

- Десктоп + 375px: CTA видна без скролла (`visibleNoScroll: true`; на мобильном `btnBottom`
  800 из 812 с safe-area), `horizontalScroll=0`, disabled до согласия, enabled после
  согласия+скролла; консоль чистая. Скриншоты десктоп/мобильный.
- typecheck:tsgo + lint (ui и archetest) — 0 ошибок.

### Системная часть

- Паттерн + правила → `.claude/docs/ui-components.md` («Основная CTA не должна уходить под фолд»).
- Бродкаст api-change в agent-mail. Фоновая задача на раскатку по aboi/dsperevod/studio/
  driving-school и др. (запущена отдельной сессией). Коммит `4cec46f`.

---
