# Changelog — synth (Архив до 2026-08-09)

> Продолжение основного `CHANGELOG.md`. Версии: 0.1.0 — 0.7.0.
> Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/), версии по [Semantic Versioning](https://semver.org/).

---

## [0.7.0] — 2026-07-29

### Добавлено

- **Экспорт/импорт `.syx`-файлов** для FM-патчей — кнопки «↓ .syx» / «↑ .syx» в FM-панели,
  доступны без подключённого MIDI-железа. Экспорт — стандартный DX7 single-voice dump (открывается
  на любом DX7-совместимом железе/плагине). Импорт распознаёт и single-voice, и 32-голосый
  bulk-банк (для банка загружается первый голос) — `src/lib/patch/syx-file.ts`.

---

## [0.6.3] — 2026-07-09

### Добавлено

- **Панель «Железо»** (`hardware-panel.tsx`) поверх студии — зеркалит физическое расположение
  SMK-37 PRO: 4 линейных фейдера в ряд + 8 круглых энкодеров в ряд (`fader.tsx` — новый read-only
  виджет), с живыми подписями текущей функции и индикатором активного банка. Раньше нужные ручки
  были разбросаны по секциям OSC/Filter/Amp — не соответствовало физической раскладке контроллера
- **Банк 2 у энкодеров** — раньше оба банка энкодеров управляли ОДНИМ и тем же набором из 8
  параметров (различался только тип MIDI-сообщения, не смысл); теперь банк 2 — отдельный набор из
  8 параметров, которые раньше не крутились вообще ни одной физической ручкой: gain осцилляторов,
  огибающая фильтра (attack/release, отдельная от огибающей amp), decay/sustain amp, азимут и
  глубина пространственного HRTF-звука. Итого 4 фейдера × 2 банка + 8 энкодеров × 2 банка = 24
  доступных живых ручки вместо 12

### Исправлено

- Реальный MIDI-протокол физических контролов SMK-37 PRO расшифрован и переработан с нуля через
  несколько раундов диагностики прямо на живом железе — прошлые предположения оказались неверны:
  - **Фейдеры** (4 шт.) — CC, с переключением между **двумя банками** (кнопка fader bank на
    устройстве): банк 1 = CC 64-67, банк 2 = CC 68-71.
  - **Энкодеры** (8 шт.) — тоже два банка, но кодируются иначе, чем у фейдеров: банк 1 —
    Control Change на **фиксированном канале**, но **разным CC-номером на каждую крутилку**
    (48-55); банк 2 — Pitch Bend на **разных каналах** (0-7), CC-номер не задействован. Первая
    попытка перепутала канал и CC-номер местами для банка 1 (искала «канал = номер энкодера»,
    как в банке 2) — из 8 крутилок откликалась только одна.
  - В обоих банках значение **абсолютное** (0-127), не относительный шаг — раньше энкодеры
    декодировались как относительные тики (0-63 = +N, 64-127 = -N), что на границе 63/64
    переворачивало знак и гнало параметр в крайнее значение почти мгновенно («либо 100%, либо
    0%» — репорт с реального железа). Теперь `MidiInputManager.onEncoder` отдаёт абсолютную
    позицию напрямую.
  - Касание энкодера (не вращение) шлёт Note On/Off на канале 0, ноты 104-111 — раньше это
    проигрывалось как случайная высокая нота в текущем движке; теперь отфильтровывается.
- `midi-mapping.ts`: `applyEncoderDelta` → `applyEncoderValue(patch, index, raw, bank)` (прямое
  присваивание вместо накопления дельты, банк — обязательный параметр); диапазоны фейдеров
  attack/release синхронизированы с тем, как `Knob` в `ParamPanel` нормализует отображение (было
  `*2`/`*3`, стало `*10` — полный ход фейдера == полный ход ручки на экране, раньше ручка
  визуально не доходила до 100%)
- Контраст текста в тултипах `Knob` — семантические токены Chakra (`fg.DEFAULT`/`bg.surface`) не
  резолвились внутри `Portal` (текст сливался с фоном), заменены на жёсткие hex-цвета, как в
  остальных ad-hoc компонентах студии

---

## [0.6.1] — 2026-07-28

### Изменено

- Рефакторинг `studio-client.tsx` (812 → 616 строк): мастер-шина/реверб/пространство вынесены в `use-master-bus.ts`, запись — в `use-recording.ts`, WAV-рендер — в `use-wav-render.ts`, MIDI CC/энкодер-маппинг — в `src/lib/patch/midi-mapping.ts`. Поведение не менялось, проверено вручную (запись/рендер/авто-орбита без ошибок)

---

## [0.6.0] — 2026-07-28

### Добавлено

- Пространство/3D: `PannerNode` (HRTF) в мастер-шине SUB-движка — ручки азимут/глубина + авто-орбита (звук непрерывно обходит слушателя по кругу) (`src/lib/audio/spatial.ts`, секция «FX — Пространство»)

---

## [0.5.0] — 2026-07-28

### Добавлено

- Публикация патчей: кнопка «⇧» экспортирует сохранённый патч в JSON (`visibility: 'public'`) для ручного переноса в `patches/` (`src/lib/patch/publish.ts`)
- Витрина `/gallery`: список опубликованных патчей, прослушивание через офлайн-рендер прямо в браузере, shareable-URL (`#<id>` + копирование ссылки)

---

## [0.4.0] — 2026-07-28

### Добавлено

- Детерминированный рендер патча в WAV через `OfflineAudioContext` (`src/lib/audio/render.ts`) — кнопка «⇄ рендер WAV» в шапке студии, работает для всех трёх движков (SUB/FM/DRUM)

### Исправлено

- Типы движков (`SubtractiveEngine`, `DrumEngine`, `FmEngine`, `buildReverbIR`) ослаблены до `BaseAudioContext`, чтобы одинаково работать в живом `AudioContext` и офлайн-рендере

---

## [0.3.0] — 2026-07-28

### Добавлено

- Библиотека патчей в IndexedDB: сохранить/загрузить/удалить, отдельно по типу движка (`src/lib/storage/patches-db.ts`, компонент `PatchLibrary`)
- Живая запись с мастер-шины: кнопка «● запись»/«● стоп» + скачивание `.webm` (`src/lib/audio/recorder.ts`)

### Исправлено

- **Критично:** `CookieBanner` рендерился вне Chakra-провайдера в `layout.tsx` — приводило к падению всей страницы (`ContextError`) сразу после первого показа баннера. Перенесён внутрь `<Provider>`
- Добавлены недостающие семантические токены темы (`bg.panel`, `brand.*`), на которые жёстко ссылается общий `@letar/ui` `CookieBanner`

---

## [0.1.6] — 2026-07-08

### Добавлено

- Драм-движок (`src/lib/audio/drums.ts`): 16 пэдов, синтез 808/909-стиля без сэмплов — kick, tom (питч-свип синуса), snare (тон + band-pass шум), hat closed/open (6 square-осцилляторов в негармоничных соотношениях, классический 909-приём), clap (4 близких шумовых всплеска — «флэм»); velocity-sensitive
- Модель драм-кита `DRUM_KIT_1` («Breakbeat Kit 1»): 12 звучащих пэдов (2 кика, 2 снейра, клэп, 2 хэта разной длины, 3 тома, перк) + 4 пустых слота под будущие сэмплы
- Экранная сетка пэдов 4×4 в MPC-раскладке (`drum-pads.tsx`) — мышь, QWERTY (Z X C V / A S D F / Q W E R / 1 2 3 4), подсветка при ударе
- Редактор пэда (`drum-panel.tsx`): выбор типа + ручки pitch/decay/tone/level
- Третий движок DRUM в переключателе движков студии; входящие MIDI-ноты 36–51 маппятся на пэды 0–15 в режиме драм-кита

---

## [0.1.5] — 2026-07-08

### Известные ограничения

- **SMK-37 PRO не отвечает на dump request** (`encodeVoiceDumpRequest` / кнопка «Прочитать из железа») — подтверждено ручной проверкой (с зажатой и отпущенной кнопкой PATCH), устройство не шлёт в ответ ни одного MIDI-сообщения. Прошивка реализует только приём патчей, не полный DX7-редакторский протокол. Инфраструктура оставлена рабочей для другого DX7-совместимого железа; в кнопку добавлен поясняющий tooltip.

---

## [0.1.4] — 2026-07-08

### Добавлено

- Приём входящего SysEx в `MidiInputManager` (`onSysex`-колбэк — браузер отдаёт весь фрейм F0…F7 одним событием)
- `encodeVoiceDumpRequest` в `dx7-sysex.ts` — запрос дампа текущего голоса (voice edit buffer) с DX7-совместимого устройства
- Кнопка «Прочитать из железа» в FM-панели: шлёт дамп-запрос и декодирует ответ через `decodeSingleVoiceSysex`, обновляя текущий FM-патч

---

## [0.1.3] — 2026-07-08

### Добавлено

- MIDI output в `MidiInputManager` (`getOutputs`, `send`) — отправка сырых MIDI/SysEx-байт на подключённое устройство
- Кнопка «Отправить в железо» в FM-панели: кодирует текущий патч в DX7 SysEx и шлёт на SMK-37

---

## [0.1.2] — 2026-07-08

### Добавлено

- Конвертер модели FM-патча ↔ стандартный Yamaha DX7 SysEx (`src/lib/patch/dx7-sysex.ts`): single-voice dump (155 параметров) и разбор 32-голосого bulk dump, с checksum
- Тесты на реальных заводских пресетах SMK-37 PRO (`__fixtures__/`)
- `tsconfig.spec.json` для vitest (первые тесты в synth)

### Известные ограничения

- Формат SysEx подтверждён как полностью стандартный DX7, но наш движок реализует 5 собственных алгоритмов вместо всех 32 настоящих топологий — звук патча на реальном железе может отличаться от браузера (см. PLAN.md, Фаза 1.5)

---

## [0.1.1] — 2026-07-07

### Исправлено

- Билд студии падал с `Module not found: Can't resolve '@letar/ui'` — библиотека не была подключена по всем трём обязательным точкам монорепо (tsconfig paths/references, package.json implicitDependencies, next.config.js transpilePackages)

### Проверено

- Первое живое подключение реального M-VAVE SMK-37 PRO: MIDI-вход и звук подтверждены end-to-end

---

## [0.1.0] — 2026-06-15

### Добавлено

- Скаффолд Next.js 16 (App Router, порт 3022)
- Тёмная тема: пустота Малевича (`void`) + золото Климта (`gold`) + тёплый уголь (`forge`)
- Провайдер Chakra UI v3 с принудительной тёмной темой
- Базовый layout с Umami-аналитикой
- Заглушка главной страницы (Фаза 0)
- Структура папок: `patches/` (публичные), `tracks/` (публичные), `private/` (gitignored)
- `project.json` с Nx-таргетами (dev/build/start/lint/format/typecheck/test)
- Документация: `PLAN.md`, `PLAN_TESTING.md`, `PLAN_COMPLETED.md`, `JOURNAL.md` (приватный)
- Скилл `/synth` с ролью ментора
- `LICENSE` (код MIT, патчи CC0)
