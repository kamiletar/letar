# Changelog — synth

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/), версии по [Semantic Versioning](https://semver.org/).

---

## [0.22.1] — 2026-07-29

### Изменено

- **Дедуп фуллскрин-оверлея** — `use-fullscreen-overlay.ts` (`_components/`): Escape-закрытие +
  выход из настоящего Fullscreen API при закрытии + `handleFullscreen()` были побайтово
  одинаковы в `vj-overlay.tsx` и новом `teleprompter-overlay.tsx`. Оба переведены на общий хук,
  публичное поведение не изменилось.
- **Дедуп localStorage-хелпера** — `local-storage-string.ts` (`lib/patch/`): load/save одиночной
  строки с SSR-guard и try/catch дублировались между `pad-midi-map.ts` и новым
  `teleprompter-storage.ts`. Второе вхождение того же паттерна — вынесено, оба модуля теперь
  тонкая обёртка поверх общего хелпера.
- Проверено живьём в браузере: VJ и суфлёр открываются/закрываются как раньше; `nx test synth`
  зелёный.

## [0.22.0] — 2026-07-29

### Добавлено

- **Телесуфлёр для стихов** (последний открытый пункт Фазы 5) — полноэкранный оверлей, кнопка
  «📜 суфлёр» рядом с VJ-кнопкой в шапке студии. Пауза — режим правки текста, запуск — крупный
  автопрокручивающийся текст (5–150 px/с, ручка «скорость»), останавливается сам у конца. Текст
  и скорость переживают перезагрузку страницы (`localStorage`) — `src/lib/patch/teleprompter-storage.ts`,
  `use-teleprompter.ts`, `teleprompter-overlay.tsx`. Проверено живьём: ввод/сохранение текста,
  переключение пауза↔правка; саму анимацию прокрутки sandbox-превью не композитит (известный
  артефакт песочницы), механизм переиспользует уже проверенный владельцем приём авто-орбиты.

### Исправлено (документация)

- **Устаревшая запись техдолга про HRTF/Haas** — PLAN.md ошибочно утверждал, что пространство
  (HRTF-panner) и реверб применяются «только к SUB». Перечитан код `studio-client.tsx`: `FmEngine`
  и `DrumEngine` подключаются в конструкторе к тому же `masterGain`, что и `SubtractiveEngine`, —
  пространство и реверб уже общие для всех трёх движков. Реально не сделана только
  Haas-стереоширина (отдельный эффект, ещё не реализован ни для одного движка). Записи в PLAN.md
  поправлены, чтобы не сбивать с толку следующую сессию.

## [0.21.0] — 2026-07-29

### Добавлено

- **Выбор голоса из 32-голосого bulk-банка `.syx`** — импорт файла по-прежнему сразу применяет
  первый голос (как раньше), но теперь дополнительно открывает список всех 32 имён пресетов
  под кнопками импорта/экспорта в FM-панели; клик по любому — применяет именно его к текущему
  FM-патчу, список остаётся открытым для прощёлкивания нескольких голосов подряд, закрывается
  кнопкой «✕» — `src/lib/patch/syx-file.ts` (`SyxImportResult.voices`), `studio-client.tsx`,
  `fm-column.tsx`, `fm-hardware-controls.tsx`. Проверено живьём в браузере на реальном заводском
  банке (`__fixtures__/smk37-pro-presets-1.syx`): список 32 имён отрисовался, клик по «02. HARP2
  MOD1» сменил патч в шапке студии с ранее авто-загруженного «PIANO 1».

## [0.20.1] — 2026-07-29

### Изменено

- **Общий IndexedDB key-value хелпер** — `src/lib/storage/indexeddb-kv.ts` (`createKvStore`).
  `patches-db.ts` и `samples-db.ts` дублировали один и тот же boilerplate (открытие базы,
  `onupgradeneeded`, промисификация put/get/getAll/delete с одинаковой обработкой ошибок).
  Публичный API обоих модулей не изменился — чистый рефакторинг.

## [0.20.0] — 2026-07-29

### Добавлено

- **Сэмплы на драм-пэдах** — загрузка своего аудиофайла на любой пэд взамен синтезированного
  удара (`src/lib/storage/samples-db.ts`, `use-drum-samples.ts`, `drum-panel.tsx`).
  Собственный приватный IndexedDB-стор `synth-samples`: аудио-данные не входят в JSON патча,
  только `sampleId`-ссылка (`DrumPadSampleSchema` в `schema.ts`) — публикация в витрину не
  утаскивает за собой личные записи. Ручки gain/pitch(скорость воспроизведения) на сэмпле.
  Секвенсор, живой удар и офлайн-рендер WAV поддержаны одинаково.

## [0.19.2] — 2026-07-29

### Изменено

- **Аудио-констрейнты `getUserMedia` вынесены в общий хелпер** — `buildMusicalAudioConstraints()`
  (`src/lib/audio/media-constraints.ts`). Три места (`hardware-recorder.ts`,
  `external-audio-input.ts`, `use-voice-chain.ts`) дублировали один и тот же набор
  «выключить `echoCancellation`/`noiseSuppression`/`autoGainControl`» вместе с комментарием-
  объяснением. Теперь правило студийного захвата звука живёт в одном месте.

## [0.19.0] — 2026-07-29

### Добавлено

- **Вокальный тракт (начало Фазы 5)** — микрофон → компрессор/лимитер → 3-полосный EQ → де-эссер
  (band-split компрессия «с/ш») → send'ы в reverb/delay мастер-шины + монитор в наушники —
  `src/lib/audio/voice-chain.ts`, хук `use-voice-chain.ts`, UI `voice-panel.tsx` +
  `level-meter-bar.tsx`. Запись голоса поверх звучащего патча/бита в один файл — новый
  `MixRecorder` (`recorder.ts`), сводит несколько источников в `MediaStreamAudioDestinationNode`.
  Репетиция — включённый монитор + индикатор уровня (RMS/пик, красный при перегрузе), без
  отдельного «режима».

## [0.18.1] — 2026-07-29

### Изменено

- **VJ: кнопки сцен переведены на `filledToggleStyle`** — `vj-overlay.tsx` больше не задаёт
  active/inactive стиль кнопок сцен инлайновым тернарником, а использует общий хелпер
  `apps/synth/src/app/_components/studio/button-style.ts` (тот же паттерн, что у FM-алгоритмов,
  типов волны/фильтра, пэдов драм-машины). Визуально — единая цветовая схема toggle-кнопок вместо
  собственных хардкод-цветов.

## [0.18.0] — 2026-07-29

### Добавлено

- **VJ: визуальные «сцены» (продолжение Фазы 4)** — 5 куратoрских пресетов «настроения» графа
  (`src/app/_components/vj/scenes.ts`): Классика/Дрон/Брейккор/Бас-фокус/Минимал. Каждый —
  множители на существующую реактивность `SpinGraphCanvas` (скорость вращения, теснота орбиты,
  чувствительность к басу, яркость искр/свечения, скорость колец-метронома), не отдельный
  рендер-движок. Переключатель — ряд кнопок в левом верхнем углу `VjOverlay`.

## [0.17.0] — 2026-07-29

### Добавлено

- **VJ: beat/tempo-синхронизация (продолжение Фазы 4)** — `vjBeatRef` в `studio-client.tsx`
  растёт на каждый четвертной удар (каждый 4-й 16-й шаг) активного секвенсора — драм-кита
  (`use-drum-sequencer.ts`) или пиано-ролла SUB/FM (`use-piano-roll.ts` через
  `use-melodic-performance.ts`) — независимо от того, звучит ли на этом шаге реальная нота.
  `SpinGraphCanvas` рисует расширяющиеся от ядра к краю кольца-метроном на каждый бит — устойчивый
  визуальный пульс в темп BPM/транспорта секвенсора, отличимый от локальной вспышки узлов на
  удар ноты/пэда (`vjPulseRef`).

## [0.16.0] — 2026-07-29

### Добавлено

- **VJ: реактивность к живым ручкам патча + внешний аудиовход (продолжение Фазы 4)** —
  `SpinGraphCanvas` теперь читает те же `patchRef`/пульс, что и звук: azimuth/depth (реально
  панорамируют звук через HRTF) поворачивают и сжимают орбиту графа, cutoff подсвечивает рёбра,
  резонанс ускоряет вращение ядра — «один девайс для звука И картинки» без дублирующей
  MIDI-проводки. Удар ноты/пэда даёт мгновенную вспышку (`vjPulseRef`, без лишних ре-рендеров).
  Новый **переключаемый внешний аудиовход** (mic/line, `src/lib/audio/external-audio-input.ts` +
  `use-external-audio-input.ts`) — граф может реагировать на чужую музыку на вечеринке/фаершоу.

## [0.15.0] — 2026-07-29

### Добавлено

- **VJ-режим: реактивный спин-граф (начало Фазы 4)** — `src/lib/audio/analyser.ts`
  (`AnalyserNode`-tap от `masterGain`, разбор FFT на бас/середину/верха), `SpinGraphCanvas`
  (полноэкранный canvas: гексаграмма из двух невозможных треугольников Пенроуза в центре
  вращается в противоположные стороны и раздувается на басу; 6 внешних узлов — эхо 6 FM-операторов
  — держат орбиту, расширяющуюся с числом одновременно звучащих нот; рёбра ярче на середине,
  «искры» бегут по ним на верхах), `VjOverlay` (полноэкранный оверлей, Esc/крестик — выход, кнопка
  Fullscreen API — для проектора/второго экрана). Кнопка «◈ VJ» в шапке студии — доступна после
  старта звука. Монохромная палитра золото/пустота — глубина/пространство важнее цвета.

## [0.14.0] — 2026-07-29

### Добавлено

- **Помощник по гармонии (закрытие Фазы 3)** — `src/lib/patch/harmony.ts`: 6 ладов с ear-first
  ярлыком-метафорой как основным именем («Тёмный, тревожный», «Светлый, открытый», …) и
  музыкальным именем вторично («натуральный минор», «мажор», …). Диатонические трезвучия каждой
  ступени лада выводятся стеканием терций структурно (один алгоритм на любой 7-тоновый лад — тот
  же приём, что уже применён к 32 алгоритмам DX7), а не хардкодом на каждое имя. Качество аккорда
  тоже подписано метафорой («светлый»/«тёмный»/«напряжённый, готов сорваться»/«зависший,
  неустойчивый»).
- **`HarmonyPanel`** (`harmony-panel.tsx` + `use-harmony.ts`) — своя пара у SUB и FM (независимые
  тональный центр/лад). Кнопка «▶ услышать лад» проигрывает лад по ноте вверх на октаву; клик по
  аккорду ступени одновременно звучит (`noteOn`/`noteOff` в обход секвенсора) и ложится в
  пиано-ролл блоком на 4 шага — курсор вставки сам едет дальше по кругу паттерна. `usePianoRoll`
  получил новый метод `insertChord` (вычищает перекрывающийся диапазон шагов перед вставкой) и
  прямой проброс `noteOn`/`noteOff` для аудирования.
- **Подсветка нот лада в пиано-ролле** — `PianoRollPanel` принимает `scalePitchClasses`, тонирует
  пустые ячейки строк, входящих в текущий лад (без нового «активного» состояния — отличимо от
  заполненных золотом и от playhead).

## [0.13.0] — 2026-07-29

### Добавлено

- **Пиано-ролл для SUB/FM (Фаза 3)** — ноты с высотой и длительностью (в отличие от булевой
  драм-сетки): `MelodicSequenceSchema` (`notes[]`, `steps`, `bpm`, `swing`), опциональное поле
  `sequence` на `SubtractiveEngineSchema`/`FmEngineSchema`. Планирование через новый
  `MelodicSequencer` (`src/lib/audio/melodic-sequencer.ts`), переиспользующий `StepSequencer` и
  триггерящий те же `noteOn`/`noteOff`, что и клавиатура/MIDI — во время воспроизведения
  подсвечиваются те же клавиши. Клик по ячейке `piano-roll-panel.tsx`: пусто → нота 1 шаг,
  повторные клики удлиняют (1→2→4), на максимуме — удаляют. Ряды без подписей нот («цвет вместо
  нот» — ориентир по светлым/тёмным полосам чёрных/белых клавиш, как на самой клавиатуре).
- **Арпеджиатор** — `ArpeggiatorSchema` (`mode: up/down/up-down/random`, `stepsPerNote`, `octaves`,
  `gate`, `bpm`), опциональное поле `arpeggiator` на обоих движках. Новый класс `Arpeggiator`
  (`src/lib/audio/arpeggiator.ts`) держит зажатые ноты и раскладывает их в бегущую
  последовательность через тот же `StepSequencer`. Включённый арпеджиатор перехватывает
  клавиатуру/MIDI на уровне `handleNoteOn`/`handleNoteOff` в `studio-client.tsx` — нота становится
  частью держимого аккорда вместо прямого звука.
- **Свинг** — `StepSequencer` теперь смещает нечётные 16-е доли на настраиваемую долю периода
  (`setSwing`), и умеет паттерн произвольной длины (8–32 шага), не только фиксированные 16.
  Поле `swing` добавлено также в драм-секвенсор (`SequencerPatternSchema`) — старые паттерны без
  него читаются как свинг 0.

### Технические детали

- Точность note-on/off для SUB/FM в обоих новых модулях не sample-accurate (в отличие от
  драм-триггеров с `AudioScheduledSourceNode.start(when)`) — используется `setTimeout` с
  задержкой, вычисленной от lookahead-времени шага. Тот же компромисс, на который уже пошёл
  FM-движок (postMessage в AudioWorklet). Для мелодических нот с собственной ADSR-атакой
  погрешность (~100мс) не слышна.

## [0.12.1] — 2026-07-29

### Добавлено

- **Сохранение паттерна секвенсора вместе с драм-патчем** — `DrumkitEngineSchema` дополнена
  опциональным полем `sequence: { pattern, bpm }`. `useDrumSequencer` больше не хранит паттерн
  в отдельном React state, а читает/пишет его прямо в `drumPatch.engine.sequence` — сохранение
  и загрузка идут через уже существующий `PatchLibrary`/IndexedDB, отдельного стораджа не
  потребовалось. Старые патчи без поля `sequence` грузятся с пустой сеткой и BPM 120 (обратная
  совместимость через `.optional()`).

## [0.12.0] — 2026-07-29

### Добавлено

- **Степ-секвенсор драм-кита (Фаза 3)** — 16 пэдов × 16 шагов, `▶ игра`/`■ стоп`, BPM (40–240).
  Планирование через lookahead-scheduler по аудио-часам (`src/lib/audio/sequencer.ts`,
  `StepSequencer`) — без джиттера JS-таймера. `DrumEngine.trigger()` теперь принимает опциональное
  время старта (`when?`) для sample-accurate планирования.

### Известные ограничения (закрыто в 0.12.1)

- ~~Паттерн секвенсора не сохраняется вместе с патчем — живёт только в памяти открытой вкладки.~~

## [0.11.0] — 2026-07-29

### Добавлено

- **MCP-сервер для Claude Desktop (Фаза 2)** — `apps/synth/src/mcp/`, `nx run synth:mcp:serve`,
  зарегистрирован в `.mcp.json` как `synth-mcp`. Два набора инструментов:
  - **Ментор:** `highlight_param`, `dim_all`, `focus_section`, `play_demo` (куратoрские демо-патчи
    `reese-bass`/`glass-bells`/`breakbeat-kit-1`) — реагируют в студии золотой подсветкой/подсказкой.
  - **DAW:** `load_patch`, `send_midi_sequence`, `generate_chord_pattern`.
  - 3 ресурса: `synth://current-patch`, `synth://patches`, `daw://current-state`.
- **SSE-канал ментора** — `/api/mentor/events/` (браузер подписан), `/api/mentor/emit/` (публикует
  MCP-сервер), `/api/mentor/state/` (heartbeat состояния студии). Опциональный токен
  `SYNTH_MENTOR_TOKEN` для публичного деплоя.
- `MentorOverlay` и `MentorFocusZone` — визуальная реакция студии на события ментора.

### Изменено

- **Рефакторинг `studio-client.tsx`** — логика интеграции с MCP-ментором (загрузка патчей/MIDI-
  последовательностей, heartbeat состояния студии) вынесена в отдельный хук `use-studio-mentor.ts`
  по образцу соседних `use-*.ts` хуков компонента. Поведение не менялось.

## [0.10.0] — 2026-07-29

### Добавлено

- **MIDI Learn для пэдов драм-кита** — SMK-37 PRO (как и большинство контроллеров) не имеет
  фиксированной note/CC-карты для 16 пэдов, раскладка живёт в пресетах прошивки. Кнопка
  «MIDI Learn: пэды» включает режим обучения: клик по экранному пэду → следующий удар по железу
  назначается на него. Карта хранится в `localStorage`, переживает переключение движка/страницы —
  `src/lib/patch/pad-midi-map.ts`, `use-pad-midi-learn.ts`.
- **MIDI-монитор** — диагностическая панель с сырыми входящими MIDI-сообщениями (hex + разбор
  типа/канала), скрыта по умолчанию, разворачивается кнопкой рядом со статусом MIDI. Нужна, чтобы
  выяснить, шлют ли недокументированные кнопки устройства (ARP/NOTE REPEAT/SCALE/CHORD/GLOBE/
  BT/PATCH/PARA/FX/SEQ/SEQ PLAY/SEQ REC) что-то на хост вообще — community SysEx-карта их не
  описывает — `src/lib/audio/midi-decode.ts`, `use-midi-monitor.ts`, `midi-monitor.tsx`.

---

## [0.9.0] — 2026-07-29

### Добавлено

- **Все 32 настоящих алгоритма DX7** вместо 5 собственных приближений — движок (`fm-processor.js`)
  и FM-панель теперь поддерживают полный набор графов модуляции, сверенных по независимым
  источникам. Feedback (0-7) применяется к правильному оператору для каждого алгоритма (не всегда
  op0 — алгоритмы 4 и 6 берут обратную связь от соседнего оператора). SysEx-кодирование обновлено
  соответственно — критично для звуковой точности при передаче патчей на реальное DX7-железо.

### Изменено

- Дефолтные FM-патчи (`FM Bass`, `Glass Bells`) перестроены под настоящую топологию своих
  алгоритмов — будут звучать иначе, чем раньше (ожидаемо: раньше использовались приближения).

---

## [0.8.0] — 2026-07-29

### Добавлено

- **Запись аппаратного звука через USB-audio input** — панель «Запись аппаратного звука» пишет
  реальный сигнал с внешнего устройства (например, встроенный DX7-движок SMK-37 через его
  USB-audio выход) через `getUserMedia`/`MediaRecorder`, независимо от MIDI-подключения и от
  браузерного движка. Голосовые фичи входа (`echoCancellation`/`noiseSuppression`/
  `autoGainControl`) явно выключены, чтобы не портить музыкальный сигнал.

---

Продолжение в ./CHANGELOG_2026_08_09.md (версии 0.1.0 — 0.7.0)
