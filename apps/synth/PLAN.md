# PLAN — synth (музыкальная студия)

> Это **не типовое CRUD-приложение монорепо**: нет ZenStack/Postgres в v1, ядро — Web Audio / Web MIDI / синтез звука. Стандартный шаблон `/create:new-app` применяем выборочно (см. «Отличия от шаблона монорепо»).

## 🎯 Видение

Личная браузерная студия для **синтеза, исполнения и записи звука**, построенная вокруг аппаратной клавиатуры **M-VAVE SMK-37 PRO** (встроенный FM-движок класса DX7). Плюс **публичная open-source витрина** — но публикация **поштучно и только по желанию автора** (см. «Приватность и публикация»).

Пользователь — телесный, потоковый музыкант: фаер-перформер (пои), хочет виджеить. Поэтому студия — не только про звук, но и про **живой перформанс**: реактивные визуалы выносятся в полноэкранный **VJ-режим** (Фаза 4), реактивный к звуку и MIDI. Принцип всего интерфейса: простота и поток, никакой сложности «платного VJ-софта». Дальняя цель — **единое сольное выступление**: свои биты + стихи в микрофон + визуалы на проекторе + танец с поями (огонь). Все части проектируем так, чтобы однажды срослись в один live.

Двойная роль ассистента: **архитектор** (технически) + **музыкальный ментор** (ненавязчиво учит языку музыки через метафоры и ощущения, ведёт дневник успехов `JOURNAL.md`).

**Профиль пользователя:** веб/нэйтив-архитектор, тончайший слух и вкус; цвет в звуке **не видит**, но остро чувствует «правду/фальшь» звука и **слышит пространство/объём** («сцену»); **не знает нот и сольфеджио** — работает на слух и на глаз. Эстетика: «живой» звук Nord (virtual analog) + стеклянные тембры DX-7 (FM). Подробности — в `claude.md`.

---

## 🎨 Эстетика и философия

Концептуальный север: **«звук становится геометрией и пространством»** (не цветом — пользователь НЕ видит цвет в звуке, но остро слышит **объём/«сцену»** и чувствует «правду» звука).

Его картина мира — петлевая квантовая гравитация: всё из **геометрии пространства**, спиновой пены и сетей графов (узлы, рёбра; вихри-узлы из фотонов скручивают граф-пространство → масса; бегущие волны = фотоны; гравволны меняют длину рёбер).

> Совпадение, на котором держится визуал: **FM-движок сам по себе граф** — операторы это узлы, «алгоритм» это рёбра (кто кого модулирует). Структура звука, его картина мира и визуал — одна форма: сеть.

- **Палитра:** тёмная пустота (Малевич / космос) + светящееся вихревое движение (Ван Гог / фотоны) + золотой орнамент-мерцание (Климт) + кишащая сюрреал-деталь (Босх).
- **Визуал = реактивный спин-граф:** узлы пульсируют от нот, рёбра растягиваются (как гравволны меняют длину рёбер), бас скручивает вихри-узлы, волны бегут по рёбрам — **глубина/пространство важнее цвета**.
- **Мотив/лого:** **Звезда Давида из двух невозможных треугольников Пенроуза** (невозможный гексаграм) — Пенроуз же и автор спиновых сетей, двойная связь с его ТВ; ветвление многомирия как паттерн для VJ-сцен.
- **Тон:** студия — поток и тело, не таблицы; допустим космический юмор в духе его гонзо-sci-fi (в copy и пустых состояниях).
- **Визуал рождает сюжет:** абстракция, в которую зритель проецирует эмоцию и историю (визуализация WMP однажды без слов вызвала у него «диалог мужчины и женщины»). Два узла + ребро = «диалог» — отношения видны прямо на языке графа. Не случайные эффекты — оставляем место для смысла.

## 🎼 Музыкальный профиль и звуковые цели

Звук делаем не абстрактный, а **его**. Опора — слух (ловит «правду/фальшь»), пространство и вкус.

**Играет (на слух):** варган, диджериду, шаманский бубен (→ обертоны/дроны), дарбука, джембе, индийские таблы (→ грув/полиритм), фоно, гитара, флейта, труба (→ мелодия/дыхание). Эти инструменты — **готовые мостики к теории**: учим синтез через то, что руки уже знают.

**Жанры:** Drum and Bass, Breakbeat, Breakcore, Chillrave, IDM, LoFi Chill & Lounge, Hardcore.

**Ориентиры (для саунд-дизайна):** Йоко Канно, Таку Ивасаки, OST Death Note, **Ясунори Мицуда (Chrono Cross)**, Бетховен; The Chemical Brothers, Metrik, Blaerg, Carbon Based Lifeforms, Dead Can Dance, Juno Reactor, Beats Antique, Hol Baumann, Atlantida Project, **Йоши Хирокава** (за пространство/«сцену»).

**Любимый звук:** раскатистый бас.

### Звуковые приоритеты и первые патчи

1. **Раскатистый бас №1** — Reese/sub/growl (DnB). **Первый «геройский» патч**, с него начинаем.
2. **Дроны/обертоны** — эволюционирующие гармонически богатые текстуры (его диджериду/варган на слух).
3. **Органик-эмоциональный фьюжн** (à la Chrono Cross / Beats Antique) — струнные/лид + мировая перкуссия + пространство.
4. **Брейкбит/драм** — нарезка брейков, breakcore-ритмы (перкуссионный бэкграунд).
5. **Колокольчики / электропиано DX-7** — стеклянный FM.

Сквозные ценности: **раскатистый бас + объём/«сцена» (3D-звук) + полный динамический диапазон** (от LoFi-чилла до Hardcore) + **«правда» тембра** (фальшь — кринж, поэтому свой движок и придирчивая настройка).

### Сетап и устройства

- Сейчас: второй экран, наушники, ноутбук.
- Хочет (гик): Korg перкуссионный MIDI-пэд, микрофон (для Фазы 5), камера Canon EOS-R (вебкам для motion-входа / съёмка перформансов).
- → **MIDI-вход устройство-агностичный**: SMK-37 + Korg + любой class-compliant; не хардкодим одну клавиатуру.

---

## ✅ Принятые решения

| # | Решение            | Значение                                                                                                   |
| - | ------------------ | ---------------------------------------------------------------------------------------------------------- |
| 1 | Имя приложения     | **`synth`** (`@letar/synth`, папка `apps/synth`)                                                           |
| 2 | Видимость кода     | **Публичный** app в `letar` — код и движок open-source, чтобы паттерны были копируемы                      |
| 3 | Приватность музыки | **Патчи/треки приватны по умолчанию** (локально, IndexedDB). Публикация в витрину — поштучно, по клику     |
| 4 | Хранилище          | local-first (IndexedDB) → публикуемые патчи как JSON-файлы в `patches/` (Git). Postgres/ZenStack — позже   |
| 5 | FM-движок          | **Свой 6-операторный AudioWorklet** (учебно + чистый MIT). Эталон точности — само железо + опц. WASM-Dexed |
| 6 | Старт              | Браузерное ядро; железо (SysEx) — Фаза 1.5; MCP — Фаза 2; FL Studio — Фаза 6 (в конце очереди)             |

**Осталось определить:** свободный порт 3xxx (на этапе скаффолда).

---

## 🔒 Приватность и публикация (важно)

Студия — это **твоя закрытая комната**. Витрина — **дверь, которую открываешь только ты и только когда захочешь**.

- **По умолчанию ничего не публикуется.** Патчи и записи сохраняются локально (IndexedDB браузера) — физически не покидают машину.
- **Публикация — отдельное осознанное действие** на конкретный патч: только тогда его JSON попадает в публичный `patches/` (и в Git). Один патч за раз, в любой момент можно снять с публикации.
- **Open-source-миссия не требует обнажать личное.** «Паттерны, которые копируют другие» = код, синт-движок, формат патчей и патчи-шаблоны — а не обязательно твои личные треки. Витрина может стартовать пустой или с парой демо-патчей.
- **`JOURNAL.md` — приватный по умолчанию** (можно держать в `.gitignore` или приватно). Это безопасное пространство проб и ошибок.

> Технически: студия пишет в IndexedDB; кнопка «Опубликовать» экспортирует патч в `patches/<id>.json`. Публичная `/gallery` читает только содержимое `patches/`.

### Раскладка папок на диске

```
apps/synth/
├── .gitignore        # игнорирует private/
├── private/          # 🔒 НЕ коммитится — «закрытая комната»
│   ├── tracks/       #    сырые записи (WAV), черновики
│   └── patches/      #    черновики патчей
├── tracks/           # ✅ отслеживается — опубликованные треки
└── patches/          # ✅ отслеживается — опубликованные патчи (читает /gallery)
```

Рабочий цикл: пишешь/творишь → файл лежит в `private/…` (локально) → когда готов, **переносишь** в отслеживаемую папку (`private/tracks → tracks`, `private/patches → patches`) и коммитишь. Ничего не уходит наружу без твоего переноса.

---

## 🎹 Аппаратная база — M-VAVE SMK-37 PRO

| Параметр    | Значение                                                               |
| ----------- | ---------------------------------------------------------------------- |
| Клавиши     | 37, velocity-sensitive, 3 октавы                                       |
| Пэды        | 16 RGB backlit, velocity + aftertouch                                  |
| Энкодеры    | 8 × 360° бесконечных                                                   |
| Фейдеры     | 4 назначаемых → 8 через fader bank                                     |
| Дисплей     | OLED                                                                   |
| **Звук**    | **встроенный 6-операторный FM-движок, совместим с патчами Yamaha DX7** |
| Секвенсор   | 8 паттернов, режимы key/live/drum, до 64 шагов, swing/gate/tempo       |
| Прочее      | арпеджиатор, note repeat                                               |
| **Аудио**   | **3.5 мм выход (наушники/линия) + режим USB-аудиоинтерфейса**          |
| Подключение | USB-C + Bluetooth Wireless MIDI (Win/Mac/iOS/Android)                  |
| Патчи       | **загрузка по SysEx, в т.ч. с веб-страниц** с FM-коллекциями           |
| Редактор    | M-Vave MidiSuite (требует подключённого устройства)                    |

**Следствия для архитектуры:**

- Устройство = **источник звука И USB-аудиоинтерфейс** → аппаратный FM-голос пишется в браузер через `getUserMedia`.
- **Один FM-патч (формат DX7)** живёт в трёх местах: браузерный AudioWorklet (дизайн где угодно) · аппаратный движок (заливка по Web MIDI SysEx) · экспорт `.syx` (откроется на любом DX7-совместимом железе/плагине — Dexed, Volca FM, реальный DX7).
- Наличие реального железа даёт нам **эталон DX7-звука бесплатно** — можно A/B-сравнивать свой браузерный FM-движок с аппаратным и подтягивать точность.
- Субтрактивные (Nord-style) патчи аппаратного аналога не имеют → живут только в браузере + витрине (JSON + отрендеренное аудио).

⚠️ Точную SysEx-карту устройства (запрос дампа патча, управление LED пэдов, текст OLED, CC энкодеров/фейдеров) **подтвердить** по community-докам (`jonathaslacerda/smk-37-pro-docs`) и мануалу — задача Фазы 1.5.

---

## 🏗️ Архитектура (browser-first, слоями)

Нижние слои не зависят от верхних. Keystone — **модель патча**.

```
┌─ Фаза 1 (браузерная студия) ───────────────────────────────┐
│ 1. Модель патча (JSON, портативная)                         │
│    ├─ type: 'fm'         → маппинг в/из DX7 SysEx (155 байт) │
│    └─ type: 'subtractive'→ осцилляторы→фильтр→amp, LFO, ADSR │
│ 2. Аудио-движок (Web Audio; ядро DSP в AudioWorklet)        │
│    ├─ FM: 6 операторов, 32 алгоритма, EG на оператор         │
│    ├─ Subtractive: осц. + фильтр (cutoff/резонанс) + ADSR    │
│    └─ Drums: 16 пэдов → синт-перкуссия 808/909 + сэмплы      │
│ 3. Ввод (Web MIDI, sysex:true): клавиши/пэды/энкодеры/фейдеры│
│ 4. Реактивный UI (60fps, rAF + Canvas/SVG): клавиатура,      │
│    ручки; реактивный спин-граф (глубина/пространство)        │
│ 5. Запись: MediaRecorder (живая) + OfflineAudioContext (рендер)│
│ 6. Витрина /gallery (публичная): только опубликованные патчи │
│    + экспорт .syx + shareable-URL (состояние в #hash)        │
│ + IndexedDB (приватное хранилище) + JOURNAL.md (приватный)   │
└─────────────────────────────────────────────────────────────┘
┌─ Фаза 1.5 (мост к железу) ─────────────────────────────────┐
│ 7. Web MIDI SysEx ↔ аппаратный FM-движок (залить/прочитать  │
│    DX7-патч); запись аппаратного звука через USB-audio in     │
└─────────────────────────────────────────────────────────────┘
┌─ Фаза 2 (MCP-сервер для Claude Desktop) ───────────────────┐
│ 8. JSON-RPC 2.0 (stdio/SSE): ресурс daw://current-state,    │
│    инструменты send_midi_sequence / generate_chord_pattern   │
└─────────────────────────────────────────────────────────────┘
┌─ Фаза 6 (мост к FL Studio, в конце очереди) ────────────────┐
│ 9. device_synth.py (Python в FL Studio) ↔ loopMIDI ↔        │
│    отдельный Node-процесс ↔ Next (WS/SSE): BPM/Play-Stop/трек │
└─────────────────────────────────────────────────────────────┘
```

### Транспорт (важно)

- Браузерное ядро (Фазы 1–1.5) **не требует своего сервера** — Web MIDI и Web Audio событийны в клиенте, 60fps через `requestAnimationFrame`. Кастомный сервер Next **не поднимаем**.
- Фаза 6 (FL Studio, в конце очереди): нативные MIDI-либы (node-gyp) и связь с FL Studio изолированы в **отдельном Node-процессе** (свой Nx-таргет), общается с Next по WS/SSE.

---

## 🎛️ Открытый формат патча (сердце витрины)

```jsonc
{
  "schemaVersion": 1,
  "id": "glass-bells-01",
  "name": "Стеклянные колокольчики",
  "author": "kami",
  "visibility": "private", // 'private' (по умолчанию) | 'public'
  "license": "CC0-1.0", // патчи — public domain / CC; код — MIT
  "type": "fm", // 'fm' | 'subtractive' | 'drumkit'
  "tags": ["bells", "dx7", "bright"],
  "createdAt": "2026-06-15",
  "engine": {
    // type:'fm' — параметры, изоморфные DX7-голосу (6 операторов,
    //   алгоритм 1–32, feedback, EG/levels, keyboard scaling, LFO, ...)
    // type:'subtractive' — осцилляторы, фильтр(cutoff,res), ADSR, LFO, fx
  },
  "render": { "previewWav": "glass-bells-01.preview.wav" }, // опц.
  "color": "#7cc8ff" // цвет-метка патча для витрины (UI-тег, не из синестезии)
}
```

- **FM-патчи** дополнительно экспортируются в стандартный `.syx` (DX7 single-voice: `F0 43 0n 00 01 1B …155 байт… checksum F7`; bulk-банк = 32 голоса × 128-байт пакеты = 4104 байт). Совместимы с любым DX7-движком и заливаемы в железо.
- Хранилище: IndexedDB (приватно) → публикуемые в `apps/synth/patches/*.json` (Git).
- Shareable-URL: всё состояние патча кодируется в `#hash` → ссылка открывает звук без бэкенда.
- **Драм-киты** — родственный артефакт (набор из 16 пэдов × звук), тот же JSON + витрина (`type: 'drumkit'`).

---

## 🧰 Технологический стек

| Слой            | Технология                                                                    |
| --------------- | ----------------------------------------------------------------------------- |
| Синтез          | **Web Audio API**, ядро DSP в **AudioWorklet** (sample-accurate); опц. WASM   |
| Ввод            | **Web MIDI API** (`requestMIDIAccess({ sysex: true })`)                       |
| Запись          | **MediaRecorder** (живая) + **OfflineAudioContext** (детерминированный → WAV) |
| Аппаратный звук | устройство как **USB-audio input** → `getUserMedia` → запись                  |
| Хранилище       | IndexedDB (приватно) + файлы `patches/` (публично)                            |
| Фронт           | Next.js 16 (App Router), React 19, **Chakra UI v3** (+ токены/recipes)        |
| Формы           | **@letar/forms** + Zod v4 (метаданные патчей)                                 |
| Графика         | Canvas/SVG, 60fps                                                             |
| Фаза 2          | MCP SDK (JSON-RPC 2.0, stdio/SSE)                                             |
| Фаза 6          | Python (FL Studio MIDI Scripting API), loopMIDI, Node-процесс (WS/SSE)        |

**Совместимость браузеров:** Web MIDI — только Chromium (Chrome/Edge 79+, Opera) и Firefox (с add-on). **Safari/iOS не поддерживают.** Chrome 124+ — весь Web MIDI за разрешением; SysEx требует `sysex: true`. → Целевой браузер: **Chrome/Edge на десктопе**. Витрина-просмотр (без MIDI) работает везде.

**Латентность:** потолок браузера на Windows — нет ASIO (≈10–30 мс). Серьёзный низколатентный трекинг — задача Фазы 6 (FL Studio + ASIO).

---

## 📅 Фазы разработки

### Фаза 0 — Скаффолд

✅ Закрыта полностью — перенесена в `PLAN_COMPLETED.md` (перенос 2026-08-09).

### Фаза 1 — Браузерный звук (MVP «услышать первый звук»)

- [x] **Модель патча** (JSON-схема + Zod) — `src/lib/patch/schema.ts`; FM + subtractive + drumkit; утверждена
- [x] **Субтрактив-движок** (native Web Audio, без AudioWorklet): 2 осц → фильтр → amp; ADSR; LFO (cutoff/pitch/amp); 8-голосая полифония + voice stealing — `src/lib/audio/subtractive.ts`
- [x] **AudioWorklet FM**: 6 операторов, 5 алгоритмов, DX7-EG (4 rate + 4 level) на оператор, feedback, 8-голосая полифония — `public/worklets/fm-processor.js` + `src/lib/audio/fm.ts`
- [x] Переключение движков (SUB/FM) в шапке студии; ленивое создание FM при первом переходе
- [x] **Первый «геройский» патч — раскатистый бас (Reese):** 2 расстроенные пилы (+7 центов) → фильтр LP с LFO-дыханием — `src/lib/patch/defaults.ts`
- [x] **Драм-движок**: 16 пэдов → синтезируемая перкуссия (808/909-style: kick/snare/hat/clap/tom), velocity-sensitive — `src/lib/audio/drums.ts`; опц. загрузка сэмплов — не сделано, пока только синтез
- [x] **Драм-режим UI**: экранная сетка пэдов 4×4 (MPC-раскладка), подсветка при ударе, редактор пэда (тип + pitch/decay/tone/level) — `src/app/_components/studio/drum-pads.tsx`, `drum-panel.tsx`; первый кит `Breakbeat Kit 1` — `src/lib/patch/drum-defaults.ts` (12 звучащих пэдов + 4 пустых под сэмплы); драм-киты тоже шарятся в витрине — задел есть (тип `drumkit` в `PatchSchema`), сама витрина ещё не сделана
- [x] **Web MIDI вход** (устройство-агностично: SMK-37 + Korg перк-пэд + любой class-compliant): клавиши → ноты (velocity), полифония, octave-сдвиг
- [x] **Маппинг фейдеров/энкодеров SMK-37 → параметры SUB-патча** — реальный протокол расшифрован живой диагностикой на железе (2026-07-09): 4 фейдера × 2 банка (CC 64-67 / 68-71) + 8 энкодеров × 2 банка (банк 1 — фиксированный канал, CC 48-55; банк 2 — Pitch Bend, канал = номер энкодера), все значения абсолютные — `src/lib/patch/midi-mapping.ts`. Панель «Железо» зеркалит физическую раскладку (4 фейдера + 8 энкодеров в ряд) — `hardware-panel.tsx`. FM/DRUM живой контроль ручками — открытый пункт Фазы 1.5
- [x] **Виртуальная клавиатура + ручки**: 2 октавы (C2–B3), мышь + QWERTY (A..;); SVG-кнобы с золотой дугой; подсветка нажатых клавиш — `src/app/_components/studio/`
- [x] **Режим ментора (тултипы)**: у каждой ручки — `Tooltip.Root` с физической метафорой; хинты в `src/lib/patch/hints.ts`
- [x] **Реверберация (мастер-шина)**: ConvolverNode с синтетическим IR; wet/decay ручки в FX-секции; CC 91 (GM) → wet; IR строится асинхронно без блокировки старта
- [x] **Пространство/3D-сцена**: бинаурал через PannerNode (HRTF) в мастер-шине, между общим `masterGain` и dry/reverb-раздачей — ручки азимут/глубина + авто-орбита (звук непрерывно обходит слушателя по кругу через `requestAnimationFrame`) — `src/lib/audio/spatial.ts`, секция «FX — Пространство» в `param-panel.tsx`. `masterGain` один на все три движка (`FmEngine`/`DrumEngine` подключаются в конструкторе к тому же узлу, что и `SubtractiveEngine` — `studio-client.tsx`), поэтому HRTF-пространство и реверб уже звучат одинаково на SUB/FM/DRUM, хоть ручки FX и читаются только из SUB-патча (см. комментарий в `use-master-bus.ts`). ⚠️ Уточнение 2026-07-29: старая формулировка этого пункта ошибочно утверждала «сейчас только SUB» — перепроверено чтением кода, это не так. Не сделана только **Haas-стереоширина** (отдельный эффект искусственного расширения стерео, ещё не реализован ни для одного движка).
- [ ] **Surround-мечта (позже):** multichannel-вывод Web Audio на 5.1+ мониторов вокруг зала — иммерсивный 3D-звук с сюжетом, движущийся по кругу
- [x] **Запись**: live (MediaRecorder) с мастер-шины, кнопка запись/стоп + скачивание `.webm` — `src/lib/audio/recorder.ts`
- [x] **Детерминированный рендер**: OfflineAudioContext → WAV-экспорт конкретного патча (SUB/FM/DRUM) — `src/lib/audio/render.ts`, кнопка «⇄ рендер WAV» в шапке студии
- [x] **IndexedDB**: приватное сохранение патчей (сохранить/загрузить/удалить, отдельно по типу движка) — `src/lib/storage/patches-db.ts`, `PatchLibrary`
- [x] **Публикация**: кнопка «⇧» у сохранённого патча — экспорт JSON (`visibility: 'public'`) для ручного переноса в `patches/` — `src/lib/patch/publish.ts`
- [x] **Витрина /gallery**: список опубликованных патчей (читает `patches/*.json` на сервере), прослушивание через офлайн-рендер, shareable-URL (`#<id>` + кнопка «поделиться» в буфер обмена) — `src/app/gallery/page.tsx`, `GalleryClient`
- [x] **Сэмплы на драм-пэдах** — альтернатива синтезируемым ударам: загрузка аудиофайла на
      пэд (кнопка «↑ сэмпл» в `drum-panel.tsx`), декодирование через `decodeAudioData`,
      приватное хранение в отдельном IndexedDB-сторе `synth-samples` (`samples-db.ts`) — само
      аудио никогда не попадает в JSON патча, только `sampleId`-ссылка (`DrumPadSampleSchema`
      в `schema.ts`). Пэд звучит либо сэмплом, либо синтезом — назначение одного гасит другое.
      `DrumEngine.trigger()` принимает теперь весь пэд (не только `synth`), кэширует
      декодированные буферы по `sampleId`. Собственные ручки gain/pitch(playbackRate) сэмпла.
      Секвенсор, живой удар и офлайн-рендер (`render.ts`) поддержаны одинаково. **Известное
      ограничение (сознательное, не баг):** сэмплы приватны — при публикации патча в витрину
      `patches/*.json` ссылка на `sampleId` уходит в JSON, но само аудио не публикуется
      (никогда не покидает браузер владельца), поэтому у чужих посетителей витрины такие пэды
      будут молчать, пока не сохранена личная привычная запись, — синтезируемые пэды того же
      кита звучат как обычно. Проверено живьём (браузерный upload-flow + IndexedDB round-trip + очистка).
- 🎵 _Термины: ADSR (огибающая) · оператор/несущая-модулятор · cutoff (занавеска яркости) · резонанс · velocity (сила удара → громче/ярче) · transient (резкий «щелчок» атаки удара) · пространство/реверберация (объём и «сцена»)._

### Фаза 1.5 — Мост к железу (SysEx + аппаратная запись)

- [x] Подтвердить SysEx-карту SMK-37 PRO — стандартный Yamaha DX7 формат бит-в-бит (заголовки, checksum, single/bulk raскладка), сверено с реальными пресетами устройства и `sysex-format.txt`
- [x] **MIDI Learn для пэдов**: раз пэды/кнопки/фейдеры **не имеют фиксированной CC-карты** (переназначаются в MidiSuite, хранятся в 4 пресетах на устройстве), вместо жёсткого маппинга сделан обучаемый — кнопка «MIDI Learn: пэды» в драм-режиме, клик по экранному пэду + удар по железу назначает ноту, карта в `localStorage` — `src/lib/patch/pad-midi-map.ts`, `use-pad-midi-learn.ts`. Дефолт остался прежний (нота 36 = пэд 0, GM-стандарт), учить нужно только если пресет на железе другой
- [x] **MIDI-монитор** (диагностика недокументированных кнопок ARP/NOTE REPEAT/SCALE/CHORD/GLOBE/BT/PATCH/PARA/FX/SEQ/SEQ PLAY/SEQ REC/RESET, см. фото владельца 2026-07-29) — панель сырых входящих MIDI-сообщений, скрыта по умолчанию, разворачивается кнопкой рядом со статусом MIDI — `src/lib/audio/midi-decode.ts`, `use-midi-monitor.ts`, `midi-monitor.tsx`. Community SysEx-карта эти кнопки не описывает вообще (`SYSEX.md` репозитория `smk-37-pro-docs` — всего 7 строк, только ссылки на банки пресетов), похоже на меню внутренней прошивки (правка патча/секвенсора на OLED), а не MIDI-исходящие команды — **требует проверки живым железом**: открыть `localhost:3022` в браузере владельца, подключить MIDI, развернуть монитор, по очереди нажать каждую кнопку с фото и посмотреть, появится ли строка. Если что-то придёт — домаппить/включить в обучение; если тихо — кнопка не для хоста, фиксируем в доках и закрываем пункт
- [x] Конвертер «модель патча (fm) ↔ DX7 SysEx (155 байт)» + checksum — `src/lib/patch/dx7-sysex.ts`, round-trip протестирован на реальном заводском банке пресетов (`__fixtures__/`)
  - ⚠️ Известное ограничение: наш движок реализует 5 собственных алгоритмов-приближений, не все 32 настоящих топологии DX7 — точность звука при передаче на железо для поля `algorithm` пока не гарантирована (см. заметку в коде и техдолг ниже)
- [x] **Залить патч в железо** по Web MIDI SysEx — `MidiInputManager.send()` (MIDI output) + кнопка «Отправить в железо» в FM-панели студии
  - ✅ Проверено на реальном SMK-37 PRO (2026-07-08): SysEx долетает и корректно разбирается устройством — «узнаваемая родня, но детали другие» (владелец), что подтверждает: инфраструктура работает, расхождение локализовано именно в топологии алгоритма (см. техдолг ниже), а не в передаче параметров
  - ⚠️ Нюанс железа: аудиовыход SMK-37 включается только когда **зажата/залатчена кнопка PATCH** — иначе клавиши/пэды шлют MIDI, но звука на выходе нет (не связано с нашим кодом)
- [x] **Прочитать** патч с железа (запрос дампа + приём входящего SysEx) — `MidiInputManager.onSysex` + `encodeVoiceDumpRequest` + кнопка «Прочитать из железа» в FM-панели
  - ⚠️ **Аппаратное ограничение SMK-37 PRO (подтверждено 2026-07-08):** устройство не отвечает на dump request (`F0 43 2n 00 F7`) вообще никаким MIDI-сообщением — ни с зажатой, ни с отпущенной кнопкой PATCH. Документация сообщества (`smk-37-pro-docs/sysex/SYSEX.md`) описывает только заводские банки пресетов, протокол запроса-ответа нигде не задокументирован. Вывод: прошивка реализует только приём патчей (одностороннее SysEx), не полный DX7-редакторский протокол. Код инфраструктуры (`onSysex`, `encodeVoiceDumpRequest`, кнопка) оставлен рабочим — пригодится для другого DX7-совместимого железа (реальный DX7, Volca FM, Dexed через виртуальный MIDI)
- [x] **Экспорт/импорт `.syx`** — кнопки «↓ .syx» (скачать текущий FM-патч как single-voice DX7 dump) / «↑ .syx» (загрузить с диска) в FM-панели, не требуют подключённого MIDI-железа — `src/lib/patch/syx-file.ts`, `syx-file.spec.ts`. Импорт распознаёт и 32-голосый bulk-банк — сразу применяет первый голос и открывает список всех 32 имён для выбора другого (`fm-hardware-controls.tsx`)
- [x] **Запись аппаратного звука через USB-audio input** — SMK-37 в режиме USB-audio-интерфейса
      виден браузеру как обычный микрофон; панель «Запись аппаратного звука» (независима от MIDI и от
      нашего AudioContext) даёт выбрать конкретное аудиоустройство и записать через `getUserMedia` +
      `MediaRecorder` (`echoCancellation`/`noiseSuppression`/`autoGainControl` явно выключены — они для
      голосовой связи и портят музыкальный сигнал) — `src/lib/audio/hardware-recorder.ts`,
      `use-hardware-recording.ts`, `hardware-recording-panel.tsx`. Реальная проверка на живом железе —
      за владельцем (сэндбокс-превью блокирует доступ к микрофону/аудиовходу)
- [ ] (Если поддерживается) RGB пэдов / текст OLED через SysEx — **исследовано 2026-07-29:** `SYSEX.md` репозитория `smk-37-pro-docs` не документирует ничего, кроме заводских FM-банков (7 строк, только ссылки на 4 файла пресетов); ни LED, ни OLED протокол нигде не описаны community. Слепое реверс-инжиниринг SysEx на реальном железе рискует зависанием/некорректным состоянием устройства без гарантии результата — не делаем вслепую. Если появится желание — тот же MIDI-монитор (см. пункт выше) можно использовать для наблюдения, шлёт ли MidiSuite что-то по SysEx при смене цвета пэда в редакторе (собственный трафик приложения, не наш), это единственный практический путь разведки без документации
- [x] **Техдолг закрыт: все 32 настоящих алгоритма DX7** — `fm-processor.js` больше не использует
      5 собственных приближений. Графы модуляции (`src`/`carriers`/`fbOp` на каждый алгоритм) сверены по
      двум независимым источникам (`mmontag/dx7-synth-js` + инвариант из `Dexed/Source/AlgoDisplay.cpp`),
      вынесены в `src/lib/patch/dx7-algorithms.ts` (используется UI и SysEx-кодированием) и продублированы
      в AudioWorklet (импорт модулей туда невозможен). Обработка операторов теперь фиксированным порядком
      6→1 без явной топологической сортировки под каждый алгоритм — работает для всех 32 благодаря
      структурному инварианту DX7 (модулятор оператора i всегда имеет индекс ≥ i, кроме ровно одного
      feedback-ребра). Feedback (0-7) теперь применяется к правильному оператору для каждого алгоритма,
      а не всегда к op0 — важно для алгоритмов 4 и 6, где обратная связь идёт не строго на себя, а
      на соседний оператор. Round-trip через SysEx и рендер всех 32 алгоритмов покрыты тестами.
- 🎵 _Термины: SysEx как «язык» патчей · алгоритм FM (схема связи операторов)._

### Фаза 2 — MCP-сервер для Claude Desktop

✅ Закрыта полностью — перенесена в `PLAN_COMPLETED.md` (перенос 2026-08-09). Единственный
непроверенный хвост (вызов из настоящего Claude Desktop, не curl-эмуляции) — см. «Открытые хвосты»
ниже.

### Фаза 3 — Композиция и аранжировка

✅ Закрыта полностью — перенесена в `PLAN_COMPLETED.md` (перенос 2026-08-09).

### Фаза 4 — Перформанс / VJ-режим (живые визуалы)

> Пользователь крутит пои и устраивает фаершоу, хочет виджеить, но платный VJ-софт оказался слишком сложным. Цель — **простой** web-VJ.

- [x] **Полноэкранный визуальный движок (Canvas), реактивный к аудио** — `AnalyserNode`-tap от
      мастер-шины (`src/lib/audio/analyser.ts`, разбор FFT на бас/середину/верха), реактивный
      спин-граф (`SpinGraphCanvas`): гексаграмма из двух невозможных треугольников Пенроуза
      в центре (лого studio) + 6 внешних узлов-«операторов» на орбите; `VjOverlay` — полноэкранный
      оверлей с Fullscreen API, кнопка «◈ VJ» в шапке студии — `src/app/_components/vj/`.
      Реактивность к MIDI (ноты/пэды/энкодеры напрямую, не только через число активных нот) и
      к **внешнему аудиовходу** (mic/line — для чужой музыки на вечеринке) — не сделано, открытый
      пункт этой же вехи
- [x] **Реактивность визуала к живым ручкам патча + внешний аудиовход** —
      `SpinGraphCanvas` читает `patchRef` (тот же ref, что уже используют аудио-коллбэки студии):
      azimuth/depth (реально управляют HRTF-панорамой звука — `use-master-bus.ts`) поворачивают и
      сжимают орбиту графа, cutoff подмешивается в яркость рёбер, резонанс — в скорость вращения
      ядра. `pulseRef` (счётчик в `studio-client.tsx`, растёт на каждую ноту/удар пэда, без
      `setState`) даёт резкую вспышку графа синхронно с атакой — раньше, чем FFT её подхватит.
      **Внешний аудиовход** (mic/line) — `src/lib/audio/external-audio-input.ts`
      (`ExternalAudioInput`: `getUserMedia` → `MediaStreamAudioSourceNode` → свой `AnalyserNode`,
      никуда дальше не подключается — не пишет и не подмешивает чужой звук в наши колонки) +
      `use-external-audio-input.ts` — переключатель источника прямо в `VjOverlay` («🎤 внешний
      вход»/выбор устройства), для реакции на чужую музыку на вечеринке/фаершоу без своего звука.
      Энкодеры/фейдеры отдельного маппинга на визуал не получили — переиспользуется уже живое
      состояние патча, а не дублируется MIDI-проводка
- [x] **Beat/tempo-синхронизация** — `vjBeatRef` растёт на каждый четвертной удар (каждый 4-й
      16-й шаг) активного секвенсора (драм-кит/пиано-ролл SUB/FM), независимо от того, звучит ли
      на нём реальная нота — прокинуто через `use-drum-sequencer.ts`/`use-piano-roll.ts`/
      `use-melodic-performance.ts`. `SpinGraphCanvas` рисует расширяющиеся от ядра кольца-метроном
      на каждый бит (`beatRings`) — устойчивый визуальный пульс в темп BPM, отличимый от локальной
      вспышки узлов на удар ноты/пэда (`vjPulseRef`/`pulseRef`). Несколько секвенсоров с разным BPM
      сознательно не разруливаются отдельно — «бит» это общий счётчик от любого из них, работает
      корректно для типового сценария (играет один секвенсор за раз)
- [x] **Визуальные «сцены»** — 5 куратoрских пресетов множителей реактивности `SpinGraphCanvas`
      (`src/app/_components/vj/scenes.ts`): «Классика» (нейтральный контроль), «Дрон» (медленно,
      мягкое свечение — под варган/диджериду), «Брейккор» (быстро, искры на пределе — DnB/breakcore),
      «Бас-фокус» (ядро дышит только на баса — под Reese-бас), «Минимал» (тихо — не отвлекает от
      голоса/текста на сцене). Не отдельные рендер-движки — тот же граф, другое «настроение»:
      скорость вращения/теснота орбиты/чувствительность к басу/яркость искр и свечения/скорость
      колец-метронома. Переключатель — ряд кнопок в левом верхнем углу `VjOverlay`, `sceneRef`
      читается прямо в rAF-цикле (без пересоздания эффекта на переключение, тот же паттерн, что
      `patchRef`/`pulseRef`/`beatRef`). **Публикация сцен в витрину (JSON, как патчи звука) —
      сознательно не сделана в этой вехе** — куратoрский список поставляется с кодом, не редактируется
      пользователем; полноценный конструктор с сохранением/шарингом — отдельная, более крупная задача,
      если понадобится
- [ ] Вывод на второй экран/проектор — Fullscreen API уже есть в `VjOverlay` (кнопка «⛶»), не
      проверено на реальном втором дисплее/проекторе (за владельцем)
- [ ] Дизайн-принцип: предельная простота и поток (антипод «сложного платного софта»)
- 🎵 _Дальний горизонт: motion-вход (веб-камера) — движение тела рулит звуком/визуалом (телесное восприятие → управление)._

### Фаза 5 — Голос / спокен-ворд (и репетиция сцены)

> Пользователь — поэт; на сцене паникует, мешает скачущая громкость голоса в микрофоне. Цель — приручить голос и снять страх через репетицию в реальных условиях.

- [x] **Вокальный тракт** — микрофон → highpass(рокот) → де-эссер(band-split компрессия «с/ш») →
      компрессор/лимитер (порог+сила) → 3-полосный EQ (низ/середина/верх) → send'ы в reverb/delay
      общей мастер-шины + монитор в наушники — `src/lib/audio/voice-chain.ts` (чистый граф узлов,
      `applyVoiceChainParams` для живого твика без пересборки — тот же приём, что `useMasterBus`),
      `use-voice-chain.ts` (React-хук: устройства/старт-стоп/параметры/уровень/запись),
      `voice-panel.tsx` + `level-meter-bar.tsx` (UI). Reverb-send подключается к уже существующему
      `convolver` мастер-шины (`useMasterBus`) — голос звучит в той же «комнате», что инструмент,
      не в отдельной
- [x] **Запись голоса поверх патчей/битов** — `MixRecorder` (`src/lib/audio/recorder.ts`, новый
      класс рядом с `MasterRecorder`): несколько источников (`masterGain` инструмента + сухой выход
      вокального тракта) сводятся в один `MediaStreamAudioDestinationNode` — Web Audio сам
      микширует. Кнопка «● записать голос + бит» в `VoicePanel`, отдельная от инструментальной
      записи в шапке студии (не мешают друг другу)
- [x] **Репетиция** — не отдельный режим, а само включённое состояние тракта: монитор всегда
      слышен в наушниках сразу (без задержки записи), плюс `LevelMeterBar` (RMS-заливка + пиковая
      метка, красный при перегрузе) — видно, не «зашкаливает» ли голос, ещё до выхода на сцену
- [x] **Телесуфлёр для текста стихов** — полноэкранный оверлей (тот же Fullscreen API, что у
      VJ-режима), кнопка «📜 суфлёр» в шапке студии, открывается независимо от вокального тракта.
      Пауза = режим правки (textarea с текстом), запуск = крупный (40px) автопрокручивающийся
      текст; прокрутка остановится сама у конца текста. Текст и скорость (5–150 px/с)
      переживают перезагрузку страницы через `localStorage` (не IndexedDB — суфлёр не хранит
      историю версий, один активный текст зараз) — `src/lib/patch/teleprompter-storage.ts`,
      `use-teleprompter.ts`, `teleprompter-overlay.tsx`. Прокрутка — прямая запись в `scrollTop`
      внутри `requestAnimationFrame` (тот же приём, что авто-орбита мастер-шины), не React-state
      на каждый кадр. Проверено живьём в браузере: ввод текста → сохранение в `localStorage` →
      пауза/запуск переключают edit/scroll-режимы корректно. Саму анимацию прокрутки в
      sandbox-превью визуально не увидеть — здесь `requestAnimationFrame` не тикает, пока вкладка
      не скомпозитится (тот же известный артефакт песочницы, что и раньше, не баг кода) —
      механизм идентичен уже проверенной владельцем авто-орбите, отдельная живая проверка именно
      скорости прокрутки — на усмотрение владельца
- ⚠️ Не проверено живым микрофоном (sandbox-превью блокирует `getUserMedia`, задокументированное
  ограничение — см. правило «тестировать в браузере владельца» выше) — UI корректно показал
  «✗ Permission denied» без падения, дальше нужна проверка на реальном голосе
- 🎵 _Термины: компрессия (выравнивание громкости) · gain-staging (уровни без перегруза)._

### Фаза 6 — Мост к FL Studio (pro-студия)

> Отодвинуто в конец очереди (2026-07-29, по просьбе владельца) — сперва осваиваем всё в браузере, FL Studio не горит.

- [ ] `device_synth.py` (FL Studio MIDI Scripting): BPM/Play-Stop/активный трек
- [ ] loopMIDI ↔ отдельный Node-процесс (Nx-таргет)
- [ ] Node ↔ Next по WS/SSE; индикатор состояния DAW
- [ ] Низколатентный трекинг через ASIO
- 🎵 _Термины: BPM/темп · транспорт (play/stop/loop)._

---

## 🎓 Программа обучения (ненавязчиво, 1–2 термина за веху)

Принцип **ear-first / eye-first**: сначала ощущение и цвет/форма, потом — название. Каждый термин — метафорой, демонстрируется в звуке, записывается в `JOURNAL.md`.

Дорожная карта терминов (см. 🎵 в фазах): тембр → ADSR → оператор/несущая/модулятор → cutoff/резонанс → velocity/transient → пространство/реверберация → spatialization (HRTF) → SysEx/алгоритм → BPM/транспорт → аккорд/интервал → лад/тональность/гамма/арпеджио → компрессия/gain-staging.

**Мостики из его инструментов:** диджериду/варган/бубен → обертоны и дроны (FM, фильтр); дарбука/джембе/таблы → грув и драм; труба/флейта → атака/огибающая; фоно → гармония. Учим теорию через то, что тело уже знает.

### `JOURNAL.md` — формат записи

```markdown
## 2026-06-15 — Первый колокольчик

**Создано:** патч `glass-bells-01` (FM, алгоритм 5).
**Услышал:** как feedback оператора добавляет «песок» в атаку.
**Новый термин:** оператор — генератор-синус; несущая звучит, модулятор её «трясёт».
**Метафора:** модулятор — рука, что быстро дёргает струну несущей.
```

---

## 🧪 Тестирование (детали — в `PLAN_TESTING.md`)

- Unit (Vitest): конвертер DX7 SysEx ↔ модель патча (round-trip + checksum), кодирование shareable-URL, парсинг MIDI.
- Аудио: снимок параметров AudioWorklet; рендер через OfflineAudioContext → проверка длительности/RMS.
- E2E (Playwright): витрина, открытие патча по URL, воспроизведение (mock Web Audio/MIDI).
- Ручное: реальный SMK-37 PRO — вход MIDI, заливка SysEx, запись аппаратного звука.

### ⚠️ Правило: тестировать в браузере владельца, не во встроенном превью

Встроенный sandbox-превью (agent preview tool) физически не видит USB-устройства — Web MIDI там
недоступен в принципе. Для любой фичи, завязанной на реальное железо (MIDI-вход, энкодеры/фейдеры,
SysEx), просить владельца открыть `localhost:3022` **в его собственном браузере** (Chrome/Edge/Brave)
и тестировать там — не пытаться воспроизвести/проверить через `preview_*` тулы. Одно и то же
окружение на всех шагах диагностики: не переключаться между sandbox-превью и браузером владельца
в рамках одной сессии отладки — это даёт разные результаты (разные разрешения MIDI, разное
состояние подключённых устройств) и путает диагностику.

---

## ⚠️ Риски и ограничения

- Web MIDI — нет Safari/iOS; нужен Chrome/Edge + разрешение (sysex). Витрина-просмотр кроссбраузерна.
- SysEx-карта SMK-37 PRO не задокументирована официально — нужна разведка; управление LED/OLED может не поддерживаться.
- Латентность браузера (нет ASIO) — низколатентный трекинг на Фазу 2.
- node-gyp/нативные MIDI-либы — только в изолированном Node-процессе Фазы 6 (FL Studio).
- Точность браузерного FM vs реальный DX7 — при необходимости WASM-ядро Dexed (внимание: лицензия GPL).

---

## 🧹 Техдолг (мелкий, не блокирующий)

- **Инлайновые style-объекты для кнопок вместо `button-style.ts`** — `apps/synth/src/app/_components/studio/button-style.ts`
  уже абстрагирует формы кнопок (`filledToggleStyle`/`outlineButtonStyle`), используется в `HardwareRecordingPanel` и
  других панелях. Но `studio-client.tsx` (шапка: SUB/FM/DRUM, «● запись», «⇄ рендер WAV», «◈ VJ») и
  `vj-overlay.tsx` (своя константа `buttonStyle` для некнопок-тумблеров: fullscreen/close/внешний вход) продолжают
  писать инлайновые `style={{...}}` объекты. При следующем touch этих файлов — перевести на общие хелперы вместо
  копирования. ✅ Кнопки VJ-сцен (Классика/Дрон/Брейккор/Бас-фокус/Минимал) в `vj-overlay.tsx` уже переведены на
  `filledToggleStyle` (2026-07-29).

---

## 🔀 Отличия от шаблона монорепо (`/create:new-app`)

- **Нет ZenStack/Postgres/миграций** в v1 — патчи это файлы + IndexedDB; БД только при появлении аккаунтов.
- **Нет стандартного CRUD-PLAN** — план вокруг звуковых/музыкальных вех, не сущностей.
- **Скилл `.claude/commands/synth.md` расширен** ролью музыкального ментора + правилом обновлять `JOURNAL.md` (термин за веху).
- Добавлены артефакты: `JOURNAL.md`, `patches/`, `LICENSE` (MIT код / CC0 патчи), IndexedDB-слой.
- Деплой/бэкапы/РКН — по мере необходимости (витрина статична; ПД не собираем в v1).

---

## 📚 Источники исследования

- [M-VAVE SMK-37 Pro — Synthtopia: built-in Yamaha DX7 clone](https://www.synthtopia.com/content/2025/06/13/m-vave-smk-37-pro-usb-midi-controller-features-built-in-yamaha-dx7-clone/)
- [Synth Anatomy — обзор и демо SMK-37 Pro (6-op FM)](https://synthanatomy.com/2025/07/m-vave-smk-37-pro-this-100e-usb-midi-controller-features-a-built-in-dx-7-synthesizer-engine.html)
- [heise online — тест SMK-37 Pro (аудиовыход, USB-audio)](https://www.heise.de/en/background/MIDI-keyboard-with-DX7-emulation-M-VAVE-SMK-37-Pro-tested-10503740.html)
- [M-VAVE SMK-37 Pro — User Manual (PDF)](https://www.danguitar.dk/pub/assets/MVA-SMK37.pdf)
- [Community docs: jonathaslacerda/smk-37-pro-docs](https://github.com/jonathaslacerda/smk-37-pro-docs)
- [DX7 SysEx format — Dexed (asb2m10)](https://github.com/asb2m10/dexed/blob/master/Documentation/sysex-format.txt)
- [Ted's Yamaha DX7 Page (формат голоса)](http://tedfelix.com/yamaha-dx7/index.html)
- [webaudiomodules/webdx7 — AudioWorklet + WASM DX7](https://github.com/webaudiomodules/webdx7)
- [mmontag/dx7-synth-js — JS DX7 (Web Audio + Web MIDI)](https://github.com/mmontag/dx7-synth-js)
- [FL Studio MIDI Controller Scripting — Python API](https://il-group.github.io/FL-Studio-API-Stubs/midi_controller_scripting/)
- [Web MIDI — разрешение и SysEx (Chrome Developers)](https://developer.chrome.com/blog/web-midi-permission-prompt)
- [Web MIDI API — поддержка браузеров (caniuse)](https://caniuse.com/midi)

---

**Статус (2026-06-15, конец сессии планирования):** план, `claude.md` и структура папок готовы; хэндофф агенту подготовлен. Все решения закрыты, кроме порта. Итоги — в `PLAN_COMPLETED.md`. Следующий шаг — отправить агента на Фазу 0 (скаффолд) → утверждение JSON-схемы модели патча (keystone) → Фаза 1 (первый звук = раскатистый бас).

**Статус (2026-07-29, конец сессии вокального тракта):** Фазы 0, 1, 1.5, 2, 3 закрыты; Фаза 4
закрыта по коду (спин-граф, реактивность к патчу/пульсу, внешний аудиовход, beat-синхронизация,
5 визуальных сцен); Фаза 5 начата — вокальный тракт (компрессор/EQ/де-эссер/send'ы/монитор) и
запись голоса поверх бита готовы.

**Открытые хвосты (все — «за владельцем» или сознательно отложены):**

1. 🎤 **Проверить вокальный тракт на реальном микрофоне** — sandbox блокирует `getUserMedia`,
   код проверен только на корректной обработке отказа. Самый приоритетный хвост: без живой
   проверки неизвестно, не хрустит ли компрессор и не режет ли де-эссер лишнее.
2. 🎹 **MIDI-монитор на живом железе** — нажать по очереди недокументированные кнопки SMK-37
   (ARP/NOTE REPEAT/SCALE/CHORD/GLOBE/BT/PATCH/PARA/FX/SEQ/…) и посмотреть, шлют ли они
   что-нибудь хосту. Если тихо — фиксируем в доках и закрываем.
3. 📽 **Fullscreen VJ на реальном проекторе/втором экране** — код есть, живой проверки нет.
4. 🔌 **Настоящий Claude Desktop → `synth-mcp`** — curl эмулирует HTTP-запрос, но не сам процесс.
5. 💡 RGB пэдов / OLED по SysEx — без документации, вслепую не лезем.

**Возможные следующие шаги по коду:** телесуфлёр для стихов (последний пункт Фазы 5) · загрузка
сэмплов в драм-пэды (задел есть, 4 пустых пэда в `Breakbeat Kit 1`) · Haas-стереоширина (мастер-шина,
единственное реально отсутствующее из связки «HRTF/Haas», см. правку пункта Фазы 1 выше).

✅ **Выбор голоса из 32-голосого bulk-банка `.syx` в UI** — сделано 2026-07-29 (см. CHANGELOG
0.21.0): импорт bulk-файла показывает список всех имён пресетов, клик применяет выбранный.
