# === DATABASE ===
# Сгенерируй пароль: openssl rand -base64 24
DB_PASSWORD=CHANGE_ME

# === UMAMI CONFIG ===
# Сгенерируй секрет: openssl rand -base64 32
APP_SECRET=CHANGE_ME

# Имя скрипта трекера (нейтральное для обхода adblock)
TRACKER_SCRIPT_NAME=script
