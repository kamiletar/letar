# PLAN_TESTING — Animatrona IPFS Player

## Ручная проверка

- [ ] `nx dev animatrona-ipfs-player` — окно открывается, интерфейс реагирует на клики
- [ ] `nx build:win animatrona-ipfs-player` — `dist/*.exe` собирается без ошибок
- [ ] Установленный `.exe` реально запускается и работает (GUI Electron не проверяется
      в сендбоксе Claude Code — см. `.claude/rules/electron.md` § «Грабли»)
