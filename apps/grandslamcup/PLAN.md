# Кубок Большого Слэма (grandslamcup) — ТЗ

## Фаза 7 — Стабильность счетовода + Оффлайн (v3.30.0+)

> Источник: инцидент 2026-04-14, матч СПб — таймер ушёл в минус, интерфейс завис.

### Группа A — Экстренные исправления

1. ~~**Таймер зависал: кнопка завершения не появлялась при овертайме**~~ ✅ v3.30.0 — кнопка «Выступление окончено» теперь показывается всегда при `isOvertime`, не зависит от `timer.isRunning`. При работающем таймере + овертайм лейбл меняется на «⚠ Форс-завершить».

2. ~~**isPending зависал навсегда**~~ ✅ v3.30.0 — авто-сброс через 8 секунд + сообщение об ошибке.

3. ~~**Классический режим `?mode=classic` удалён**~~ ✅ v3.30.0 — устранён источник случайного «Завершить матч» без подтверждения.

### Группа B — Улучшения ввода времени

4. ~~**Ручной ввод времени при овертайме**~~ ✅ v3.30.0 — кнопка «✏ Своё время» в диалоге, поле ввода М:СС.

### Группа C — Оффлайн

5. ~~**Автоматическая предзагрузка оффлайн-данных**~~ ✅ v3.30.0 — `OfflineStatusBar` сохраняет снэпшот при монтировании.

6. ~~**Оптимистичный СТОП таймера**~~ ✅ v3.30.0 — мгновенное обновление UI при нажатии СТОП, оффлайн-очередь `STOP_TIMER`.

7. ~~**Операции STOP_TIMER, END_PERFORMANCE в sync API**~~ ✅ v3.30.0.

---

## Фаза 6 — Обратная связь 2026-04-10 (v3.22.0+)

> Источник: заметки пользователя, `C:\Users\Kami\Desktop\КБС.md`, после прогона матча в СПб.

### Группа A — Критические production-баги

1. ~~**404 на `/admin/matches/[id]/edit`**~~ ✅ v3.23.0 — удалена ссылка «Редактировать матч» из desktop- и mobile-вариантов таблицы `matches-client.tsx`. Редактирование оценок остаётся через `EditScoresButton` на детальной странице. Полноценный edit-роут — в скоупе Группы C.

2. ~~**Страница поэта в админке падает**~~ ✅ v3.23.0 — убрано `pendingUserId: true` из `include` в `admin/players/[id]/page.tsx:37`. Это скалярное поле, а не relation.

3. ~~**Протокол матча вешает ошибку**~~ ✅ v3.23.0 — в Next.js 16 Server Component не может иметь inline `onClick`. Кнопка печати вынесена в client-компонент `protocol/_components/print-button.tsx`. Добавлен `protocol/error.tsx`.

4. ~~**Модал «Карточка» появляется за пределами экрана**~~ ✅ v3.23.0 — `card-dialog.tsx` переписан с устаревшей `<DialogRoot><DialogContent>` на compound `Dialog.Root → Portal → Dialog.Backdrop → Dialog.Positioner → Dialog.Content`.

5. ~~**Push-уведомления: «не удалось подписаться»**~~ ✅ v3.23.0 — добавлены `res.ok` проверки на клиенте (`push-subscribe-button.tsx`), серверный `route.ts` обёрнут в try/catch с осмысленными ошибками. При ошибке сервера клиент откатывает браузерную подписку.

6. ~~**Неверные статусы матчей 1 марта**~~ ✅ v3.23.0 — `getDisplayStatus` существовала в `match-status.ts`, но **нигде не использовалась в UI**. Заменено на `getDisplayStatus(match)` в 6 местах: `matches-client.tsx` (desktop+mobile), `match-hero-admin.tsx`, `(public)/matches/[id]`, `(public)/[citySlug]/matches/[id]`, `match-card.tsx`, `opengraph-image.tsx`, `coach/matches/page.tsx`. Локальные дубли `STATUS_LABEL`/`STATUS_COLOR` удалены в пользу общих `matchStatusLabels`/`matchStatusColors`.

### Группа B — UX счетовода / ведущего / тренера

7. ~~**Личные кабинеты счетовода и ведущего в меню пользователя**~~ ✅ v3.24.0 — `/api/auth/me` возвращает `isScorer`/`isPresenter`, `UserMenu` показывает пункты «Кабинет счетовода» / «Кабинет ведущего» только для назначенных. Созданы страницы `/my/scorer-matches` и `/my/presenter-matches` с секциями LIVE/предстоящие/прошедшие.

8. ~~**Полный ручной контроль счетовода**~~ ✅ v3.24.0 — добавлены 2 server action: `forceCompleteVotingAction` (завершение голосования с неполным жюри, гибкий подсчёт adjusted) и `updatePerformanceScoresAction` (редактирование оценок уже подсчитанного выступления + пересчёт счёта матча). UI в `vote-panel.tsx`: кнопка «Завершить с неполным жюри» во время фаз голосования + `ScoreEditorDialog` (✏ рядом с каждым выступлением в истории).

9. ~~**Быстрый ввод оценок — кликабельные блоки 1-5**~~ ✅ v3.24.0 — новый компонент `scorer-vote-input.tsx`: 5 колонок по одному на судью, в каждой блоки 1-5 с цветом судьи (`JUDGE_COLORS`). Использует существующий `enterManualVoteAction`. Колонка блокируется когда судья проголосовал. Показывается в `vote-panel.tsx` во время TEXT_VOTING/DELIVERY_VOTING.

10. ~~**Большой таймер ведущего на весь экран**~~ ✅ v3.24.0 — новый компонент `fullscreen-timer.tsx`: `position:fixed inset:0 bg:black`, цифры через `clamp(6rem, 30vw, 40rem)`, обратный отсчёт 3:00 → 0:00 → «+» для превышения. Крупные кнопки старт/стоп/сброс. Кнопка «Таймер на весь экран» в `presenter-client.tsx`, выход по ESC или кнопке.

11. ~~**Ведущий умеет работать без скорера**~~ ❌ **отменено 2026-04-10** — Скорер всегда присутствует на матче. У ведущего узкоспециализированный интерфейс только для него самого (таймер, жеребьёвка, отвод судей, подсказки залу). Скорер может работать один (ведущий подаёт знаки аналогово) или оба с интерфейсами — тогда часть ведущего автоматизируется. Улучшения интерфейса ведущего пойдут отдельной задачей, не через дублирование функционала скорера.

12. ~~**Счетовод может заявить состав за команду**~~ ✅ v3.24.0 — новый server action `submitScorerLineupAction` с проверкой `match.scorerUserId === currentUser.id || isAdmin` (без 6-часового окна). В `scorer-client.tsx` секция «Составы команд» с двумя карточками и кнопкой «Заявить состав» / «Изменить состав». Новый диалог `scorer-lineup-dialog.tsx` с чекбоксами из roster команды (5-8 игроков). `page.tsx` загружает `roster` для обеих команд.

13. ~~**Кабинет тренера — расширить редактирование команды**~~ ✅ v3.24.0 — `/coach/page.tsx` теперь показывает `<EditTeamButton>` рядом с именем команды (видна только для `coach.role === 'COACH'`, не для ASSISTANT_COACH). Добавлено отображение description команды под заголовком. Переиспользует существующий диалог редактирования.

### Группа C — Пошаговый пайплайн проведения матча

14. ~~**Редизайн интерфейса скорера как последовательного workflow**~~ ✅ v3.25.0 — реализован 11-шаговый wizard в `apps/grandslamcup/src/app/match/[id]/score/_components/wizard/`. Ключевое архитектурное решение: wizard state **вычисляемый** через `computeWizardStep(match, matchState)` — нет локальной machine, просто реактивное отображение текущего состояния матча. При любом SSE event wizard автоматически перерисовывается на нужный шаг.

    **Реализованные шаги:**
    1. ✅ START_MATCH — большая кнопка старта + проверка составов
    2. ✅ SELECT_JURY — QR + 5 слотов с цветами + ручное назначение
    3. ✅ COIN_FLIP — две карточки команд + кнопка жеребьёвки
    4. ✅ PERFORMER_PICK — ожидание тренера + fallback ручного выбора
    5. ✅ TEXT_VOTING — имя + таймер + ScorerVoteInput + кнопка force-complete
    6. ✅ DELIVERY_VOTING — тот же компонент с prop dimension
    7. ✅ PAIR_RESULTS — разбор пары (отброс min/max) + победитель
    8. ✅ HALF_SUMMARY — итоги тайма, топ-3, таблица всех пар
    9. ✅ INTERMISSION — перерыв с таймером
    10. Объединено с 11 → финальный экран
    11. ✅ VICTORY_POEM — выбор поэта команды-победителя
    12. ✅ MATCH_FINISHED — финальный счёт + MVP + ссылки

    **Стратегия отката:** `?mode=classic` → возврат к старому `scorer-client.tsx` (не трогался, остаётся для emergency).

15. ~~**Поле «победное стихотворение» в модели Match**~~ ✅ v3.25.0 — добавлено `Match.victoryPoemPlayerId: String?` с relation `victoryPoemPlayer Player?`. Миграция `add_victory_poem`. Используется в шаге VICTORY_POEM + отображается в MATCH_FINISHED.

### Группа D — Питер: исторические данные и правила

16. **Неверный номер сезона Питера** — на сайте показывается «3-й сезон», а в реальности идёт 2-й. Модель `Season` не имеет `seasonNumber`, номер извлекается из `name`/`slug`. Проверить данные: либо существует лишний `Season` в БД, либо отображение считает неправильно. Исправить.

17. **Структура 2-го сезона Питера** — добавить в БД:
    - 4 дивизиона по 4 команды (первая фаза — round-robin внутри дивизиона)
    - Из каждого дивизиона: top-2 → Высшая лига, bottom-2 → Первая лига
    - Вторая фаза — round-robin внутри лиги
    - Сейчас модели `Division` нет. Добавить `Division` в `schema.zmodel` или использовать существующую `League` с группировкой.

18. **Суммирование игр дивизион ↔ лига** — статистика поэта/команды за сезон должна включать обе фазы (дивизион + лига). Сейчас игры в дивизионах не суммируются. Проверить агрегацию в `apps/grandslamcup/src/app/[citySlug]/teams/[slug]/page.tsx` и связанных statistics-запросах.

19. **Заблокировать игроков до конца сезона** — через `/admin/suspensions` создать записи `PlayerSuspension` с `untilEndOfSeason: true`, `reason: OTHER` (или новый enum):
    - Евгений Лимонов (команда Бугульма)
    - Героиновая лужа (команда Бугульма)
    - Причина: нарушение правил (уточнить у организаторов формулировку).

20. **Обнуление результатов исключённых команд** — по итогу 1 круга команду «Бугульма» исключили (команда), «Бюро» и «Чумные» расформировались сами. Их результаты должны быть **обнулены** в таблице итогов сезона, но сейчас учитываются. Проверить логику агрегации standings, добавить флаг `Team.excluded: Boolean` или фильтр по `Team.status`.

21. **Поправка правил от 2026-03-01: несовершеннолетние** — добавить в `apps/grandslamcup/src/app/[citySlug]/rules/page.tsx` (или аналог): «Несовершеннолетние участники не допускаются без родителей или опекунов». Синхронизировать с правилами Питера.

22. **Скопировать правила Питера с telegra.ph** — источник: https://telegra.ph/Pravila-KBS-01-27. Скопировать текст целиком в markdown, внести поправку из п.21. Положить в `apps/grandslamcup/src/app/[citySlug]/rules/` как data-файл (или через админку `/admin/rules`).

### Группа E — Ключница (auth-hub)

23. **Миграция `oidc-provider` → `@better-auth/oauth-provider`** — deprecation warning в логах auth-hub. Плагин удалят в следующем мажоре Better Auth. Миграция по `https://www.better-auth.com/docs/plugins/oauth-provider`. Файл: `apps/auth-hub/src/lib/auth.ts`.

24. **State mismatch: State not persisted correctly** — production-ошибка при OAuth callback: `code: 'state_security_mismatch'`. Причины: неправильный cookie domain, отсутствует `sameSite`, обрыв сессии между redirect. Проверить `cookies` настройки в `apps/auth-hub/src/lib/auth.ts`.

25. **Редирект после OAuth возвращает в Ключницу вместо исходного приложения** — пользователь жмёт «Войти через Google» из `grandslamcup.letar.best`, после авторизации попадает на `auth.letar.best` вместо возврата в `grandslamcup`. Нужно при старте OAuth-флоу сохранять `returnTo` URL (через state или signed cookie), после callback редиректить туда. Файлы: `apps/auth-hub/src/lib/auth.ts`, `apps/auth-hub/src/app/(auth)/sign-in/...`.

### Группа F — Москва: составы 1-го и 2-го туров

26. **Добавить составы в матчи 1 и 2 туров Москвы** — итеративная работа с пользователем: он назовёт дату и команды, найдёт составы в чате, я внесу в `MatchLineup` через `/admin/matches/[id]` или напрямую SQL. Список матчей получить через MCP `postgres-grandslamcup` — SELECT по `tour.number IN (1, 2) AND city = 'Moscow'`.

---

## Фаза 5 — Обратная связь и доработки (v2.7.0+)

> Источник: фидбек от Морены Лабутиной (зам. тренера) и организаторов, 2026-04-06

### Группа A — Баги и критические исправления

1. ~~**Тренер не может редактировать профили игроков с привязанной учёткой**~~ ✅ v2.8.0

2. ~~**Метка "ASSISTANT_COACH" отображается как raw enum**~~ ✅ v2.8.0

3. ~~**На мобиле в админке не листается таблица вправо**~~ ✅ v2.7.0

4. ~~**Поля ввода не видны на тёмной теме**~~ ✅ v2.7.0

5. ~~**Команда "СТИХИ НАРОДА" → 404**~~ ✅ v2.8.0 (redirect на правильный город)

6. ~~**Нет отступа логотипа от шапки на мобиле (главная)**~~ ✅ v2.8.0

7. ~~**Прошедшие матчи остаются в статусе SCHEDULED**~~ ✅ v2.8.0 (псевдостатус PAST_SCHEDULED)

### Группа B — Роли и модель команды

8. ~~**Рефакторинг ролей в команде**~~ ✅ v3.6.0
   - Ограничение 1 COACH на команду (5 проверок), фильтрация неиграющих на паблике

9. ~~**Счетовод и ведущий — новые роли**~~ ✅ v3.6.0
   - Назначение в админке (dropdown), страницы `/presenters` и `/scorers`

10. ~~**Привязка поэта к учётке — улучшение UX**~~ ✅ v2.9.0
    - Кнопка "Это я" на публичной странице поэта → заявка с модерацией (pendingUserId)
    - ✅ UI одобрения в модерации — диалог подтверждения с аватаром, данными пользователя, причиной отказа (v3.18.0)

### Группа C — Расширенная статистика

11. ~~**Рейтинговая таблица поэтов**~~ ✅ v3.0.0
    - Колонки "Всего" и "Лучший", сортировка по суммарному баллу
    - ✅ График динамики балл��в (recharts) + таблица ��стории соперников (v3.18.0)

12. ~~**Расширенная статистика поэта**~~ ✅ v3.0.0
    - Карточки, тридцатки, процент побед, ср. время, метка "Дебют"

13. ~~**Статистика в кабинете тренера**~~ ✅ v3.0.0
    - W/D/L, очки, средний балл, топ-3 перформера

14. ~~**Карточки на страницах поэтов и команд**~~ ✅ v3.0.0
    - Поэт: badge в hero. Команда: суммарные + предупреждение о дисквалификации

15. ~~**Реестр дисквалифицированных поэтов**~~ ✅ v3.10.0
    - Чтение чужих стихов → минимальные оценки (1) + дисквалификация на сезон
    - ✅ Админка `/admin/suspensions` — управление отстранениями, создание с поиском поэта
    - ✅ Кнопка "Плагиат" в деталях матча `/admin/matches/[id]` — обнуление оценок + дисквалификация
    - ✅ Публичный реестр `/[citySlug]/suspensions` — "Чтение чужих стихов", "До конца сезона"
    - ✅ Проверка при формировании заявки — дисквалифицированные не допускаются

### Группа D — Кликабельность и навигация

16. ~~**Ссылки в таблицах и списках**~~ ✅ v3.1.0
    - Кабинет тренера, roster, матч — все имена кликабельны

17. ~~**Бесконечная загрузка для списков**~~ ✅ v3.6.0
    - "Показать ещё" для поэтов и команд (server-side pagination)

18. ~~**Индикация загрузки**~~ ✅ v3.1.0
    - TopLoader + loading.tsx для match, profile, sign-in

19. ~~**Активный матч в расписании**~~ ✅ v3.1.0
    - LIVE матчи в отдельной секции "Сейчас идёт", не попадают в прошедшие

### Группа E — Кабинет тренера (доработки)

20. ~~**Ближайшие матчи и мотивация заполнить состав**~~ ✅ v3.2.0
    - CTA "Заявить состав" на дашборде, предупреждения по времени
    - ✅ Push-уведомления за день до матча — модель PushSubscription, web-push API, кнопка подписки в хедере, cron endpoint (v3.18.0)

21. ~~**Заявки — упрощение**~~ ✅ v2.9.0
    - Тренер добавляет игроков мгновенно, модерация только для трансферов

22. ~~**Товарищеские матчи**~~ ✅ v3.2.0 → v3.7.0 → v3.17.0 (challenge workflow)
    - Организатор создаёт через `/admin/matches/create`
    - ✅ Заявка от тренера: модель FriendlyMatchRequest, `/coach/friendly`, модерация `/admin/moderation/friendly`
    - ✅ v3.17.0: Workflow «Тренер → Соперник → Админ» — тренер-соперник принимает/отклоняет вызов перед модерацией

23. ~~**Заявка состава на матч из кабинета тренера**~~ ✅ v3.2.0
    - `/coach/matches/[id]/lineup` — выбор 5-8 игроков, предзаполнение
    - Отображение в Telegram-афише с гиперссылками на профили

### Группа F — Telegram-бот

24. ~~**Бот для публикации в канал Telegram**~~ ✅ v3.14.0
    - ✅ Модель TelegramConfig (глобальный токен) + City.telegramChatId (канал города)
    - ✅ Кнопки "Анонс", "Итог тайма", "Результат" в админке матча
    - ✅ Афиша: составы, ведущий, счетовод, ссылки на профили, Яндекс.Карты
    - ✅ Метка 🆕 для дебютантов, товарищеские с другим заголовком
    - ✅ Промежуточные итоги по таймам, финальный результат с MVP и карточками
    - ✅ /admin/settings — настройки бота (токен, тест, вкл/выкл)
    - ✅ API cron: `/api/telegram/weekly` и `/api/telegram/today`
    - TODO: Настроить cron на сервере после деплоя — согласовать расписание с организаторами
      - `GET /api/telegram/weekly?secret=CRON_SECRET` — еженедельное расписание (предложение: пн 09:00 МСК)
      - `GET /api/telegram/today?secret=CRON_SECRET` — утреннее напоминание (предложение: ежедневно 09:00 МСК)
      - `GET /api/push/match-reminder?secret=CRON_SECRET` — push за день до матча (предложение: ежедневно 17:00 МСК)
    - ✅ Постер (satori→PNG) для анонсов и результатов — автоматически с sendPhoto (v3.18.0)

### Группа G — Пайплайн матча (ревизия)

25. **Детальная ревизия пайплайна проведения матча** (в процессе)
    - Создание матча организатором: команды, время, место, счетовод, ведущий
    - Тренеры заявляют составы через кабинет
    - После заполнения — матч можно опубликовать в Telegram
    - ~~**Интерфейс тренера:** выбор кто выходит, статус игроков (не играл / 1-й тайм / оба), **отвод судьи**~~ ✅ v3.11.0
    - ~~**Интерфейс ведущего:** жеребьёвка, опция "разрешён ли отвод судьи"~~ ✅ v3.12.0
    - ~~**Интерфейс судьи:** изменение голоса до финального подсчёта, cookie-блокировка для 2-го тайма~~ ✅ v3.12.0
    - ~~**Интерфейс счетовода (оффлайн):** полная работа без интернета (предзагрузка составов → IndexedDB), ввод оценок вручную, публикация при восстановлении связи~~ ✅ v3.13.0
    - ~~**Интерфейс проектора:** экран перерыва с донатами, "оставить подарок на баре"~~ ✅ v3.12.0
    - Нет возможности добавить новый матч из админки — ✅ уже есть `/admin/matches/create`
    - ~~Нет ручного ввода/редактирования оценок и итогов в админке — добавить~~ ✅ v3.12.0
    - ~~Фильтры матчей в админке по городу и статусу~~ ✅ v3.12.0

### Группа H — Личный кабинет поэта

26. ~~**Личный кабинет поэта**~~ ✅ v3.3.0
    - `/poet` — дашборд, профиль, стихи. Модель Poem с CRUD.

27. ~~**Ссылка на Ключницу в профиле**~~ ✅ v3.3.0
    - Ссылка "Настройки аккаунта" в `/poet/profile`

### Группа I — Фотографии

28. ~~**Увеличить лимит загрузки до 15 МБ + ресайз**~~ ✅ v3.4.0
    - 15 МБ лимит, sharp ресайз до 1920px, аватары 400x400

29. ~~**Кадрирование 1:1 для фото профиля поэта**~~ ✅ v3.4.0
    - react-easy-crop (круглый, aspect 1:1) в EntityPhotoUploader

### Группа J — Прочее

30. ~~**Объединение дублей профилей поэтов**~~ ✅ v3.7.0
    - Пример: "Судаков Павел" и "Паша Судаков" — один человек
    - ✅ `/admin/players/merge` — поиск, preview переносимых данных, транзакционное объединение с проверкой unique constraints

31. ~~**История команд поэта**~~ ✅ v3.6.0
    - Timeline на профиле поэта: сезон, команда, лига, роль, даты

32. ~~**Мобильная админка — полная проверка**~~ ✅ v3.21.0
    - ✅ 3 shared-компонента: AdminResponsiveList, AdminCard, AdminActionsMenu
    - ✅ 14 таблиц → карточки на mobile (< md): Cities, Venues, Seasons, Teams, Players, Matches, Moderation, Friendly, Claims, Suspensions, Users, News, Donate
    - ✅ Фикс search/filter overflow: minW="200px" → minW="0", dropdown w={{ base: '100%', sm: '200px' }}
    - ✅ Action-кнопки → ⋮ меню на mobile через AdminActionsMenu
    - ✅ Шапка: «КБС Админ» на mobile, имя пользователя скрыто на < md
    - ✅ Desktop-вид без изменений — переключение через display={{ base, md }}

33. **Трансляция матча на сайте (вопрос)**
    - Возможность встраивания стрима (YouTube/VK Live) на страницу матча
    - Требует уточнения у организатора

34. ~~**Новости и Поддержать — привязка к городу**~~ ✅ v3.6.0
    - cityId на NewsPost/DonateLink, городские роуты, header/footer ссылки, выбор города в админке

35. ~~**Привязка поэт↔пользователь из админки**~~ ✅ v3.9.0
    - `/admin/players/[id]`: секция привязки — привязать по email, отвязать, одобрить/отклонить заявку "Это я"
    - `/admin/users/[id]`: секция привязки — поиск поэта по имени с debounce, привязка, отвязка
    - `/admin/moderation/claims`: страница модерации заявок "Это я" (pendingUserId)
    - Кнопка "Привязка профилей" с badge на странице модерации

### Группа K — Инфраструктура

36. ~~**Staging-окружение gsc-test.letar.best**~~ ✅
    - ✅ `docker-compose.staging.yml` — отдельные контейнеры на s1 (порты 5453/3016)
    - ✅ `.env.staging.example` — шаблон переменных
    - ✅ `--staging` флаг в `deploy-affected.sh` (compose, env, image tag)
    - ✅ `infra/staging/sync-db-staging.sh` — pg_dump/restore prod→staging
    - ✅ SSH s1→s2 настроен для скрипта синхронизации
    - ✅ OIDC redirect URI в `auth-hub/src/lib/auth.ts` (trustedClients)
    - ✅ `robots.ts` запрещает индексацию staging-доменов
    - ✅ Зарегистрирован в Dashboard (DeployedApp на s1)
    - Workflow: `./deploy-affected.sh --app grandslamcup --staging` → проверка → prod

---

## 1. О проекте

**Кубок Большого Слэма (КБС)** — первый в России командный поэтический турнир в формате poetry-clash, организованный по принципу футбольного чемпионата.

**Текущее состояние:** Лендинг на Tilda (grandslamcup.ru) — статичные HTML-таблицы, текстовое расписание, правила, несколько страниц команд. Результаты матчей ведутся в Google Таблицах.

**Масштаб:**

- **Санкт-Петербург:** 2-й сезон (2025-2026), 16 команд, Высшая лига (8) + Первая лига (8)
- **Москва:** 1-й сезон (2025), 12 команд
- ~300+ поэтов, ~30 площадок-стадионов
- Регулярность: 2-4 матча в неделю

**Цель:** Полноценная турнирная платформа — живые таблицы, расписание, профили, живой скоринг матчей (замена Google Таблиц), админка и кабинет тренера.

**Технические решения:**

- Приложение `apps/grandslamcup` в монорепо letar
- Next.js 16, Chakra UI v3, PostgreSQL + Prisma + ZenStack
- Миграция всех исторических данных с Tilda
- Роли: суперадмин, организатор города, тренер, публичный доступ

---

## 2. Регламент турнира (из правил КБС)

### 2.1 Структура сезона

- Сезон = 2 круга (осень + весна)
- В каждом круге каждая команда играет с каждой по одному разу
- За сезон каждая команда встречается с каждым соперником дважды: один раз дома, один раз в гостях
- Между кругами — **трансферное окно** (игроки могут сменить команду)

### 2.2 Команда

- Минимум 5 поэтов, верхнего лимита нет
- Обязателен **тренер** (играющий или не играющий)
- Обязателен **домашний стадион** (бар, клуб, библиотека и т.д.)
- Новых игроков можно привлекать без ограничений в течение сезона
- Если в команде менее 5 игроков — снимается с турнира

### 2.3 Трансферы

- Игрок, выступавший за одну команду, **не может** свободно перейти в другую
- Переход возможен **только в трансферное окно** (между кругами)
- Новые игроки (не выступавшие ни за кого) могут присоединиться в любое время

### 2.4 Матч — правила

**Заявка:** 5-8 игроков от команды. Тренер подаёт ведущему за 6 часов до матча. Играющий тренер входит в лимит 8. Не играющий тренер — сверх лимита.

**Структура:** 2 тайма, в каждом по 5 пар (10 пар за матч).

**Очерёдность:** В 1-м тайме — жеребьёвка, кто начинает. Во 2-м тайме — порядок меняется (кто был первым, становится вторым).

**Тренер** непосредственно перед выходом очередного игрока озвучивает, кто выходит к микрофону (порядок не фиксирован заранее).

**Замены:** Во 2-м тайме команда может сделать до 2 замен запасными из заявки.

**Ход раунда (пары):**

1. Выступает поэт команды A (лимит 3 минуты)
2. Выступает поэт команды B (лимит 3 минуты)
3. Судьи оценивают поэта A
4. Судьи оценивают поэта B
5. Следующая пара

**В одном тайме игрок не может выходить дважды.**
**Но один игрок МОЖЕТ играть в обоих таймах** (если в заявке 5-8 человек, тренер волен повторить кого-то во 2-м тайме).

### 2.5 Судейство

**Жюри:** 5 человек, выбираются ведущим случайно из зрителей. Не болельщики команд, не из одной компании. **Жюри полностью обновляется на второй тайм.**

**Оценки по двум категориям:**

- **Текст** — оценка стихотворения (1-5)
- **Подача** — оценка исполнения (1-5)

Каждый судья ставит 2 оценки → итого 10 оценок на поэта.

**Подсчёт:** Из 5 оценок в каждой категории отбрасывается максимальная и минимальная. Оставшиеся 3 складываются. Баллы двух категорий суммируются = итого поэта.

Пример: текст [4,5,4,3,5] → отброс 3 и 5 → 4+4+5=13, подача [3,4,4,5,4] → отброс 3 и 5 → 4+4+4=12, **итого: 25**.

Максимум за выступление: 30 баллов (15 текст + 15 подача).

### 2.6 Результаты

**Матч:** Все баллы поэтов команды суммируются. Команда с большей суммой — победитель.

- Победа = 1 очко
- Ничья = 0.5 очка каждой команде
- Поражение = 0 очков

**Тай-брейк при равенстве очков** в таблице: по результатам личных встреч.

**Личный зачёт:** Каждый поэт — рейтинг по баллам (справочно).

### 2.7 Карточки (нарушения)

**Жёлтая карточка** — за нарушения (превышение 3 минут, пение и др.).
**Красная карточка** — за грубые нарушения.

**Накопительный эффект:** 5 жёлтых карточек у команды за сезон = дисквалификация команды (снятие с турнира).

**UX:** Выдача карточки — через диалог подтверждения (чтобы избежать случайных нажатий).

---

## 3. Модель данных

### Справочники

```
City              — город (СПб, Москва, ...)

Venue             — стадион/площадка
  → name, slug, city, address, coordinates, photo, description
  → telegramLink, websiteUrl
```

### Турнирная структура

```
Season            — сезон (2024-2025 СПб, 2025 Москва, ...)
  → city, name, startDate, endDate
  → status: upcoming | active | finished

League            — лига в рамках сезона (Высшая, Первая)
  → season, name, order

Round             — круг (Первый круг, Второй круг)
  → season, name, number, startDate, endDate

Tour              — тур внутри круга (1 тур, 2 тур, ...)
  → round, number
```

### Участники

```
Team              — команда
  → name, slug, city, logo, description, homeVenue
  → telegramLink
  → previousNames: String[] (история переименований, напр. "Болт" → "Вином")

TeamSeason        — участие команды в сезоне + лиге
  → team, season, league

Player            — поэт/игрок
  → name, slug, photo, bio, city
  → telegramLink, vkLink

PlayerTeamSeason  — принадлежность игрока к команде в сезоне
  → player, teamSeason
  → role: player | coach | playing_coach | producer
  → joinedAt, leftAt (null если активен)

Transfer          — трансфер между командами
  → player, fromTeamSeason, toTeamSeason, date
```

### Матчи и результаты

```
Match             — матч
  → tour, league
  → homeTeam, awayTeam (TeamSeason)
  → venue, scheduledAt
  → status: scheduled | live | finished | postponed
  → homeScore, awayScore (суммарные баллы, рассчитываются)
  → homePoints, awayPoints (турнирные очки: 1 / 0.5 / 0)
  → firstHalfStartTeam: home | away (жеребьёвка)
  → scorerToken, presenterToken (доступ по ссылке)

MatchLineup       — заявка на матч
  → match, teamSeason, player
  → status: starter_half1 | starter_half2 | substitute | unused
  → order (порядок выхода, заполняется по ходу матча)

PlayerPerformance — выступление поэта в раунде
  → match, player, teamSeason
  → half (1 | 2), roundNumber (1-5)
  → textScores: Int[] (5 оценок за текст, шкала 1-5)
  → deliveryScores: Int[] (5 оценок за подачу, шкала 1-5)
  → textAdjusted: Int (после отброса макс/мин, сумма 3 средних)
  → deliveryAdjusted: Int (после отброса макс/мин, сумма 3 средних)
  → totalScore: Int (textAdjusted + deliveryAdjusted)
  → durationSec: Int (время выступления с таймера)

Card              — карточка (нарушение)
  → playerPerformance
  → type: yellow | red
  → reason: overtime | singing | other
  → note: String? (комментарий)
  → TODO: уточнить влияние на баллы у организатора
```

### Кэшированные данные (пересчитываются)

```
Standings         — турнирная таблица
  → teamSeason, position
  → played, won, drawn, lost, points
  → scored, conceded, difference

PlayerRating      — личный зачёт поэта
  → player, season
  → matchesPlayed, totalScore, avgScore, bestScore
```

---

## 4. Роли и доступ

| Роль                   | Доступ                                                                                  |
| ---------------------- | --------------------------------------------------------------------------------------- |
| **Суперадмин**         | Всё: сезоны, лиги, матчи, результаты, команды, пользователи, настройки                  |
| **Организатор города** | Свой город: матчи, результаты, модерация заявок, генерация ссылок скоринга              |
| **Тренер**             | Своя команда: профиль, состав, заявка на матч (за 6 ч), трансферные заявки              |
| **Скорер / Ведущий**   | Конкретный матч: ввод оценок / просмотр + таймер (по ссылке с токеном, без регистрации) |
| **Публичный**          | Просмотр: таблицы, расписание, профили, результаты (без авторизации)                    |

---

## 5. Живой скоринг матча — ключевая фича

Замена Google Таблиц. Три экрана для одного матча, связанных в реальном времени.

### 5.1 Экран судьи `/match/[id]/judge` (телефон жюри)

**Кто:** Каждый из 5 судей. Доступ по QR-коду (сканируют у скорера перед таймом).

**Регистрация:**

- Скорер показывает QR-код на ноутбуке/экране
- Судья сканирует → вводит своё имя → подключился
- Система: «Анна подключилась», «Дмитрий подключился» ... «5/5 — готовы»
- На 2-й тайм — новый QR-код для нового жюри

**Два режима судейства (на каждого судью отдельно):**

- **Телефон** (основной) — судья голосует сам через экран
- **Табличка** (fallback) — судья без телефона/интернета получает физические таблички, скорер вводит его оценки вручную. Скорер при регистрации жюри отмечает: «Судья N — таблички»

**Интерфейс (мобильный, максимально простой):**
Экран судьи управляется ведущим — судья видит только то, что сейчас нужно оценить.

Состояния экрана:

1. **«Ожидание...»** — выступления идут, голосование ещё не открыто
2. **«Оцените ТЕКСТ: [Имя поэта]»** — ведущий открыл голосование за текст. Кнопки 1-5, крупные. Судья нажимает → «Ваша оценка: 4 ✓ Ожидание других судей...»
3. **«Оцените ПОДАЧУ: [Имя поэта]»** — ведущий открыл голосование за подачу. Аналогично
4. **«Спасибо! Следующая пара...»** — оба голосования завершены

- Блокируется после отправки оценки (нельзя переголосовать)
- Одна оценка за раз — сначала только текст, потом только подача
- Судья видит только своё имя и номер («Судья 3 — Анна»), не видит чужих оценок до объявления

### 5.2 Скорер `/match/[id]/score` (панель управления матчем)

**Кто:** Помощник у сцены (ноутбук). Доступ по ссылке с токеном.

**Интерфейс:**

- Текущий тайм → раунд → пара поэтов
- **Генерация QR-кода** для жюри текущего тайма
- Мониторинг: сколько судей подключилось (0/5 ... 5/5 ✓)
- Фиксация кто вышел выступать:
  - **Основной:** тренер выбирает на экране телефона `/match/[id]/coach` → скорер видит мгновенно
  - **Fallback:** тренер говорит устно → скорер выбирает из списка вручную
- Мониторинг голосования: оценки прилетают в реальном времени (3/5... 5/5 ✓)
- Видит все индивидуальные оценки судей + автоподсчёт
- **Ручной ввод (fallback):** Если судья не может с телефона — скорер вводит его оценки вручную
- Возможность сбросить оценку судьи (если ошибка)
- Фиксация карточек (нарушений)

### 5.3 Экран ведущего `/match/[id]/presenter`

**Кто:** Ведущий у микрофона (телефон). Доступ по ссылке с токеном.
**Роль: ведущий управляет процессом голосования со сцены.**

**Интерфейс (крупный шрифт, высокий контраст):**

- **Таймер выступления (лимит 3 мин):** старт/стоп. Цвета: зелёный → жёлтый (2:30) → красный (3:00)

- **Управление голосованием (пошагово):**
  1. `▶ Голосуем за ТЕКСТ` → у судей на телефонах появляется форма оценки текста
     → Прогресс с именами: «Анна ✓, Дмитрий ✓, ожидание: Олег...» → 5/5 ✓
     → Результат: **«Текст: 13»** (после отброса макс/мин)
  2. `▶ Голосуем за ПОДАЧУ` → у судей появляется форма оценки подачи
     → Прогресс с именами → 5/5 ✓ → Результат: **«Подача: 12. Итого: 25»**
  3. `→ Следующий поэт` → переход к поэту B (повторяется шаг 1-2)
  4. `→ Следующая пара` → переход к следующему раунду
  5. `✕ Отменить голосование` → сброс текущего раунда голосования (если нажал рано)

- **Таймер — вибрация телефона:**
  - 2:30 — короткая вибрация (предупреждение)
  - 3:00 — длинная вибрация (лимит)

- **Детализация оценок (после каждого голосования):**
  - Именные оценки: «Анна: 4, Дмитрий: 5, Олег: 2, Марина: 4, Саша: 4»
  - Ведущий видит кто поставил сколько → может спросить: «Олег, почему 2?»
  - Итого после отброса макс/мин

- **Информация для озвучивания:**
  - После текста: «Иванов, текст: 13 баллов»
  - После подачи: «Иванов, подача: 12. Итого за выступление: 25»
  - После обоих поэтов пары: «Иванов 25 — Петров 23»
  - Общий счёт команд: «Чумные 75 : 68 Блины. Лидируют Чумные»

### 5.4 Экран тренера `/match/[id]/coach` (телефон тренера)

**Кто:** Тренер каждой команды (по одному на команду). Доступ по ссылке с токеном или через кабинет тренера.

**Интерфейс:**

- Список игроков из заявки на матч (5-8 человек)
- Статус каждого: «не играл» / «играл в 1-м тайме» / «играл в обоих»
- Кнопка `▶ Выпустить` напротив игрока → скорер и ведущий мгновенно видят кто выходит
- Текущий счёт матча (read-only)
- Результаты раундов (read-only)
- Замены во 2-м тайме (пометить кого меняет)

### 5.5 Экран для проектора `/match/[id]/live` (фаза 2)

**Кто:** Проекция на стену в баре для зрителей.

**Интерфейс:**

- Команды, логотипы, текущий счёт
- Таймер (синхронизирован с ведущим)
- Анимации при вводе новых оценок
- Минимум UI, максимум зрелища
- **QR-код для зрительского голосования** (виден на экране)

### 5.6 Зрительское голосование `/match/[id]/audience` (народное жюри)

**Кто:** Любой зритель в зале. QR-код на проекторе или у скорера.

**Не влияет на результат** — параллельная оценка для статистики и вовлечения.

**Интерфейс (телефон):**

- Минимальная регистрация: имя (опционально) или анонимно
- После каждой пары: «Оцените выступление» → текст 1-5, подача 1-5
- Результат «мнение зала» отображается на проекторе после официального результата
- Ведущий может озвучить: «Жюри поставило 25, а зал — 27!»

**Статистика:**

- Среднее мнение зала vs жюри за матч
- Самые «спорные» выступления (большой разрыв жюри/зал)
- Контент для соцсетей и обзоров

### 5.7 Сценарий матча

```
ПОДГОТОВКА (за 6 часов):
  → Тренеры подают составы (5-8 игроков) через кабинет тренера
  → Или организатор вводит вручную в админке

ПЕРЕД МАТЧЕМ:
  → Организатор генерирует ссылки (score + presenter + coach×2) в админке
  → Скорер открывает /match/123/score на ноутбуке
  → Ведущий открывает /match/123/presenter на телефоне
  → Тренеры открывают /match/123/coach на телефонах (или устно — fallback)
  → Жеребьёвка: кто начинает 1-й тайм (скорер отмечает)

ПОДКЛЮЧЕНИЕ ЖЮРИ (перед каждым таймом):
  → Ведущий выбирает 5 зрителей в жюри
  → Скорер нажимает «Новое жюри» → генерируется QR-код
  → Скорер показывает QR-код на экране ноутбука
  → Судьи сканируют телефонами → «Судья 1 подключился... 5/5 ✓»
  → Готово к началу тайма

1-Й ТАЙМ (5 раундов):
  Раунд 1:
    → Тренер команды A нажимает «Выпустить» на телефоне (или озвучивает → скорер выбирает)
    → Ведущий и скорер видят: «Выходит: Иванов (Чумные)»
    → Ведущий запускает таймер → поэт A выступает → стоп (≤3 мин)
    → Тренер команды B нажимает «Выпустить» (или озвучивает)
    → Ведущий запускает таймер → поэт B выступает → стоп (≤3 мин)
    → Ведущий: «Голосуем за ТЕКСТ!» → нажимает кнопку на телефоне
    → У судей на экранах: «Оцените ТЕКСТ: Иванов» → кнопки 1-5
    → Оценки прилетают (1/5... 5/5 ✓) → автоподсчёт
    → Ведущий озвучивает: «Текст — 13 баллов!»
    → Ведущий: «Оцениваем ПОДАЧУ!» → нажимает кнопку
    → У судей: «Оцените ПОДАЧУ: Иванов» → кнопки 1-5
    → 5/5 ✓ → автоподсчёт
    → Ведущий озвучивает: «Подача — 12. Итого Иванов — 25 баллов!»
    → Ведущий нажимает «Следующий поэт» → аналогично для поэта B
    → После обоих: «Иванов 25 — Петров 23. Счёт: Чумные 25 : 23 Блины»
    → (если нарушение — скорер фиксирует карточку)
  ... повторить 5 раундов

ПЕРЕРЫВ:
  → Экран показывает итоги 1 тайма
  → Ведущий выбирает новое жюри (5 человек)
  → Скорер нажимает «Новое жюри» → новый QR-код → 5 новых судей подключаются
  → Команды могут сделать до 2 замен (скорер отмечает)

2-Й ТАЙМ (5 раундов):
  → Порядок команд меняется (кто был первым → вторым)
  → Аналогично 1-му тайму

ФИНАЛ:
  → Итоговый счёт автоматически
  → Определение победителя + турнирные очки
  → Данные сразу в БД → таблица на сайте обновляется
```

### 5.8 Защита и аналитика судейства

**Защита от повторных судей:**

- Система запоминает имена судей по матчам
- Если имя совпадает с судьёй из 1-го тайма → предупреждение скореру при регистрации во 2-м
- Если имя совпадает с игроком из состава команды → предупреждение: «Возможно болельщик!»

**Статистика судей (аналитика после матча):**

- Средний балл каждого судьи за матч
- Разброс оценок (кто ставил всем 1 или всем 5)
- Корреляция с другими судьями (насколько оценки отличались от среднего)
- Не для наказания, но видно если судья «неадекватен»

### 5.9 После матча

**MVP матча — автоматически:**

- Лучший поэт матча (максимальный балл за выступление)
- Отображается на странице матча
- Статистика за сезон: «Больше всех MVP — Иванов (4 раза)»

**Протокол матча (PDF):**

- Автоматическая генерация после завершения
- Содержит: дата, стадион, составы, все оценки поэт-по-поэту с именами судей, карточки, итоговый счёт, MVP
- Скачивается из админки / страницы матча

**Шаринг результатов:**

- Кнопка «Поделиться» на странице матча
- Генерация красивой карточки (OG-image): счёт, команды, MVP, стадион
- Оптимизировано для Telegram/VK — при вставке ссылки показывается превью с результатом

### 5.10 Технические требования

- **Реальное время:** WebSocket или SSE (судьи → скорер → ведущий → проектор, задержка <1 сек)
- **Оффлайн-устойчивость:** Данные сохраняются локально (IndexedDB), синхронизируются при восстановлении Wi-Fi
- **Доступ без регистрации:** Ссылки с токенами для скорера/ведущего, QR-код с токеном для судей
- **QR-код жюри:** Генерируется на каждый тайм (новое жюри = новый код). Истекает после тайма. При регистрации судья вводит имя
- **Смешанный режим:** Часть судей с телефонами, часть с табличками — в одном тайме одновременно. Скорер вводит оценки за «табличных» судей вручную
- **Откат:** Скорер может сбросить оценку судьи и запросить переголосование
- **Таймер (лимит 3 мин):** Синхронизирован между экранами. Индикация: зелёный → жёлтый (2:30) → красный (3:00)
- **Блокировка:** После завершения матча — редактирование только через админку
- **Защита от повторного голосования:** Судья не может проголосовать дважды за одного поэта

---

## 6. Публичная часть (страницы)

### Главная `/`

- Герой-блок: название, описание проекта, города
- Ближайшие матчи (карточки с датой, командами, стадионом)
- Турнирные таблицы текущих сезонов (превью топ-5)
- Последние результаты
- Ссылка на правила

### Расписание `/schedule`

- Фильтры: город, лига, тур, команда
- Список матчей: дата, время, стадион, команды, результат (если завершён)
- Клик → страница матча

### Матч `/matches/[id]`

- Статус: запланирован / идёт / завершён
- Дата, время, стадион (адрес)
- Составы обеих команд (заявка)
- Если завершён: результат поэт-по-поэту (текст + подача + итого)
- Итоговый счёт
- Если live: текущий счёт в реальном времени

### Таблица `/standings`

- Фильтры: город, сезон, лига
- Турнирная таблица: позиция, команда, И, В, Н, П, забито, пропущено, разница, очки
- Перекрёстная таблица: кто с кем играл, счёт каждого матча

### Команды `/teams`

- Список всех команд с фильтром по городу/лиге
- Карточки: лого, название, стадион, лига

### Команда `/teams/[slug]`

- Профиль: лого, описание, тренер (ссылка на Telegram), стадион
- Текущий состав (список поэтов)
- Статистика текущего сезона (И, В, Н, П, очки)
- Календарь матчей команды (результаты + предстоящие)
- Результаты матчей (таблица: дата, соперник, счёт, очки)
- Архив по сезонам

### Поэты `/players`

- Личный зачёт / рейтинг (топ по среднему баллу)
- Фильтры: город, сезон, команда

### Поэт `/players/[slug]`

- Фото, био, ссылки (Telegram, VK), текущая команда
- Статистика: раундов сыграно, средний балл, лучшее выступление, средний текст / средняя подача
- **Индивидуальный зачёт** (как на Tilda): таблица по раундам — дата, матч, тайм, оппонент, счёт vs счёт оппонента
- Итого: раундов, общий счёт, разница с оппонентами

### Стадионы `/venues`

- Список площадок с фото и адресами
- Карта (Яндекс.Карты)

### Правила `/rules`

- Статичная страница (из markdown)

---

## 7. Админка `/admin`

### Дашборд

- Текущие сезоны, ближайшие матчи
- Заявки на модерацию (от тренеров)
- Живые матчи (ссылки на скоринг)

### Сезоны и лиги

- CRUD сезонов, привязка к городу
- Управление лигами внутри сезона
- Управление кругами и турами

### Команды

- CRUD команд, привязка к сезону/лиге
- Управление составом (добавить/убрать игрока)
- Трансферное окно (открытие/закрытие, одобрение заявок)

### Матчи

- Создание расписания (вручную или генератор round-robin)
- Генерация ссылок скоринга (score + presenter)
- Ввод/редактирование результатов (если не через живой скоринг)
- Пересчёт таблицы

### Поэты

- CRUD профилей
- Привязка к командам/сезонам

### Стадионы

- CRUD площадок (название, адрес, координаты, фото, ссылки)

### Модерация заявок

- Заявки на новые команды
- Заявки на вступление в команду
- Трансферные заявки

---

## 8. Кабинет тренера `/coach`

- Профиль команды (редактирование описания, лого)
- Управление составом (добавить/убрать — на модерацию организатору)
- **Заявка на матч** (выбор 5-8 игроков из состава, за 6 часов до матча)
- Трансферные заявки (подать/отозвать — в трансферное окно)
- Расписание и результаты своей команды
- **Уведомления:** за день до матча — «Завтра матч с Блинами, стадион Глагол, 19:00. Не забудь подать состав» (email / Telegram)

---

## 9. Миграция данных

Перенести с Tilda:

- Все команды: СПб сезон 1 (12 команд) + сезон 2 (16 команд, 2 лиги) + Москва сезон 1 (12 команд)
- Составы команд (поэты + тренеры + продюсеры)
- Переименования команд (Болт → Вином)
- Все матчи с итоговыми счетами
- **Данные по раундам** — на страницах поэтов есть поэт vs оппонент с баллами по каждому раунду каждого матча
- Стадионы (названия, Telegram-ссылки)
- Перекрёстные таблицы
- Страницы команд (описания, ссылки)
- Страницы поэтов (био, ссылки VK/Telegram, индивидуальный зачёт)

Формат: парсинг HTML с Tilda → seed-скрипт для БД.

**Примечание:** Адреса стадионов на Tilda отсутствуют (только названия и Telegram). Адреса нужно будет заполнить вручную.

---

## 10. Фазы реализации

### Фаза 1 — MVP

1. ~~Создание приложения в монорепо~~ ✅ v0.1.0
2. ~~Модель данных (schema.zmodel)~~ ✅ v0.2.0
3. ~~Auth (суперадмин + организаторы + тренеры)~~ ✅ v0.1.0
4. ~~Админка: сезоны, команды, игроки, стадионы, матчи~~ ✅ v0.3.0 + v0.4.0
5. ~~**Живой скоринг: судьи на телефонах + скорер** (Phase 1 MVP)~~ ✅ v0.5.0
6. ~~Таймер с вибрацией + отмена голосования + таймаут судей + экран ведущего (Phase 2)~~ ✅ v0.6.0
7. ~~MVP матча (автоматически)~~ ✅ v0.7.0
8. ~~Публичная часть: таблицы, расписание, профили команд, результаты матчей~~ ✅ v0.7.0
9. ~~Шаринг результатов (OG-карточки для Telegram/VK)~~ ✅ v0.7.1
10. ~~Миграция данных с Tilda~~ ✅ v1.4.0 (итерация 1 — СПб из HTML)
    ~~10b. Миграция данных из Telegram (итерация 2 — СПб + Москва из AI-экстракции)~~ ✅ v1.5.0
11. ~~Деплой~~ ✅ v2.7.0

### Фаза 2 — Расширение

1. ~~Кабинет тренера (заявки на матч, управление составом)~~ ✅ v0.8.0
2. Уведомления тренерам (email / Telegram — за день до матча)
3. ~~Регистрация команд / заявки / трансферы~~ ✅ v1.0.0
4. ~~Личный зачёт поэтов (рейтинги, профили)~~ ✅ v0.9.0
5. ~~Экран для проектора (`/match/[id]/live`)~~ ✅ v0.9.0
6. ~~Зрительское голосование (народное жюри)~~ ✅ v0.9.0
7. ~~Защита от повторных судей + аналитика судейства~~ ✅ v0.9.0
8. ~~Протокол матча (PDF)~~ ✅ v0.9.0
9. ~~Страницы стадионов с картой~~ ✅ v0.8.0
10. ~~iCal-экспорт расписания~~ ✅ v0.9.0

### Фаза 3 — КБС-Москва 2026

1. ~~Универсальная турнирная модель (ROUND_ROBIN + SWISS)~~ ✅ v1.2.0
2. ~~Швейцарская система (генерация раундов)~~ ✅ v1.2.0
3. ~~Double Elimination плей-офф (сетка 16 команд)~~ ✅ v1.2.0
4. ~~Обновлённые карточки (2 жёлтых=красная, диссы, отстранения)~~ ✅ v1.2.0
5. ~~Заместитель тренера (ASSISTANT_COACH)~~ ✅ v1.2.0
6. ~~Тай-брейк при ничьей (11-я пара)~~ ✅ v1.2.0
7. ~~Интеграция карточек в live scoring (автоматическая красная при 2 жёлтых)~~ ✅ v1.6.0
8. ~~Автопродвижение в сетке (победитель → следующий слот)~~ ✅ v1.6.0
9. ~~Обновление standings для SWISS формата (W-L запись)~~ ✅ v1.6.0

### Фаза 4 — Улучшения

1. ~~Новостная лента / блог (обзоры матчей)~~ ✅ v1.3.0
2. ~~Фото привязанные к матчам~~ ✅ v1.1.0
3. Telegram-бот (уведомления о матчах, результаты, расписание)
4. ~~Донаты / призовой фонд~~ ✅ v1.3.0
5. ~~PWA~~ ✅ v1.3.0
6. ~~Mobile UX аудит: hamburger menu, error.tsx в public routes, touch targets в admin~~ ✅ v1.7.0
7. ~~E2E тесты (Playwright, 28 тестов)~~ ✅ v1.7.0
8. ~~Swiss Bracket визуализация (CS2 Major стиль)~~ ✅ v2.1.0
9. ~~Редизайн публичных страниц (hero, таблицы, карточки, header, footer)~~ ✅ v2.3.0
10. ~~Управление пользователями — список, назначение организаторов городов, роли~~ ✅ v2.6.0
11. ~~Мобильная доступность админки — overflowX таблицы, touch targets, responsive диалоги, dark theme borders~~ ✅ v2.7.0

### E2E тесты — покрытие

| Файл                       | Кол-во | Покрытие                                       |
| -------------------------- | ------ | ---------------------------------------------- |
| `01-public.spec.ts`        | 10     | Главная, логотип, секции, навигация            |
| `02-standings.spec.ts`     | 6      | Round-Robin, Swiss W-L, badge, переключение    |
| `03-admin.spec.ts`         | 8      | Auth redirect, дашборд, sidebar, CRUD страницы |
| `04-teams-players.spec.ts` | 4      | Профили команд, поэты, расписание              |
| **Итого**                  | **28** | Публичные + admin + auth                       |

**Запуск:** `nx e2e grandslamcup-e2e -- --project=chromium`
**Auth:** `/api/auth/dev-session` (dev-only, описано ниже)

---

## Preview-авторизация для аудита и e2e

Dev-only endpoint `/api/auth/dev-session` создаёт сессию без OIDC (заблокирован в production).

**Админ в БД:** `admin@grandslamcup.ru`, roles: {ADMIN, USER}

**Использование в preview:**

```javascript
// После preview_start — навигируем на dev-session endpoint:
window.location.href = '/api/auth/dev-session?email=admin@grandslamcup.ru&redirect=/admin'
// → Создаёт Session в БД, подписывает cookie (Hono HMAC-SHA256), редиректит
```

**Для e2e тестов (Playwright):**

```typescript
await page.goto('/api/auth/dev-session?email=admin@grandslamcup.ru&redirect=/admin')
await page.waitForURL('/admin')
```

**Файл:** `src/app/api/auth/dev-session/route.ts`

---

## 10b. Миграция данных — полный контекст

### Источники данных

1. **Tilda (grandslamcup.ru)** — старый сайт, HTML-парсинг через cheerio
   - Cached HTML: `scripts/migrate/cache/` (gitignored)
   - Парсер: `scripts/migrate/extract.ts`
   - Данные: СПб С1 (45 матчей) + С2 (41 матч), 3 команды с составами, 30 стадионов
   - **Устарел** — заменён Telegram-экстракцией (больше данных, адреса площадок)

2. **Telegram "Кубок Большого Слэма" (СПб)** — экспорт канала
   - Источник: `C:\Users\Kami\Downloads\Telegram Desktop\spb\result.json` (6.4 MB)
   - 3284 сообщений, 2925 фото, период: 2024-07-10 — 2026-04-02
   - Покрывает: **СПб С1** (сент-дек 2024), **С2** (дек 2024 - май 2025), **С3** (сент 2025+)
   - Чанки: `scripts/migrate/chunks-spb/chunk-{1..7}.json` (gitignored)
   - AI-экстракция: `scripts/migrate/chunks-spb/chunk-{1..7}-extracted.json` (gitignored)
   - Merged: `scripts/migrate/manual-data/spb-ai-extracted.json` (152 KB)
   - **Очищенные данные: `scripts/migrate/manual-data/spb-clean.json`**

3. **Telegram "Кубок Большого Слэма (Москва)"** — экспорт канала
   - Источник: `C:\Users\Kami\Downloads\Telegram Desktop\slam\result.json` (6.9 MB)
   - 3011 сообщений, 2227 фото, 698 видео, период: 2025-01-29 — 2026-04-03
   - Покрывает: **Москва С1** (февр-дек 2025), **С2** (февр 2026+, в процессе)
   - Чанки: `scripts/migrate/chunks/chunk-{1..6}.json` (gitignored)
   - AI-экстракция: `scripts/migrate/chunks/chunk-{1..6}-extracted.json` (gitignored)
   - Merged: `scripts/migrate/manual-data/moscow-ai-extracted.json` (130 KB)
   - **Очищенные данные: `scripts/migrate/manual-data/moscow-clean.json`**

### Как парсили

1. **Tilda (итерация 1, v1.4.0):** `crawl.ts` скачал HTML → `extract.ts` парсил через cheerio (type=468 расписание, type=431 таблицы, type=524 составы) → `seed.ts` загрузил в БД через pg Pool (raw SQL, ON CONFLICT DO NOTHING).

2. **Telegram (итерация 2):**
   - `prepare-chunks.ts` / `prepare-chunks-spb.ts` — очистка JSON (оставляем {id, date, text}), разбивка на чанки ~146KB
   - 13 параллельных Haiku-агентов (6 Москва + 7 СПб) — AI-экстракция структурированных данных
   - `merge-chunks.ts` / `merge-chunks-spb.ts` — объединение + дедупликация по msgId
   - `clean-extracted.ts` — нормализация имён команд (белый список + алиасы), фильтрация мусора, унификация полей

### Текущее состояние данных

| Метрика  | Москва (clean) | СПб (clean) | Итого |
| -------- | -------------- | ----------- | ----- |
| Матчей   | 90             | 52          | 142   |
| Составов | 159            | 122         | 281   |
| Игроков  | 648            | 503         | 1151  |
| Площадок | 40             | 53          | 93    |
| Профилей | 15             | 584         | 599   |
| Команд   | 26             | 35          | 61    |

### Текущее состояние БД (seed-v2, v1.5.0)

Данные из Telegram AI-экстракции: 2 города (СПб + Москва), 97 площадок, 5 сезонов, 6 лиг, 46 команд, 1136 игроков, 99 матчей, 50 standings, 2335 PlayerTeamSeason.

43 матча пропущены (нет пары команд). Восстановление из ростеров добавило ~30 матчей.

### Белые списки команд

**СПб С1** (12, сент-дек 2024): Шь, Чумные, Обормоты, Блины, Или, Пыжыки, Firma, Софийка, Болт, Бюро, Вином, Ива
**СПб С2** (16, дек 2024 - май 2025, 2 лиги ВЛ+1Л): Чумные, Блины, Или, ПЗК, Обормоты, Менестрели подземелья, 1163, Вином, Бугульма, Пыжыки, Винета, Бюро, Болт, Состав, Апостроф, Веселые ребята
**СПб С3** (сент 2025+): те же + Синий Пушкин, СТИХИ НАРОДА, ДА
**Москва С1** (12+, февр-дек 2025): Бродячие артисты, Шатуны, РЫБА, ЛитПон, СИИ, ДА, Кашалот, Птица поэта, Чатл, Жемчужные, Opezdol's Crew, TERIYAKI SQUAD, Против, Дикпики
**Москва С2** (16, февр 2026+): In Folio, ДА, Кашалот, Манулы, Маски, Метаморфоза, НЕНАХОД НОГИ, НеСТИХай, ОПГ, Поэтория, Прогрев, РЫБА, СТИХИ НАРОДА, Солянка, Чатл, Шатуны

### Переименования команд

- Болт → Вином (СПб, между С1 и С2)
- Софийка → Ива (СПб, между С1 и С2)

### Скрипты миграции

```
scripts/migrate/
├── cache/                    # HTML с Tilda (gitignored)
├── chunks/                   # Москва чанки (gitignored)
├── chunks-spb/               # СПб чанки (gitignored)
├── manual-data/
│   ├── moscow-ai-extracted.json   # Сырые AI-данные Москвы (130 KB)
│   ├── moscow-clean.json          # Очищенные данные Москвы ← ИСПОЛЬЗОВАТЬ
│   ├── spb-ai-extracted.json      # Сырые AI-данные СПб (152 KB)
│   └── spb-clean.json             # Очищенные данные СПб ← ИСПОЛЬЗОВАТЬ
├── crawl.ts                  # Краулер Tilda
├── extract.ts                # Парсер HTML (cheerio) — устарел
├── types.ts                  # Типы данных
├── seed.ts                   # Seed v1 (Tilda → БД) — УСТАРЕЛ
├── prepare-chunks.ts         # Подготовка чанков Москва
├── prepare-chunks-spb.ts     # Подготовка чанков СПб
├── merge-chunks.ts           # Объединение чанков Москва
├── merge-chunks-spb.ts       # Объединение чанков СПб
├── clean-extracted.ts        # Чистка + нормализация
└── seed-v2.ts                # Seed v2 (JSON → БД) — TODO
```

---

## 11. Визуализация швейцарской сетки (Swiss Bracket)

### Контекст

КБС-Москва Сезон 2 использует **швейцарскую систему с элиминацией** (как CS2 Major):

- 16 команд, 5 раундов
- **3 победы → в плей-офф** (Double Elimination)
- **3 поражения → вылет**
- Победители играют с победителями, проигравшие с проигравшими

Это НЕ обычная швейцарка (N раундов → standings). Это **дерево решений**, где команды двигаются по веткам W-L записи. Визуализация должна показывать текущее состояние турнира в реальном времени.

### Структура швейцарки (из правил КБС-Москва 2.0)

```
Раунд 1: 16 команд → 8 пар
  Все проходят дальше (8 победителей 1-0, 8 проигравших 0-1)

Раунд 2:
  Группа 1-0: 8 → 4 пары → 4 команды 2-0, 4 команды 1-1
  Группа 0-1: 8 → 4 пары → 4 команды 1-1, 4 команды 0-2

Раунд 3 (начинаются выбытия):
  Группа 2-0: 4 → 2 пары → 2 команды 3-0 ✅ В ПЛЕЙ-ОФФ, 2 команды 2-1
  Группа 1-1: 8 → 4 пары → 4 команды 2-1, 4 команды 1-2
  Группа 0-2: 4 → 2 пары → 2 команды 1-2, 2 команды 0-3 ❌ ВЫЛЕТ

Раунд 4:
  Группа 2-1: 6 → 3 пары → 3 команды 3-1 ✅ В ПЛЕЙ-ОФФ, 3 команды 2-2
  Группа 1-2: 6 → 3 пары → 3 команды 2-2, 3 команды 1-3 ❌ ВЫЛЕТ

Раунд 5 (финальный):
  Группа 2-2: 6 → 3 пары → 3 команды 3-2 ✅ В ПЛЕЙ-ОФФ, 3 команды 2-3 ❌ ВЫЛЕТ
```

**Итого:**

- 8 команд выходят в плей-офф (2×3-0 + 3×3-1 + 3×3-2)
- 8 команд вылетают (2×0-3 + 3×1-3 + 3×2-3)

### Визуализация (аналог CS2 Major Pick'Em)

```
                    Раунд 1      Раунд 2      Раунд 3      Раунд 4      Раунд 5

3-0 ✅ ПЛЕЙ-ОФФ                               ┌─A─┐
                                  ┌─A─┐       │3-0│✅
                    ┌─A─┐        │2-0│        └───┘
                    │1-0│        └───┘         ┌─B─┐
2-1                 └───┘                      │2-1│        ┌─C─┐
                                  ┌─B─┐       └───┘        │3-1│✅
3-1 ✅ ПЛЕЙ-ОФФ     ┌─B─┐        │1-1│                     └───┘
                    │0-1│        └───┘         ┌─C─┐        ┌─D─┐
1-2                 └───┘                      │1-2│        │2-2│
                                  ┌─C─┐       └───┘        └───┘
3-2 ✅ ПЛЕЙ-ОФФ                   │0-2│        ┌─D─┐               ┌─E─┐
                                  └───┘        │0-3│❌              │3-2│✅
0-3 ❌ ВЫЛЕТ                                   └───┘               └───┘
1-3 ❌ ВЫЛЕТ                                                        ┌─F─┐
2-3 ❌ ВЫЛЕТ                                                        │2-3│❌
                                                                    └───┘
```

Каждый узел — группа команд с одинаковой W-L записью. Внутри узла — список команд и пар матчей.

### Desktop вариант

CSS Grid с 5 колонками (раунды). Каждый узел (W-L группа) — карточка:

- Заголовок: "2-0" / "1-1" / "0-2" с цветовой индикацией (зелёный → красный)
- Список команд в группе
- Пары матчей (если раунд текущий) или результаты (если сыгран)
- Статус: ✅ "В плей-офф" / ❌ "Вылет" / "→ Раунд N"
- SVG коннекторы между раундами (winner → группу выше, loser → группу ниже)

### Mobile вариант

Вертикальный список раундов (Tabs или Accordion):

- Таб на каждый раунд
- Внутри: группы W-L с командами и результатами
- Текущий раунд — открыт по умолчанию

### Данные

Нужна новая модель или расширение существующей:

```
// Вариант 1: вычисляемое на лету из матчей
// Для каждого матча знаем home, away, homeScore, awayScore
// По дате/раунду определяем порядок → строим W-L записи

// Вариант 2: хранить swiss round info в БД
model SwissRound {
  id        String @id @default(cuid())
  seasonId  String
  number    Int    // 1-5

  season Season @relation(...)
  pairs  SwissPair[]

  @@unique([seasonId, number])
}

model SwissPair {
  id            String @id @default(cuid())
  roundId       String
  team1Id       String  // TeamSeason
  team2Id       String  // TeamSeason
  matchId       String? // ссылка на Match (после проведения)
  winsBeforeT1  Int     // W-record команды 1 до этого матча
  lossesBeforeT1 Int    // L-record команды 1 до этого матча

  round SwissRound @relation(...)
}
```

**Рекомендация:** Вариант 1 (вычисляемое) — проще, не нужна миграция. Матчи уже в БД с датами и счётом. Можно вычислить W-L записи на лету для визуализации.

### Привязка матчей к раундам швейцарки

Текущая проблема: матчи в БД привязаны к Tour (один тур на весь сезон), а не к конкретному раунду швейцарки. Нужно:

1. Определить раунд по дате матча и W-L записям команд на момент матча
2. Или: seed-v2 должен проставлять swiss round number при создании матча

Раунды по датам (из данных Москва Сезон 2):

- Раунд 1: 26 февр — 5 мар 2026 (8 матчей)
- Раунд 2: 10-19 мар 2026 (8 матчей)
- Раунд 3: ~23 мар — 2 апр 2026 (8 матчей: 2 в группе 2-0, 4 в группе 1-1, 2 в группе 0-2)
- Раунд 4: будущие (6 матчей)
- Раунд 5: будущие (6 матчей)

### Компонент

```
<SwissBracket seasonId={seasonId} />
```

Серверный компонент:

1. Загружает все матчи сезона
2. Сортирует по дате
3. Вычисляет W-L записи после каждого матча
4. Группирует по раундам (по W-L группам)
5. Рендерит сетку

### Страница

`/[citySlug]/bracket/[seasonSlug]` — если сезон SWISS, показывает SwissBracket. Если есть BracketSlot (DE сетка) — показывает DE bracket. Выбор автоматический.

### Файлы для реализации

| Файл                                                        | Действие                                                  |
| ----------------------------------------------------------- | --------------------------------------------------------- |
| `src/lib/swiss-bracket.ts`                                  | **Создать** — логика вычисления W-L записей и группировки |
| `src/app/_components/swiss-bracket.tsx`                     | **Создать** — визуальный компонент сетки                  |
| `src/app/_components/swiss-bracket-mobile.tsx`              | **Создать** — мобильная версия                            |
| `src/app/(public)/[citySlug]/bracket/[seasonSlug]/page.tsx` | **Обновить** — автовыбор Swiss vs DE                      |
| `scripts/migrate/seed-v2.ts`                                | **Обновить** — проставить swiss round number для матчей   |

### Приоритет

**Высокий** — это главная фича для текущего сезона. Зрители и организаторы хотят видеть кто где в турнирной сетке прямо сейчас.

---

## 12. Открытые вопросы

| #   | Вопрос                                                                                                  | Кого спросить |
| --- | ------------------------------------------------------------------------------------------------------- | ------------- |
| 1   | ~~Карточки: влияют на баллы? Что при красной? 2 жёлтые = красная?~~ ✅ Ответ в правилах Москвы 2.0      | Решён         |
| 2   | Есть ли данные поэт-по-поэту за прошлые сезоны (или только итоговые счета)?                             | Организатор   |
| 3   | ~~Москва — те же правила? Есть ли отличия?~~ ✅ Получены правила КБС-Москва 2.0, реализована поддержка  | Решён         |
| 4   | Домен — оставляем grandslamcup.ru?                                                                      | Организатор   |
| 5   | Нужна ли регистрация зрителей (для будущих фич — донаты, подписки)?                                     | Организатор   |
| 6   | Генератор round-robin расписания — нужен, или организатор составляет вручную?                           | Организатор   |
| 7   | Может ли поэт играть в обоих таймах? На Tilda есть такие данные (Стрельникова — оба тайма одного матча) | Организатор   |
| 8   | ~~Роль «Продюсер команды» — какие права?~~ ✅ Роль не существует в КБС, удалить                         | Решён         |
| 9   | Есть ли страницы московских команд на Tilda? Или только СПб?                                            | Организатор   |
