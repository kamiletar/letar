'use client'

/**
 * Header — заменён на Sidebar (левый фиксированный сайдбар)
 * Компонент возвращает null для обратной совместимости со страницами
 * которые ещё импортируют Header, но теперь навигация управляется через
 * Sidebar в layout.tsx
 */
export const Header = () => null
