---
title: Числовые поля
description: Числовой ввод — Number, Slider, Currency, Percentage, Rating
---

import { DemoContainer } from '@/components/demo-container'
import { SkinCodeFile } from '@/components/skin/skin-code-file'

<DemoContainer demo="number" height={550} />

## Полный пример

Полный sandbox-пример, прочитанный напрямую из исходников `form-develop-app` /
`form-develop-app-shadcn` на сборке — переключите фреймворк/скин, чтобы увидеть то же поле в
разных реализациях.

<SkinCodeFile
  chakra="apps/form-develop-app/src/app/numeric-demo/page.tsx"
  shadcn="apps/form-develop-app-shadcn/src/app/numeric-demo/page.tsx"
  vue="libs/forms-vue-shadcn/demo/examples/number-demo.ts"
  angular="libs/forms-angular/demo/examples/number-demo.ts"
/>

## Number

**API:** `Form.Field.Number`

Числовое поле с кнопками +/-.

```tsx
const Schema = z.object({
  quantity: z.number().min(1).max(100).int().meta({
    ui: { title: 'Количество' },
  }),
})

<Form.Field.Number name="quantity" />
```

### Автоматические ограничения

```tsx
z.number().min(0).max(100)
// → min={0} max={100} step={1} на <input>

z.number().int()
// → step={1} (только целые числа)
```

## Slider

**API:** `Form.Field.Slider`

Ползунок для выбора числа из диапазона.

```tsx
const Schema = z.object({
  volume: z.number().min(0).max(100).meta({
    ui: { title: 'Громкость' },
  }),
})

<Form.Field.Slider name="volume" />
```

### Продвинутые настройки

```tsx
<Form.Field.Slider name="brightness" step={10} showValue />
```

## Currency

**API:** `Form.Field.Currency`

Поле для ввода денежных сумм. `currency` — проп самого компонента, а не часть блока
`meta().ui` в Zod; по умолчанию `'RUB'`.

```tsx
const Schema = z.object({
  price: z.number().min(0).meta({
    ui: { title: 'Цена' },
  }),
})

<Form.Field.Currency name="price" currency="USD" />
```

## Percentage

**API:** `Form.Field.Percentage`

Процентное поле (0-100%) с суффиксом `%`.

```tsx
const Schema = z.object({
  discount: z.number().min(0).max(100).meta({
    ui: { title: 'Скидка' },
  }),
})

<Form.Field.Percentage name="discount" />
```

## Rating

**API:** `Form.Field.Rating`

Оценка звёздами.

```tsx
const Schema = z.object({
  rating: z.number().min(1).max(5).meta({
    ui: { title: 'Оценка' },
  }),
})

<Form.Field.Rating name="rating" />
```

### Варианты

```tsx
// 10 звёзд с половинками
<Form.Field.Rating name="quality" count={10} allowHalf />

// Разные размеры
<Form.Field.Rating name="small" size="sm" />
<Form.Field.Rating name="large" size="lg" />
```
