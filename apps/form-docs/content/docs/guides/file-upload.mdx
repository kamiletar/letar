---
title: File Upload
description: Upload files with button, dropzone, or input variants
---

import { SkinCodeFile } from '@/components/skin/skin-code-file'

## Full example

The complete sandbox example, read directly from the `form-develop-app` / `form-develop-app-shadcn`
source at build time. Both sources demonstrate `Form.Field.FileUpload`; the shadcn source
(`specialized-demo`) additionally shows ColorPicker, Editable and Signature (not covered by this
guide) — same overlap pairing as the `Fields → Specialized` category page. No Vue/Angular equivalent
exists yet — those tabs show as disabled.

<SkinCodeFile
  chakra="apps/form-develop-app/src/app/file-upload-demo/page.tsx"
  shadcn="apps/form-develop-app-shadcn/src/app/specialized-demo/page.tsx"
/>

## Basic Upload

```tsx
const Schema = z.object({
  avatar: z.any().optional().meta({
    ui: { title: 'Profile Photo' },
  }),
})

<Form.Field.FileUpload name="avatar" accept="image/*" />
```

## Multiple Files

```tsx
<Form.Field.FileUpload name="documents" maxFiles={5} />
```

## Dropzone Variant

```tsx
<Form.Field.FileUpload name="files" variant="dropzone" accept="image/*,.pdf" maxSize={5 * 1024 * 1024} />
```

## Props

| Prop       | Type                     | Description                             |
| ---------- | ------------------------ | --------------------------------------- |
| `accept`   | `string`                 | Accepted file types (`image/*`, `.pdf`) |
| `maxFiles` | `number`                 | Maximum number of files                 |
| `maxSize`  | `number`                 | Maximum file size in bytes              |
| `variant`  | `'button' \| 'dropzone'` | Upload UI variant                       |
