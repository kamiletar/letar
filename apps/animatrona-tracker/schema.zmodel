// ZenStack schema file for Animatrona Tracker
// Платформа для просмотра и поиска IPFS контента
//
// Модели разбиты по доменам в schema/ — см. .claude/docs/zenstack-multifile-schema-circular-imports.md

import "./schema/enums"
import "./schema/auth"
import "./schema/content"
import "./schema/anime"
import "./schema/distribution"
import "./schema/pinning"
import "./schema/library"

datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "./prisma"
}

plugin prisma {
  provider = '@core/prisma'
  output = './src/generated/schema.prisma'
}

// TypeScript plugin (v3) — генерирует schema.ts для ZenStackClient
plugin typescript {
  provider = '@core/typescript'
  output = './src/generated'
}

// TODO: включить после исправления @zenstackhq/tanstack-query exports
// plugin hooks {
//   provider = '@zenstackhq/tanstack-query'
//   output = "./src/generated/hooks"
//   target = "react"
// }

// Access control plugin (v3)
plugin policy {
  provider = '@zenstackhq/plugin-policy'
}

plugin formSchema {
  provider = '../../libs/zenstack-form-plugin/dist/index.js'
  output = './src/generated/form-schemas'
}
