'use client'

import { createForm } from '@letar/forms'

/**
 * App-specific форма для Ключницы (auth-hub).
 *
 * Без extraSelects/Comboboxes — доменных enum-сущностей для форм пока нет.
 */
export const AuthHubForm = createForm()
