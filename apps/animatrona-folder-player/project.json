{
  "name": "animatrona-folder-player",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/animatrona-folder-player",
  "projectType": "application",
  "tags": ["type:app", "scope:animatrona-folder-player", "owner:letar", "platform:electron"],
  "targets": {
    "dev": {
      "command": "node scripts/dev.js",
      "options": {
        "cwd": "apps/animatrona-folder-player"
      }
    },
    "build:win": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "next build renderer",
          "npx webpack --config main/webpack.config.js",
          "npx electron-builder --win"
        ],
        "cwd": "apps/animatrona-folder-player",
        "parallel": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "apps/animatrona-folder-player/main/**/*.{ts,tsx}",
          "apps/animatrona-folder-player/renderer/**/*.{ts,tsx}"
        ]
      }
    },
    "typecheck:tsgo": {
      "command": "tsgo --project apps/animatrona-folder-player/tsconfig.json --noEmit"
    },
    "format": {
      "command": "dprint fmt apps/animatrona-folder-player/**/*.{ts,tsx,json}"
    }
  }
}
