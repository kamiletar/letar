/**
 * @letar/forms-react/testing — минимальный харнесс для изолированного рендера полей
 * в тестах UI-скинов (`@letar/forms-shadcn` и т.п.), без полного `createForm()`.
 */
export { TestForm } from './test-form'
