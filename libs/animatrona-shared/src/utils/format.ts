/**
 * Утилиты форматирования — реэкспорт из @letar/animatrona-utils
 *
 * Общие функции для mobile, TV и desktop приложений.
 * Сохраняем обратную совместимость: все потребители этого модуля
 * продолжают импортировать из '@letar/animatrona-shared'.
 */

export {
  formatBitrate,
  formatBitrateKbps,
  formatBytes,
  formatChannels,
  formatDuration,
  formatDurationHuman,
  formatDurationMs,
  formatFps,
} from '@letar/animatrona-utils'
