/// <reference types="vitest" />
<% if (react) { %>import react from '@vitejs/plugin-react'
<% } %>import { defineConfig } from 'vitest/config'

export default defineConfig({
<% if (react) { %>  plugins: [react()],
<% } %>  cacheDir: '../../node_modules/.vitest/<%= name %>',
  test: {
    name: '@letar/<%= name %>',
    environment: '<%= react ? 'jsdom' : 'node' %>',
    globals: true,
<% if (react) { %>    setupFiles: ['./vitest.setup.ts'],
<% } %>    include: ['src/**/*.{test,spec}.{ts,tsx}'],
    exclude: ['node_modules', 'dist'],
    coverage: {
      provider: 'v8',
      reportsDirectory: '../../coverage/libs/<%= name %>',
      reporter: ['text', 'json', 'html'],
    },
    clearMocks: true,
    restoreMocks: true,
  },
})
