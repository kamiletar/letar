{
  "$schema": "http://json-schema.org/schema",
  "$id": "NewApp",
  "title": "New App Generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Имя приложения (папка apps/<name>), kebab-case",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Имя нового Next.js приложения (apps/<name>)?"
    },
    "port": {
      "type": "number",
      "description": "Dev-порт (apps/<name>/.env). По умолчанию — следующий свободный 3xxx порт среди apps/*/.env"
    },
    "displayName": {
      "type": "string",
      "description": "Человекочитаемое имя (title, metadata). По умолчанию — Title Case от name"
    },
    "description": {
      "type": "string",
      "description": "Описание приложения (README.md, metadata). По умолчанию — заглушка"
    },
    "private": {
      "type": "boolean",
      "description": "Приложение приватное (будет отдельным git submodule) — влияет только на подсказки после генерации",
      "default": false
    }
  },
  "required": ["name"]
}
