/**
 * Реализация вынесена в @letar/forms-core (Фаза 7.1, Этап 3в, dependency-free ядро) —
 * этот файл только реэкспортирует, чтобы публичный путь `./providers` не менялся для потребителей.
 */
export { createDaDataProvider } from '@letar/forms-core/address'
export type { AddressProvider, AddressSuggestion, SuggestionOptions } from '@letar/forms-core/address'
