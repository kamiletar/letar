// Эта библиотека не содержит TS-кода — только переиспользуемые .zmodel-фрагменты
// (см. src/better-auth.zmodel). Импортируются напрямую в schema.zmodel приложений
// через относительный путь, а не через модульную систему TypeScript.
export {}
