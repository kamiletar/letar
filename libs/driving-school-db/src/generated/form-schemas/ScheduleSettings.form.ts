// AUTO-GENERATED by @letar/zenstack-form-plugin
// DO NOT EDIT MANUALLY

import { z } from 'zod/v4'

/**
 * Create schema for ScheduleSettings with UI metadata.
 */
export const ScheduleSettingsCreateFormSchema = z.object({
  instructorId: z.string(),
  lessonDuration: z.number().int().min(30).max(180).multipleOf(15).default(90)
    .meta({
      ui: { title: 'Длительность занятия (мин)' }
    }),
  breakDuration: z.number().int().min(0).max(60).multipleOf(5).default(15)
    .meta({
      ui: { title: 'Перерыв между занятиями (мин)' }
    }),
  planningHorizon: z.number().int().min(1).max(30).default(7)
    .meta({
      ui: { title: 'Горизонт планирования (дней)' }
    })
})

/**
 * Update schema for ScheduleSettings (all fields optional).
 */
export const ScheduleSettingsUpdateFormSchema = ScheduleSettingsCreateFormSchema.partial()

/**
 * Fields excluded from forms.
 */
export const ScheduleSettingsExcludedFields = ['id', 'instructor', 'workingHours', 'customBreaks', 'updatedAt'] as const

/**
 * Types.
 */
export type ScheduleSettingsCreateForm = z.infer<typeof ScheduleSettingsCreateFormSchema>
export type ScheduleSettingsUpdateForm = z.infer<typeof ScheduleSettingsUpdateFormSchema>
