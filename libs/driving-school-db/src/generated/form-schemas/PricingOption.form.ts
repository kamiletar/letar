// AUTO-GENERATED by @letar/zenstack-form-plugin
// DO NOT EDIT MANUALLY

import { z } from 'zod/v4'
import { VehicleOwnerFormSchema } from './enums/VehicleOwner.form'

/**
 * Create schema for PricingOption with UI metadata.
 */
export const PricingOptionCreateFormSchema = z.object({
  lessonTypeId: z.string(),
  vehicleOwner: VehicleOwnerFormSchema
    .meta({
      ui: { title: 'Чья машина', fieldType: 'radioCard' }
    }),
  vehicleId: z.string().nullable().optional()
    .meta({
      ui: { title: 'Автомобиль', fieldType: 'select', fieldProps: { relation: {"labelField":"brand"} } }
    }),
  lessonsCount: z.number().int().min(1).max(50).default(1)
    .meta({
      ui: { title: 'Количество занятий' }
    }),
  pricePerLesson: z.number().min(0)
    .meta({
      ui: { title: 'Цена за занятие (₽)' }
    }),
  discountPercent: z.number().int().min(0).max(50).default(0)
    .meta({
      ui: { title: 'Скидка за курс (%)' }
    }),
  isActive: z.boolean().default(true)
    .meta({
      ui: { title: 'Активен', fieldType: 'switch' }
    }),
  isPrimary: z.boolean().default(false)
    .meta({
      ui: { title: 'Основной вариант', fieldType: 'switch' }
    })
})

/**
 * Update schema for PricingOption (all fields optional).
 */
export const PricingOptionUpdateFormSchema = PricingOptionCreateFormSchema.partial()

/**
 * Fields excluded from forms.
 */
export const PricingOptionExcludedFields = ['id', 'lessonType', 'vehicle', 'sortOrder', 'lessons', 'createdAt', 'updatedAt'] as const

/**
 * Types.
 */
export type PricingOptionCreateForm = z.infer<typeof PricingOptionCreateFormSchema>
export type PricingOptionUpdateForm = z.infer<typeof PricingOptionUpdateFormSchema>
