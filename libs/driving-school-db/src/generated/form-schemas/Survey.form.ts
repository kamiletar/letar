// AUTO-GENERATED by @letar/zenstack-form-plugin
// DO NOT EDIT MANUALLY

import { z } from 'zod/v4'
import { SurveyTriggerTypeFormSchema } from './enums/SurveyTriggerType.form'

/**
 * Create schema for Survey with UI metadata.
 */
export const SurveyCreateFormSchema = z.object({
  organizationId: z.string(),
  name: z.string()
    .meta({
      ui: { title: 'Название', placeholder: 'Например: Опрос после обучения' }
    }),
  description: z.string().nullable().optional()
    .meta({
      ui: { title: 'Описание' }
    }),
  triggerType: SurveyTriggerTypeFormSchema
    .meta({
      ui: { title: 'Триггер отправки' }
    }),
  delayDays: z.number().int().default(1)
    .meta({
      ui: { title: 'Отправить через (дней)', placeholder: '1' }
    }),
  expirationDays: z.number().int().default(14)
    .meta({
      ui: { title: 'Срок действия (дней)', placeholder: '14' }
    }),
  isActive: z.boolean().default(true)
    .meta({
      ui: { title: 'Активен' }
    })
})

/**
 * Update schema for Survey (all fields optional).
 */
export const SurveyUpdateFormSchema = SurveyCreateFormSchema.partial()

/**
 * Fields excluded from forms.
 */
export const SurveyExcludedFields = ['id', 'organization', 'responses', 'createdAt', 'updatedAt'] as const

/**
 * Types.
 */
export type SurveyCreateForm = z.infer<typeof SurveyCreateFormSchema>
export type SurveyUpdateForm = z.infer<typeof SurveyUpdateFormSchema>
