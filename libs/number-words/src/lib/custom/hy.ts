/**
 * Армянский язык — числительные.
 * Армянский — отдельная ветвь индоевропейских языков с уникальным алфавитом.
 *
 * TODO: полная реализация требует верификации с носителем языка.
 * Пока возвращает цифровую запись.
 */

/** Кардинальное числительное (армянский) */
export function cardinal(n: number): string {
  return String(n)
}

/** Порядковое числительное (армянский) */
export function ordinal(n: number): string {
  return String(n)
}
